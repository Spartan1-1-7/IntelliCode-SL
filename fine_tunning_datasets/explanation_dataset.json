[
  {
    "input": "def gcd_calculator_0(left, right):\n    while right != 0:\n        left, right = right, left % right\n    return left\n",
    "output": "The function gcd_calculator_0 uses the Euclidean algorithm to transform left and right into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts left, right and returns the greatest common divisor as an integer. One notable detail is that it returns the final left value when right becomes zero."
  },
  {
    "input": "def reverse_text_0(text, preserve_case=False):\n    if preserve_case:\n        return text[::-1]\n    return text.strip()[::-1]\n",
    "output": "This class reverse_text_0 uses slice-based string reversal to transform text and preserve_case into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, preserve_case and returns the reversed string. One notable detail is that it optionally strips surrounding whitespace before reversing."
  },
  {
    "input": "def dedupe_values_0(items, keep_order=True):\n    seen = set()\n    output = []\n    for item in items:\n        if item not in seen:\n            seen.add(item)\n            output.append(item)\n    return output if keep_order else sorted(set(items))\n",
    "output": "The algorithm dedupe_values_0 uses order-preserving deduplication with a set to transform items and keep_order into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, keep_order and returns a list of unique values. One notable detail is that the ordered branch preserves the first appearance of each item."
  },
  {
    "input": "def factorial_recursive_0(n, depth=0):\n    if n <= 1:\n        return 1\n    return n * factorial_recursive_0(n - 1, depth + 1)\n",
    "output": "At its core factorial_recursive_0 uses recursive multiplication to transform n and depth into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts n, depth and returns the factorial of n as an integer. One notable detail is that it uses the n <= 1 base case, so zero and one both return one."
  },
  {
    "input": "class BankAccount_0:\n    def __init__(self, owner, balance=0):\n        self.owner = owner\n        self.balance = balance\n\n    def deposit(self, amount):\n        self.balance += amount\n        return self.balance\n\n    def withdraw(self, amount):\n        if amount > self.balance:\n            raise ValueError('insufficient funds')\n        self.balance -= amount\n        return self.balance\n",
    "output": "Under the hood BankAccount_0 uses an object-oriented balance manager to transform owner and balance into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts owner, balance and returns the updated account balance. One notable detail is that withdrawals larger than the current balance raise ValueError."
  },
  {
    "input": "def bubble_sort_0(values, descending=False):\n    for end in range(len(values) - 1, 0, -1):\n        for index in range(end):\n            if (values[index] > values[index + 1]) != descending:\n                values[index], values[index + 1] = values[index + 1], values[index]\n    return values\n",
    "output": "When called with bubble_sort_0 uses bubble sort to transform values and descending into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, descending and returns the list sorted in place and returned. One notable detail is that it works in quadratic time and mutates the original list."
  },
  {
    "input": "def binary_search_0(values, target, offset=0):\n    left = 0\n    right = len(values) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if values[mid] == target:\n            return offset + mid\n        if values[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return -1\n",
    "output": "This utility binary_search_0 uses binary search to transform values and target into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, target and returns the index of target or negative one. One notable detail is that the search assumes values is already sorted."
  },
  {
    "input": "def timing_decorator_0(func):\n    import functools, time\n    @functools.wraps(func)\n    def wrapper(*args, **kwargs):\n        start = time.perf_counter()\n        result = func(*args, **kwargs)\n        print(func.__name__, time.perf_counter() - start)\n        return result\n    return wrapper\n",
    "output": "The method timing_decorator_0 uses a timing wrapper to transform func and args into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts func, args and returns the wrapped function result after printing elapsed time. One notable detail is that it preserves the original function metadata with functools.wraps."
  },
  {
    "input": "def chunked_0(items, size):\n    for index in range(0, len(items), size):\n        yield items[index:index + size]\n",
    "output": "Built around chunked_0 uses generator-based chunking to transform items and size into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, size and returns lists of items in successive chunks. One notable detail is that the final chunk may be shorter if the length is not divisible by size."
  },
  {
    "input": "class file_context_0:\n    def __init__(self, path):\n        self.path = path\n\n    def __enter__(self):\n        self.handle = open(self.path, 'r', encoding='utf-8')\n        return self.handle\n\n    def __exit__(self, exc_type, exc, tb):\n        self.handle.close()\n        return False\n",
    "output": "Designed to file_context_0 uses a context manager to transform path and self.handle into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts path, self.handle and returns an open file handle during the with-block. One notable detail is that exceptions are not suppressed because __exit__ returns False."
  },
  {
    "input": "def safe_parse_0(text, fallback=0):\n    try:\n        return int(text)\n    except (TypeError, ValueError):\n        return fallback\n",
    "output": "Working through safe_parse_0 uses exception handling with a fallback to transform text and fallback into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, fallback and returns an integer when parsing succeeds, otherwise the fallback value. One notable detail is that TypeError and ValueError both map to the fallback path."
  },
  {
    "input": "def map_filter_reduce_0(values, factor=2):\n    from functools import reduce\n    doubled = list(map(lambda value: value * factor, values))\n    positives = list(filter(lambda value: value > 0, doubled))\n    return reduce(lambda acc, value: acc + value, positives, 0)\n",
    "output": "The generator map_filter_reduce_0 uses higher-order functions to transform values and factor into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, factor and returns the reduced total as a number. One notable detail is that empty inputs return zero because reduce is seeded with an initial value."
  },
  {
    "input": "class Animal_0:\n    def speak(self):\n        return 'sound'\n\nclass Dog_0(Animal_0):\n    def speak(self):\n        return 'woof'\n",
    "output": "This decorator Animal_0 and Dog_0 uses inheritance and method overriding to transform self and Animal_0 into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts self, Animal_0 and returns a sound string from the overridden speak method. One notable detail is that the derived class returns a dog-specific sound instead of the base sound."
  },
  {
    "input": "async def async_collect_0(items, delay=0):\n    import asyncio\n    results = []\n    for item in items:\n        await asyncio.sleep(delay)\n        results.append(item)\n    return results\n",
    "output": "The recursive async_collect_0 uses async iteration with await to transform items and delay into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, delay and returns the collected list after awaiting each item. One notable detail is that it yields control back to the event loop on every pass."
  },
  {
    "input": "class SingletonFactory_0:\n    _instance = None\n\n    def __new__(cls, *args, **kwargs):\n        if cls._instance is None:\n            cls._instance = super().__new__(cls)\n        return cls._instance\n",
    "output": "Inside this class SingletonFactory_0 uses the singleton pattern to transform args and kwargs into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts args, kwargs and returns the same instance every time. One notable detail is that state is shared because __new__ reuses the cached instance."
  },
  {
    "input": "def data_pipeline_0(records, minimum_age=18):\n    filtered = [record for record in records if record['age'] >= minimum_age]\n    normalized = [{**record, 'name': record['name'].strip().title()} for record in filtered]\n    return sorted(normalized, key=lambda record: record['name'])\n",
    "output": "The function data_pipeline_0 uses a filter-transform-sort pipeline to transform records and minimum_age into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts records, minimum_age and returns a sorted list of normalized record dictionaries. One notable detail is that missing age or name keys will still raise KeyError."
  },
  {
    "input": "def module_worker_a_0(numbers):\n    return sum(numbers)\n\ndef module_worker_b_0(numbers):\n    return max(numbers) if numbers else None\n\ndef module_worker_c_0(numbers):\n    return min(numbers) if numbers else None\n",
    "output": "This class module_worker_a_0 uses a small multi-function module to transform numbers and numbers into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts numbers, numbers and returns the computed aggregate returned by each helper. One notable detail is that empty lists cause module_worker_b and module_worker_c to return None."
  },
  {
    "input": "def gcd_calculator_1(left, right):\n    while right != 0:\n        left, right = right, left % right\n    return left\n",
    "output": "The algorithm gcd_calculator_1 uses the Euclidean algorithm to transform left and right into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts left, right and returns the greatest common divisor as an integer. One notable detail is that it returns the final left value when right becomes zero."
  },
  {
    "input": "def reverse_text_1(text, preserve_case=False):\n    if preserve_case:\n        return text[::-1]\n    return text.strip()[::-1]\n",
    "output": "At its core reverse_text_1 uses slice-based string reversal to transform text and preserve_case into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, preserve_case and returns the reversed string. One notable detail is that it optionally strips surrounding whitespace before reversing."
  },
  {
    "input": "def dedupe_values_1(items, keep_order=True):\n    seen = set()\n    output = []\n    for item in items:\n        if item not in seen:\n            seen.add(item)\n            output.append(item)\n    return output if keep_order else sorted(set(items))\n",
    "output": "Under the hood dedupe_values_1 uses order-preserving deduplication with a set to transform items and keep_order into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, keep_order and returns a list of unique values. One notable detail is that the ordered branch preserves the first appearance of each item."
  },
  {
    "input": "def factorial_recursive_1(n, depth=0):\n    if n <= 1:\n        return 1\n    return n * factorial_recursive_1(n - 1, depth + 1)\n",
    "output": "When called with factorial_recursive_1 uses recursive multiplication to transform n and depth into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts n, depth and returns the factorial of n as an integer. One notable detail is that it uses the n <= 1 base case, so zero and one both return one."
  },
  {
    "input": "class BankAccount_1:\n    def __init__(self, owner, balance=0):\n        self.owner = owner\n        self.balance = balance\n\n    def deposit(self, amount):\n        self.balance += amount\n        return self.balance\n\n    def withdraw(self, amount):\n        if amount > self.balance:\n            raise ValueError('insufficient funds')\n        self.balance -= amount\n        return self.balance\n",
    "output": "This utility BankAccount_1 uses an object-oriented balance manager to transform owner and balance into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts owner, balance and returns the updated account balance. One notable detail is that withdrawals larger than the current balance raise ValueError."
  },
  {
    "input": "def bubble_sort_1(values, descending=False):\n    for end in range(len(values) - 1, 0, -1):\n        for index in range(end):\n            if (values[index] > values[index + 1]) != descending:\n                values[index], values[index + 1] = values[index + 1], values[index]\n    return values\n",
    "output": "The method bubble_sort_1 uses bubble sort to transform values and descending into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, descending and returns the list sorted in place and returned. One notable detail is that it works in quadratic time and mutates the original list."
  },
  {
    "input": "def binary_search_1(values, target, offset=0):\n    left = 0\n    right = len(values) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if values[mid] == target:\n            return offset + mid\n        if values[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return -1\n",
    "output": "Built around binary_search_1 uses binary search to transform values and target into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, target and returns the index of target or negative one. One notable detail is that the search assumes values is already sorted."
  },
  {
    "input": "def timing_decorator_1(func):\n    import functools, time\n    @functools.wraps(func)\n    def wrapper(*args, **kwargs):\n        start = time.perf_counter()\n        result = func(*args, **kwargs)\n        print(func.__name__, time.perf_counter() - start)\n        return result\n    return wrapper\n",
    "output": "Designed to timing_decorator_1 uses a timing wrapper to transform func and args into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts func, args and returns the wrapped function result after printing elapsed time. One notable detail is that it preserves the original function metadata with functools.wraps."
  },
  {
    "input": "def chunked_1(items, size):\n    for index in range(0, len(items), size):\n        yield items[index:index + size]\n",
    "output": "Working through chunked_1 uses generator-based chunking to transform items and size into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, size and returns lists of items in successive chunks. One notable detail is that the final chunk may be shorter if the length is not divisible by size."
  },
  {
    "input": "class file_context_1:\n    def __init__(self, path):\n        self.path = path\n\n    def __enter__(self):\n        self.handle = open(self.path, 'r', encoding='utf-8')\n        return self.handle\n\n    def __exit__(self, exc_type, exc, tb):\n        self.handle.close()\n        return False\n",
    "output": "The generator file_context_1 uses a context manager to transform path and self.handle into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts path, self.handle and returns an open file handle during the with-block. One notable detail is that exceptions are not suppressed because __exit__ returns False."
  },
  {
    "input": "def safe_parse_1(text, fallback=0):\n    try:\n        return int(text)\n    except (TypeError, ValueError):\n        return fallback\n",
    "output": "This decorator safe_parse_1 uses exception handling with a fallback to transform text and fallback into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, fallback and returns an integer when parsing succeeds, otherwise the fallback value. One notable detail is that TypeError and ValueError both map to the fallback path."
  },
  {
    "input": "def map_filter_reduce_1(values, factor=2):\n    from functools import reduce\n    doubled = list(map(lambda value: value * factor, values))\n    positives = list(filter(lambda value: value > 0, doubled))\n    return reduce(lambda acc, value: acc + value, positives, 0)\n",
    "output": "The recursive map_filter_reduce_1 uses higher-order functions to transform values and factor into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, factor and returns the reduced total as a number. One notable detail is that empty inputs return zero because reduce is seeded with an initial value."
  },
  {
    "input": "class Animal_1:\n    def speak(self):\n        return 'sound'\n\nclass Dog_1(Animal_1):\n    def speak(self):\n        return 'woof'\n",
    "output": "Inside this class Animal_1 and Dog_1 uses inheritance and method overriding to transform self and Animal_1 into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts self, Animal_1 and returns a sound string from the overridden speak method. One notable detail is that the derived class returns a dog-specific sound instead of the base sound."
  },
  {
    "input": "async def async_collect_1(items, delay=0):\n    import asyncio\n    results = []\n    for item in items:\n        await asyncio.sleep(delay)\n        results.append(item)\n    return results\n",
    "output": "The function async_collect_1 uses async iteration with await to transform items and delay into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, delay and returns the collected list after awaiting each item. One notable detail is that it yields control back to the event loop on every pass."
  },
  {
    "input": "class SingletonFactory_1:\n    _instance = None\n\n    def __new__(cls, *args, **kwargs):\n        if cls._instance is None:\n            cls._instance = super().__new__(cls)\n        return cls._instance\n",
    "output": "This class SingletonFactory_1 uses the singleton pattern to transform args and kwargs into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts args, kwargs and returns the same instance every time. One notable detail is that state is shared because __new__ reuses the cached instance."
  },
  {
    "input": "def data_pipeline_1(records, minimum_age=18):\n    filtered = [record for record in records if record['age'] >= minimum_age]\n    normalized = [{**record, 'name': record['name'].strip().title()} for record in filtered]\n    return sorted(normalized, key=lambda record: record['name'])\n",
    "output": "The algorithm data_pipeline_1 uses a filter-transform-sort pipeline to transform records and minimum_age into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts records, minimum_age and returns a sorted list of normalized record dictionaries. One notable detail is that missing age or name keys will still raise KeyError."
  },
  {
    "input": "def module_worker_a_1(numbers):\n    return sum(numbers)\n\ndef module_worker_b_1(numbers):\n    return max(numbers) if numbers else None\n\ndef module_worker_c_1(numbers):\n    return min(numbers) if numbers else None\n",
    "output": "At its core module_worker_a_1 uses a small multi-function module to transform numbers and numbers into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts numbers, numbers and returns the computed aggregate returned by each helper. One notable detail is that empty lists cause module_worker_b and module_worker_c to return None."
  },
  {
    "input": "def gcd_calculator_2(left, right):\n    while right != 0:\n        left, right = right, left % right\n    return left\n",
    "output": "Under the hood gcd_calculator_2 uses the Euclidean algorithm to transform left and right into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts left, right and returns the greatest common divisor as an integer. One notable detail is that it returns the final left value when right becomes zero."
  },
  {
    "input": "def reverse_text_2(text, preserve_case=False):\n    if preserve_case:\n        return text[::-1]\n    return text.strip()[::-1]\n",
    "output": "When called with reverse_text_2 uses slice-based string reversal to transform text and preserve_case into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, preserve_case and returns the reversed string. One notable detail is that it optionally strips surrounding whitespace before reversing."
  },
  {
    "input": "def dedupe_values_2(items, keep_order=True):\n    seen = set()\n    output = []\n    for item in items:\n        if item not in seen:\n            seen.add(item)\n            output.append(item)\n    return output if keep_order else sorted(set(items))\n",
    "output": "This utility dedupe_values_2 uses order-preserving deduplication with a set to transform items and keep_order into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, keep_order and returns a list of unique values. One notable detail is that the ordered branch preserves the first appearance of each item."
  },
  {
    "input": "def factorial_recursive_2(n, depth=0):\n    if n <= 1:\n        return 1\n    return n * factorial_recursive_2(n - 1, depth + 1)\n",
    "output": "The method factorial_recursive_2 uses recursive multiplication to transform n and depth into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts n, depth and returns the factorial of n as an integer. One notable detail is that it uses the n <= 1 base case, so zero and one both return one."
  },
  {
    "input": "class BankAccount_2:\n    def __init__(self, owner, balance=0):\n        self.owner = owner\n        self.balance = balance\n\n    def deposit(self, amount):\n        self.balance += amount\n        return self.balance\n\n    def withdraw(self, amount):\n        if amount > self.balance:\n            raise ValueError('insufficient funds')\n        self.balance -= amount\n        return self.balance\n",
    "output": "Built around BankAccount_2 uses an object-oriented balance manager to transform owner and balance into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts owner, balance and returns the updated account balance. One notable detail is that withdrawals larger than the current balance raise ValueError."
  },
  {
    "input": "def bubble_sort_2(values, descending=False):\n    for end in range(len(values) - 1, 0, -1):\n        for index in range(end):\n            if (values[index] > values[index + 1]) != descending:\n                values[index], values[index + 1] = values[index + 1], values[index]\n    return values\n",
    "output": "Designed to bubble_sort_2 uses bubble sort to transform values and descending into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, descending and returns the list sorted in place and returned. One notable detail is that it works in quadratic time and mutates the original list."
  },
  {
    "input": "def binary_search_2(values, target, offset=0):\n    left = 0\n    right = len(values) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if values[mid] == target:\n            return offset + mid\n        if values[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return -1\n",
    "output": "Working through binary_search_2 uses binary search to transform values and target into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, target and returns the index of target or negative one. One notable detail is that the search assumes values is already sorted."
  },
  {
    "input": "def timing_decorator_2(func):\n    import functools, time\n    @functools.wraps(func)\n    def wrapper(*args, **kwargs):\n        start = time.perf_counter()\n        result = func(*args, **kwargs)\n        print(func.__name__, time.perf_counter() - start)\n        return result\n    return wrapper\n",
    "output": "The generator timing_decorator_2 uses a timing wrapper to transform func and args into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts func, args and returns the wrapped function result after printing elapsed time. One notable detail is that it preserves the original function metadata with functools.wraps."
  },
  {
    "input": "def chunked_2(items, size):\n    for index in range(0, len(items), size):\n        yield items[index:index + size]\n",
    "output": "This decorator chunked_2 uses generator-based chunking to transform items and size into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, size and returns lists of items in successive chunks. One notable detail is that the final chunk may be shorter if the length is not divisible by size."
  },
  {
    "input": "class file_context_2:\n    def __init__(self, path):\n        self.path = path\n\n    def __enter__(self):\n        self.handle = open(self.path, 'r', encoding='utf-8')\n        return self.handle\n\n    def __exit__(self, exc_type, exc, tb):\n        self.handle.close()\n        return False\n",
    "output": "The recursive file_context_2 uses a context manager to transform path and self.handle into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts path, self.handle and returns an open file handle during the with-block. One notable detail is that exceptions are not suppressed because __exit__ returns False."
  },
  {
    "input": "def safe_parse_2(text, fallback=0):\n    try:\n        return int(text)\n    except (TypeError, ValueError):\n        return fallback\n",
    "output": "Inside this class safe_parse_2 uses exception handling with a fallback to transform text and fallback into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, fallback and returns an integer when parsing succeeds, otherwise the fallback value. One notable detail is that TypeError and ValueError both map to the fallback path."
  },
  {
    "input": "def map_filter_reduce_2(values, factor=2):\n    from functools import reduce\n    doubled = list(map(lambda value: value * factor, values))\n    positives = list(filter(lambda value: value > 0, doubled))\n    return reduce(lambda acc, value: acc + value, positives, 0)\n",
    "output": "The function map_filter_reduce_2 uses higher-order functions to transform values and factor into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, factor and returns the reduced total as a number. One notable detail is that empty inputs return zero because reduce is seeded with an initial value."
  },
  {
    "input": "class Animal_2:\n    def speak(self):\n        return 'sound'\n\nclass Dog_2(Animal_2):\n    def speak(self):\n        return 'woof'\n",
    "output": "This class Animal_2 and Dog_2 uses inheritance and method overriding to transform self and Animal_2 into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts self, Animal_2 and returns a sound string from the overridden speak method. One notable detail is that the derived class returns a dog-specific sound instead of the base sound."
  },
  {
    "input": "async def async_collect_2(items, delay=0):\n    import asyncio\n    results = []\n    for item in items:\n        await asyncio.sleep(delay)\n        results.append(item)\n    return results\n",
    "output": "The algorithm async_collect_2 uses async iteration with await to transform items and delay into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, delay and returns the collected list after awaiting each item. One notable detail is that it yields control back to the event loop on every pass."
  },
  {
    "input": "class SingletonFactory_2:\n    _instance = None\n\n    def __new__(cls, *args, **kwargs):\n        if cls._instance is None:\n            cls._instance = super().__new__(cls)\n        return cls._instance\n",
    "output": "At its core SingletonFactory_2 uses the singleton pattern to transform args and kwargs into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts args, kwargs and returns the same instance every time. One notable detail is that state is shared because __new__ reuses the cached instance."
  },
  {
    "input": "def data_pipeline_2(records, minimum_age=18):\n    filtered = [record for record in records if record['age'] >= minimum_age]\n    normalized = [{**record, 'name': record['name'].strip().title()} for record in filtered]\n    return sorted(normalized, key=lambda record: record['name'])\n",
    "output": "Under the hood data_pipeline_2 uses a filter-transform-sort pipeline to transform records and minimum_age into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts records, minimum_age and returns a sorted list of normalized record dictionaries. One notable detail is that missing age or name keys will still raise KeyError."
  },
  {
    "input": "def module_worker_a_2(numbers):\n    return sum(numbers)\n\ndef module_worker_b_2(numbers):\n    return max(numbers) if numbers else None\n\ndef module_worker_c_2(numbers):\n    return min(numbers) if numbers else None\n",
    "output": "When called with module_worker_a_2 uses a small multi-function module to transform numbers and numbers into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts numbers, numbers and returns the computed aggregate returned by each helper. One notable detail is that empty lists cause module_worker_b and module_worker_c to return None."
  },
  {
    "input": "def gcd_calculator_3(left, right):\n    while right != 0:\n        left, right = right, left % right\n    return left\n",
    "output": "This utility gcd_calculator_3 uses the Euclidean algorithm to transform left and right into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts left, right and returns the greatest common divisor as an integer. One notable detail is that it returns the final left value when right becomes zero."
  },
  {
    "input": "def reverse_text_3(text, preserve_case=False):\n    if preserve_case:\n        return text[::-1]\n    return text.strip()[::-1]\n",
    "output": "The method reverse_text_3 uses slice-based string reversal to transform text and preserve_case into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, preserve_case and returns the reversed string. One notable detail is that it optionally strips surrounding whitespace before reversing."
  },
  {
    "input": "def dedupe_values_3(items, keep_order=True):\n    seen = set()\n    output = []\n    for item in items:\n        if item not in seen:\n            seen.add(item)\n            output.append(item)\n    return output if keep_order else sorted(set(items))\n",
    "output": "Built around dedupe_values_3 uses order-preserving deduplication with a set to transform items and keep_order into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, keep_order and returns a list of unique values. One notable detail is that the ordered branch preserves the first appearance of each item."
  },
  {
    "input": "def factorial_recursive_3(n, depth=0):\n    if n <= 1:\n        return 1\n    return n * factorial_recursive_3(n - 1, depth + 1)\n",
    "output": "Designed to factorial_recursive_3 uses recursive multiplication to transform n and depth into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts n, depth and returns the factorial of n as an integer. One notable detail is that it uses the n <= 1 base case, so zero and one both return one."
  },
  {
    "input": "class BankAccount_3:\n    def __init__(self, owner, balance=0):\n        self.owner = owner\n        self.balance = balance\n\n    def deposit(self, amount):\n        self.balance += amount\n        return self.balance\n\n    def withdraw(self, amount):\n        if amount > self.balance:\n            raise ValueError('insufficient funds')\n        self.balance -= amount\n        return self.balance\n",
    "output": "Working through BankAccount_3 uses an object-oriented balance manager to transform owner and balance into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts owner, balance and returns the updated account balance. One notable detail is that withdrawals larger than the current balance raise ValueError."
  },
  {
    "input": "def bubble_sort_3(values, descending=False):\n    for end in range(len(values) - 1, 0, -1):\n        for index in range(end):\n            if (values[index] > values[index + 1]) != descending:\n                values[index], values[index + 1] = values[index + 1], values[index]\n    return values\n",
    "output": "The generator bubble_sort_3 uses bubble sort to transform values and descending into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, descending and returns the list sorted in place and returned. One notable detail is that it works in quadratic time and mutates the original list."
  },
  {
    "input": "def binary_search_3(values, target, offset=0):\n    left = 0\n    right = len(values) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if values[mid] == target:\n            return offset + mid\n        if values[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return -1\n",
    "output": "This decorator binary_search_3 uses binary search to transform values and target into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, target and returns the index of target or negative one. One notable detail is that the search assumes values is already sorted."
  },
  {
    "input": "def timing_decorator_3(func):\n    import functools, time\n    @functools.wraps(func)\n    def wrapper(*args, **kwargs):\n        start = time.perf_counter()\n        result = func(*args, **kwargs)\n        print(func.__name__, time.perf_counter() - start)\n        return result\n    return wrapper\n",
    "output": "The recursive timing_decorator_3 uses a timing wrapper to transform func and args into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts func, args and returns the wrapped function result after printing elapsed time. One notable detail is that it preserves the original function metadata with functools.wraps."
  },
  {
    "input": "def chunked_3(items, size):\n    for index in range(0, len(items), size):\n        yield items[index:index + size]\n",
    "output": "Inside this class chunked_3 uses generator-based chunking to transform items and size into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, size and returns lists of items in successive chunks. One notable detail is that the final chunk may be shorter if the length is not divisible by size."
  },
  {
    "input": "class file_context_3:\n    def __init__(self, path):\n        self.path = path\n\n    def __enter__(self):\n        self.handle = open(self.path, 'r', encoding='utf-8')\n        return self.handle\n\n    def __exit__(self, exc_type, exc, tb):\n        self.handle.close()\n        return False\n",
    "output": "The function file_context_3 uses a context manager to transform path and self.handle into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts path, self.handle and returns an open file handle during the with-block. One notable detail is that exceptions are not suppressed because __exit__ returns False."
  },
  {
    "input": "def safe_parse_3(text, fallback=0):\n    try:\n        return int(text)\n    except (TypeError, ValueError):\n        return fallback\n",
    "output": "This class safe_parse_3 uses exception handling with a fallback to transform text and fallback into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, fallback and returns an integer when parsing succeeds, otherwise the fallback value. One notable detail is that TypeError and ValueError both map to the fallback path."
  },
  {
    "input": "def map_filter_reduce_3(values, factor=2):\n    from functools import reduce\n    doubled = list(map(lambda value: value * factor, values))\n    positives = list(filter(lambda value: value > 0, doubled))\n    return reduce(lambda acc, value: acc + value, positives, 0)\n",
    "output": "The algorithm map_filter_reduce_3 uses higher-order functions to transform values and factor into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, factor and returns the reduced total as a number. One notable detail is that empty inputs return zero because reduce is seeded with an initial value."
  },
  {
    "input": "class Animal_3:\n    def speak(self):\n        return 'sound'\n\nclass Dog_3(Animal_3):\n    def speak(self):\n        return 'woof'\n",
    "output": "At its core Animal_3 and Dog_3 uses inheritance and method overriding to transform self and Animal_3 into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts self, Animal_3 and returns a sound string from the overridden speak method. One notable detail is that the derived class returns a dog-specific sound instead of the base sound."
  },
  {
    "input": "async def async_collect_3(items, delay=0):\n    import asyncio\n    results = []\n    for item in items:\n        await asyncio.sleep(delay)\n        results.append(item)\n    return results\n",
    "output": "Under the hood async_collect_3 uses async iteration with await to transform items and delay into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, delay and returns the collected list after awaiting each item. One notable detail is that it yields control back to the event loop on every pass."
  },
  {
    "input": "class SingletonFactory_3:\n    _instance = None\n\n    def __new__(cls, *args, **kwargs):\n        if cls._instance is None:\n            cls._instance = super().__new__(cls)\n        return cls._instance\n",
    "output": "When called with SingletonFactory_3 uses the singleton pattern to transform args and kwargs into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts args, kwargs and returns the same instance every time. One notable detail is that state is shared because __new__ reuses the cached instance."
  },
  {
    "input": "def data_pipeline_3(records, minimum_age=18):\n    filtered = [record for record in records if record['age'] >= minimum_age]\n    normalized = [{**record, 'name': record['name'].strip().title()} for record in filtered]\n    return sorted(normalized, key=lambda record: record['name'])\n",
    "output": "This utility data_pipeline_3 uses a filter-transform-sort pipeline to transform records and minimum_age into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts records, minimum_age and returns a sorted list of normalized record dictionaries. One notable detail is that missing age or name keys will still raise KeyError."
  },
  {
    "input": "def module_worker_a_3(numbers):\n    return sum(numbers)\n\ndef module_worker_b_3(numbers):\n    return max(numbers) if numbers else None\n\ndef module_worker_c_3(numbers):\n    return min(numbers) if numbers else None\n",
    "output": "The method module_worker_a_3 uses a small multi-function module to transform numbers and numbers into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts numbers, numbers and returns the computed aggregate returned by each helper. One notable detail is that empty lists cause module_worker_b and module_worker_c to return None."
  },
  {
    "input": "def gcd_calculator_4(left, right):\n    while right != 0:\n        left, right = right, left % right\n    return left\n",
    "output": "Built around gcd_calculator_4 uses the Euclidean algorithm to transform left and right into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts left, right and returns the greatest common divisor as an integer. One notable detail is that it returns the final left value when right becomes zero."
  },
  {
    "input": "def reverse_text_4(text, preserve_case=False):\n    if preserve_case:\n        return text[::-1]\n    return text.strip()[::-1]\n",
    "output": "Designed to reverse_text_4 uses slice-based string reversal to transform text and preserve_case into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, preserve_case and returns the reversed string. One notable detail is that it optionally strips surrounding whitespace before reversing."
  },
  {
    "input": "def dedupe_values_4(items, keep_order=True):\n    seen = set()\n    output = []\n    for item in items:\n        if item not in seen:\n            seen.add(item)\n            output.append(item)\n    return output if keep_order else sorted(set(items))\n",
    "output": "Working through dedupe_values_4 uses order-preserving deduplication with a set to transform items and keep_order into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, keep_order and returns a list of unique values. One notable detail is that the ordered branch preserves the first appearance of each item."
  },
  {
    "input": "def factorial_recursive_4(n, depth=0):\n    if n <= 1:\n        return 1\n    return n * factorial_recursive_4(n - 1, depth + 1)\n",
    "output": "The generator factorial_recursive_4 uses recursive multiplication to transform n and depth into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts n, depth and returns the factorial of n as an integer. One notable detail is that it uses the n <= 1 base case, so zero and one both return one."
  },
  {
    "input": "class BankAccount_4:\n    def __init__(self, owner, balance=0):\n        self.owner = owner\n        self.balance = balance\n\n    def deposit(self, amount):\n        self.balance += amount\n        return self.balance\n\n    def withdraw(self, amount):\n        if amount > self.balance:\n            raise ValueError('insufficient funds')\n        self.balance -= amount\n        return self.balance\n",
    "output": "This decorator BankAccount_4 uses an object-oriented balance manager to transform owner and balance into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts owner, balance and returns the updated account balance. One notable detail is that withdrawals larger than the current balance raise ValueError."
  },
  {
    "input": "def bubble_sort_4(values, descending=False):\n    for end in range(len(values) - 1, 0, -1):\n        for index in range(end):\n            if (values[index] > values[index + 1]) != descending:\n                values[index], values[index + 1] = values[index + 1], values[index]\n    return values\n",
    "output": "The recursive bubble_sort_4 uses bubble sort to transform values and descending into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, descending and returns the list sorted in place and returned. One notable detail is that it works in quadratic time and mutates the original list."
  },
  {
    "input": "def binary_search_4(values, target, offset=0):\n    left = 0\n    right = len(values) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if values[mid] == target:\n            return offset + mid\n        if values[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return -1\n",
    "output": "Inside this class binary_search_4 uses binary search to transform values and target into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, target and returns the index of target or negative one. One notable detail is that the search assumes values is already sorted."
  },
  {
    "input": "def timing_decorator_4(func):\n    import functools, time\n    @functools.wraps(func)\n    def wrapper(*args, **kwargs):\n        start = time.perf_counter()\n        result = func(*args, **kwargs)\n        print(func.__name__, time.perf_counter() - start)\n        return result\n    return wrapper\n",
    "output": "The function timing_decorator_4 uses a timing wrapper to transform func and args into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts func, args and returns the wrapped function result after printing elapsed time. One notable detail is that it preserves the original function metadata with functools.wraps."
  },
  {
    "input": "def chunked_4(items, size):\n    for index in range(0, len(items), size):\n        yield items[index:index + size]\n",
    "output": "This class chunked_4 uses generator-based chunking to transform items and size into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, size and returns lists of items in successive chunks. One notable detail is that the final chunk may be shorter if the length is not divisible by size."
  },
  {
    "input": "class file_context_4:\n    def __init__(self, path):\n        self.path = path\n\n    def __enter__(self):\n        self.handle = open(self.path, 'r', encoding='utf-8')\n        return self.handle\n\n    def __exit__(self, exc_type, exc, tb):\n        self.handle.close()\n        return False\n",
    "output": "The algorithm file_context_4 uses a context manager to transform path and self.handle into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts path, self.handle and returns an open file handle during the with-block. One notable detail is that exceptions are not suppressed because __exit__ returns False."
  },
  {
    "input": "def safe_parse_4(text, fallback=0):\n    try:\n        return int(text)\n    except (TypeError, ValueError):\n        return fallback\n",
    "output": "At its core safe_parse_4 uses exception handling with a fallback to transform text and fallback into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, fallback and returns an integer when parsing succeeds, otherwise the fallback value. One notable detail is that TypeError and ValueError both map to the fallback path."
  },
  {
    "input": "def map_filter_reduce_4(values, factor=2):\n    from functools import reduce\n    doubled = list(map(lambda value: value * factor, values))\n    positives = list(filter(lambda value: value > 0, doubled))\n    return reduce(lambda acc, value: acc + value, positives, 0)\n",
    "output": "Under the hood map_filter_reduce_4 uses higher-order functions to transform values and factor into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, factor and returns the reduced total as a number. One notable detail is that empty inputs return zero because reduce is seeded with an initial value."
  },
  {
    "input": "class Animal_4:\n    def speak(self):\n        return 'sound'\n\nclass Dog_4(Animal_4):\n    def speak(self):\n        return 'woof'\n",
    "output": "When called with Animal_4 and Dog_4 uses inheritance and method overriding to transform self and Animal_4 into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts self, Animal_4 and returns a sound string from the overridden speak method. One notable detail is that the derived class returns a dog-specific sound instead of the base sound."
  },
  {
    "input": "async def async_collect_4(items, delay=0):\n    import asyncio\n    results = []\n    for item in items:\n        await asyncio.sleep(delay)\n        results.append(item)\n    return results\n",
    "output": "This utility async_collect_4 uses async iteration with await to transform items and delay into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, delay and returns the collected list after awaiting each item. One notable detail is that it yields control back to the event loop on every pass."
  },
  {
    "input": "class SingletonFactory_4:\n    _instance = None\n\n    def __new__(cls, *args, **kwargs):\n        if cls._instance is None:\n            cls._instance = super().__new__(cls)\n        return cls._instance\n",
    "output": "The method SingletonFactory_4 uses the singleton pattern to transform args and kwargs into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts args, kwargs and returns the same instance every time. One notable detail is that state is shared because __new__ reuses the cached instance."
  },
  {
    "input": "def data_pipeline_4(records, minimum_age=18):\n    filtered = [record for record in records if record['age'] >= minimum_age]\n    normalized = [{**record, 'name': record['name'].strip().title()} for record in filtered]\n    return sorted(normalized, key=lambda record: record['name'])\n",
    "output": "Built around data_pipeline_4 uses a filter-transform-sort pipeline to transform records and minimum_age into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts records, minimum_age and returns a sorted list of normalized record dictionaries. One notable detail is that missing age or name keys will still raise KeyError."
  },
  {
    "input": "def module_worker_a_4(numbers):\n    return sum(numbers)\n\ndef module_worker_b_4(numbers):\n    return max(numbers) if numbers else None\n\ndef module_worker_c_4(numbers):\n    return min(numbers) if numbers else None\n",
    "output": "Designed to module_worker_a_4 uses a small multi-function module to transform numbers and numbers into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts numbers, numbers and returns the computed aggregate returned by each helper. One notable detail is that empty lists cause module_worker_b and module_worker_c to return None."
  },
  {
    "input": "def gcd_calculator_5(left, right):\n    while right != 0:\n        left, right = right, left % right\n    return left\n",
    "output": "Working through gcd_calculator_5 uses the Euclidean algorithm to transform left and right into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts left, right and returns the greatest common divisor as an integer. One notable detail is that it returns the final left value when right becomes zero."
  },
  {
    "input": "def reverse_text_5(text, preserve_case=False):\n    if preserve_case:\n        return text[::-1]\n    return text.strip()[::-1]\n",
    "output": "The generator reverse_text_5 uses slice-based string reversal to transform text and preserve_case into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, preserve_case and returns the reversed string. One notable detail is that it optionally strips surrounding whitespace before reversing."
  },
  {
    "input": "def dedupe_values_5(items, keep_order=True):\n    seen = set()\n    output = []\n    for item in items:\n        if item not in seen:\n            seen.add(item)\n            output.append(item)\n    return output if keep_order else sorted(set(items))\n",
    "output": "This decorator dedupe_values_5 uses order-preserving deduplication with a set to transform items and keep_order into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, keep_order and returns a list of unique values. One notable detail is that the ordered branch preserves the first appearance of each item."
  },
  {
    "input": "def factorial_recursive_5(n, depth=0):\n    if n <= 1:\n        return 1\n    return n * factorial_recursive_5(n - 1, depth + 1)\n",
    "output": "The recursive factorial_recursive_5 uses recursive multiplication to transform n and depth into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts n, depth and returns the factorial of n as an integer. One notable detail is that it uses the n <= 1 base case, so zero and one both return one."
  },
  {
    "input": "class BankAccount_5:\n    def __init__(self, owner, balance=0):\n        self.owner = owner\n        self.balance = balance\n\n    def deposit(self, amount):\n        self.balance += amount\n        return self.balance\n\n    def withdraw(self, amount):\n        if amount > self.balance:\n            raise ValueError('insufficient funds')\n        self.balance -= amount\n        return self.balance\n",
    "output": "Inside this class BankAccount_5 uses an object-oriented balance manager to transform owner and balance into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts owner, balance and returns the updated account balance. One notable detail is that withdrawals larger than the current balance raise ValueError."
  },
  {
    "input": "def bubble_sort_5(values, descending=False):\n    for end in range(len(values) - 1, 0, -1):\n        for index in range(end):\n            if (values[index] > values[index + 1]) != descending:\n                values[index], values[index + 1] = values[index + 1], values[index]\n    return values\n",
    "output": "The function bubble_sort_5 uses bubble sort to transform values and descending into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, descending and returns the list sorted in place and returned. One notable detail is that it works in quadratic time and mutates the original list."
  },
  {
    "input": "def binary_search_5(values, target, offset=0):\n    left = 0\n    right = len(values) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if values[mid] == target:\n            return offset + mid\n        if values[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return -1\n",
    "output": "This class binary_search_5 uses binary search to transform values and target into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, target and returns the index of target or negative one. One notable detail is that the search assumes values is already sorted."
  },
  {
    "input": "def timing_decorator_5(func):\n    import functools, time\n    @functools.wraps(func)\n    def wrapper(*args, **kwargs):\n        start = time.perf_counter()\n        result = func(*args, **kwargs)\n        print(func.__name__, time.perf_counter() - start)\n        return result\n    return wrapper\n",
    "output": "The algorithm timing_decorator_5 uses a timing wrapper to transform func and args into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts func, args and returns the wrapped function result after printing elapsed time. One notable detail is that it preserves the original function metadata with functools.wraps."
  },
  {
    "input": "def chunked_5(items, size):\n    for index in range(0, len(items), size):\n        yield items[index:index + size]\n",
    "output": "At its core chunked_5 uses generator-based chunking to transform items and size into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, size and returns lists of items in successive chunks. One notable detail is that the final chunk may be shorter if the length is not divisible by size."
  },
  {
    "input": "class file_context_5:\n    def __init__(self, path):\n        self.path = path\n\n    def __enter__(self):\n        self.handle = open(self.path, 'r', encoding='utf-8')\n        return self.handle\n\n    def __exit__(self, exc_type, exc, tb):\n        self.handle.close()\n        return False\n",
    "output": "Under the hood file_context_5 uses a context manager to transform path and self.handle into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts path, self.handle and returns an open file handle during the with-block. One notable detail is that exceptions are not suppressed because __exit__ returns False."
  },
  {
    "input": "def safe_parse_5(text, fallback=0):\n    try:\n        return int(text)\n    except (TypeError, ValueError):\n        return fallback\n",
    "output": "When called with safe_parse_5 uses exception handling with a fallback to transform text and fallback into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, fallback and returns an integer when parsing succeeds, otherwise the fallback value. One notable detail is that TypeError and ValueError both map to the fallback path."
  },
  {
    "input": "def map_filter_reduce_5(values, factor=2):\n    from functools import reduce\n    doubled = list(map(lambda value: value * factor, values))\n    positives = list(filter(lambda value: value > 0, doubled))\n    return reduce(lambda acc, value: acc + value, positives, 0)\n",
    "output": "This utility map_filter_reduce_5 uses higher-order functions to transform values and factor into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, factor and returns the reduced total as a number. One notable detail is that empty inputs return zero because reduce is seeded with an initial value."
  },
  {
    "input": "class Animal_5:\n    def speak(self):\n        return 'sound'\n\nclass Dog_5(Animal_5):\n    def speak(self):\n        return 'woof'\n",
    "output": "The method Animal_5 and Dog_5 uses inheritance and method overriding to transform self and Animal_5 into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts self, Animal_5 and returns a sound string from the overridden speak method. One notable detail is that the derived class returns a dog-specific sound instead of the base sound."
  },
  {
    "input": "async def async_collect_5(items, delay=0):\n    import asyncio\n    results = []\n    for item in items:\n        await asyncio.sleep(delay)\n        results.append(item)\n    return results\n",
    "output": "Built around async_collect_5 uses async iteration with await to transform items and delay into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, delay and returns the collected list after awaiting each item. One notable detail is that it yields control back to the event loop on every pass."
  },
  {
    "input": "class SingletonFactory_5:\n    _instance = None\n\n    def __new__(cls, *args, **kwargs):\n        if cls._instance is None:\n            cls._instance = super().__new__(cls)\n        return cls._instance\n",
    "output": "Designed to SingletonFactory_5 uses the singleton pattern to transform args and kwargs into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts args, kwargs and returns the same instance every time. One notable detail is that state is shared because __new__ reuses the cached instance."
  },
  {
    "input": "def data_pipeline_5(records, minimum_age=18):\n    filtered = [record for record in records if record['age'] >= minimum_age]\n    normalized = [{**record, 'name': record['name'].strip().title()} for record in filtered]\n    return sorted(normalized, key=lambda record: record['name'])\n",
    "output": "Working through data_pipeline_5 uses a filter-transform-sort pipeline to transform records and minimum_age into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts records, minimum_age and returns a sorted list of normalized record dictionaries. One notable detail is that missing age or name keys will still raise KeyError."
  },
  {
    "input": "def module_worker_a_5(numbers):\n    return sum(numbers)\n\ndef module_worker_b_5(numbers):\n    return max(numbers) if numbers else None\n\ndef module_worker_c_5(numbers):\n    return min(numbers) if numbers else None\n",
    "output": "The generator module_worker_a_5 uses a small multi-function module to transform numbers and numbers into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts numbers, numbers and returns the computed aggregate returned by each helper. One notable detail is that empty lists cause module_worker_b and module_worker_c to return None."
  },
  {
    "input": "def gcd_calculator_6(left, right):\n    while right != 0:\n        left, right = right, left % right\n    return left\n",
    "output": "This decorator gcd_calculator_6 uses the Euclidean algorithm to transform left and right into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts left, right and returns the greatest common divisor as an integer. One notable detail is that it returns the final left value when right becomes zero."
  },
  {
    "input": "def reverse_text_6(text, preserve_case=False):\n    if preserve_case:\n        return text[::-1]\n    return text.strip()[::-1]\n",
    "output": "The recursive reverse_text_6 uses slice-based string reversal to transform text and preserve_case into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, preserve_case and returns the reversed string. One notable detail is that it optionally strips surrounding whitespace before reversing."
  },
  {
    "input": "def dedupe_values_6(items, keep_order=True):\n    seen = set()\n    output = []\n    for item in items:\n        if item not in seen:\n            seen.add(item)\n            output.append(item)\n    return output if keep_order else sorted(set(items))\n",
    "output": "Inside this class dedupe_values_6 uses order-preserving deduplication with a set to transform items and keep_order into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, keep_order and returns a list of unique values. One notable detail is that the ordered branch preserves the first appearance of each item."
  },
  {
    "input": "def factorial_recursive_6(n, depth=0):\n    if n <= 1:\n        return 1\n    return n * factorial_recursive_6(n - 1, depth + 1)\n",
    "output": "The function factorial_recursive_6 uses recursive multiplication to transform n and depth into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts n, depth and returns the factorial of n as an integer. One notable detail is that it uses the n <= 1 base case, so zero and one both return one."
  },
  {
    "input": "class BankAccount_6:\n    def __init__(self, owner, balance=0):\n        self.owner = owner\n        self.balance = balance\n\n    def deposit(self, amount):\n        self.balance += amount\n        return self.balance\n\n    def withdraw(self, amount):\n        if amount > self.balance:\n            raise ValueError('insufficient funds')\n        self.balance -= amount\n        return self.balance\n",
    "output": "This class BankAccount_6 uses an object-oriented balance manager to transform owner and balance into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts owner, balance and returns the updated account balance. One notable detail is that withdrawals larger than the current balance raise ValueError."
  },
  {
    "input": "def bubble_sort_6(values, descending=False):\n    for end in range(len(values) - 1, 0, -1):\n        for index in range(end):\n            if (values[index] > values[index + 1]) != descending:\n                values[index], values[index + 1] = values[index + 1], values[index]\n    return values\n",
    "output": "The algorithm bubble_sort_6 uses bubble sort to transform values and descending into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, descending and returns the list sorted in place and returned. One notable detail is that it works in quadratic time and mutates the original list."
  },
  {
    "input": "def binary_search_6(values, target, offset=0):\n    left = 0\n    right = len(values) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if values[mid] == target:\n            return offset + mid\n        if values[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return -1\n",
    "output": "At its core binary_search_6 uses binary search to transform values and target into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, target and returns the index of target or negative one. One notable detail is that the search assumes values is already sorted."
  },
  {
    "input": "def timing_decorator_6(func):\n    import functools, time\n    @functools.wraps(func)\n    def wrapper(*args, **kwargs):\n        start = time.perf_counter()\n        result = func(*args, **kwargs)\n        print(func.__name__, time.perf_counter() - start)\n        return result\n    return wrapper\n",
    "output": "Under the hood timing_decorator_6 uses a timing wrapper to transform func and args into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts func, args and returns the wrapped function result after printing elapsed time. One notable detail is that it preserves the original function metadata with functools.wraps."
  },
  {
    "input": "def chunked_6(items, size):\n    for index in range(0, len(items), size):\n        yield items[index:index + size]\n",
    "output": "When called with chunked_6 uses generator-based chunking to transform items and size into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, size and returns lists of items in successive chunks. One notable detail is that the final chunk may be shorter if the length is not divisible by size."
  },
  {
    "input": "class file_context_6:\n    def __init__(self, path):\n        self.path = path\n\n    def __enter__(self):\n        self.handle = open(self.path, 'r', encoding='utf-8')\n        return self.handle\n\n    def __exit__(self, exc_type, exc, tb):\n        self.handle.close()\n        return False\n",
    "output": "This utility file_context_6 uses a context manager to transform path and self.handle into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts path, self.handle and returns an open file handle during the with-block. One notable detail is that exceptions are not suppressed because __exit__ returns False."
  },
  {
    "input": "def safe_parse_6(text, fallback=0):\n    try:\n        return int(text)\n    except (TypeError, ValueError):\n        return fallback\n",
    "output": "The method safe_parse_6 uses exception handling with a fallback to transform text and fallback into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, fallback and returns an integer when parsing succeeds, otherwise the fallback value. One notable detail is that TypeError and ValueError both map to the fallback path."
  },
  {
    "input": "def map_filter_reduce_6(values, factor=2):\n    from functools import reduce\n    doubled = list(map(lambda value: value * factor, values))\n    positives = list(filter(lambda value: value > 0, doubled))\n    return reduce(lambda acc, value: acc + value, positives, 0)\n",
    "output": "Built around map_filter_reduce_6 uses higher-order functions to transform values and factor into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, factor and returns the reduced total as a number. One notable detail is that empty inputs return zero because reduce is seeded with an initial value."
  },
  {
    "input": "class Animal_6:\n    def speak(self):\n        return 'sound'\n\nclass Dog_6(Animal_6):\n    def speak(self):\n        return 'woof'\n",
    "output": "Designed to Animal_6 and Dog_6 uses inheritance and method overriding to transform self and Animal_6 into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts self, Animal_6 and returns a sound string from the overridden speak method. One notable detail is that the derived class returns a dog-specific sound instead of the base sound."
  },
  {
    "input": "async def async_collect_6(items, delay=0):\n    import asyncio\n    results = []\n    for item in items:\n        await asyncio.sleep(delay)\n        results.append(item)\n    return results\n",
    "output": "Working through async_collect_6 uses async iteration with await to transform items and delay into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, delay and returns the collected list after awaiting each item. One notable detail is that it yields control back to the event loop on every pass."
  },
  {
    "input": "class SingletonFactory_6:\n    _instance = None\n\n    def __new__(cls, *args, **kwargs):\n        if cls._instance is None:\n            cls._instance = super().__new__(cls)\n        return cls._instance\n",
    "output": "The generator SingletonFactory_6 uses the singleton pattern to transform args and kwargs into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts args, kwargs and returns the same instance every time. One notable detail is that state is shared because __new__ reuses the cached instance."
  },
  {
    "input": "def data_pipeline_6(records, minimum_age=18):\n    filtered = [record for record in records if record['age'] >= minimum_age]\n    normalized = [{**record, 'name': record['name'].strip().title()} for record in filtered]\n    return sorted(normalized, key=lambda record: record['name'])\n",
    "output": "This decorator data_pipeline_6 uses a filter-transform-sort pipeline to transform records and minimum_age into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts records, minimum_age and returns a sorted list of normalized record dictionaries. One notable detail is that missing age or name keys will still raise KeyError."
  },
  {
    "input": "def module_worker_a_6(numbers):\n    return sum(numbers)\n\ndef module_worker_b_6(numbers):\n    return max(numbers) if numbers else None\n\ndef module_worker_c_6(numbers):\n    return min(numbers) if numbers else None\n",
    "output": "The recursive module_worker_a_6 uses a small multi-function module to transform numbers and numbers into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts numbers, numbers and returns the computed aggregate returned by each helper. One notable detail is that empty lists cause module_worker_b and module_worker_c to return None."
  },
  {
    "input": "def gcd_calculator_7(left, right):\n    while right != 0:\n        left, right = right, left % right\n    return left\n",
    "output": "Inside this class gcd_calculator_7 uses the Euclidean algorithm to transform left and right into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts left, right and returns the greatest common divisor as an integer. One notable detail is that it returns the final left value when right becomes zero."
  },
  {
    "input": "def reverse_text_7(text, preserve_case=False):\n    if preserve_case:\n        return text[::-1]\n    return text.strip()[::-1]\n",
    "output": "The function reverse_text_7 uses slice-based string reversal to transform text and preserve_case into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, preserve_case and returns the reversed string. One notable detail is that it optionally strips surrounding whitespace before reversing."
  },
  {
    "input": "def dedupe_values_7(items, keep_order=True):\n    seen = set()\n    output = []\n    for item in items:\n        if item not in seen:\n            seen.add(item)\n            output.append(item)\n    return output if keep_order else sorted(set(items))\n",
    "output": "This class dedupe_values_7 uses order-preserving deduplication with a set to transform items and keep_order into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, keep_order and returns a list of unique values. One notable detail is that the ordered branch preserves the first appearance of each item."
  },
  {
    "input": "def factorial_recursive_7(n, depth=0):\n    if n <= 1:\n        return 1\n    return n * factorial_recursive_7(n - 1, depth + 1)\n",
    "output": "The algorithm factorial_recursive_7 uses recursive multiplication to transform n and depth into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts n, depth and returns the factorial of n as an integer. One notable detail is that it uses the n <= 1 base case, so zero and one both return one."
  },
  {
    "input": "class BankAccount_7:\n    def __init__(self, owner, balance=0):\n        self.owner = owner\n        self.balance = balance\n\n    def deposit(self, amount):\n        self.balance += amount\n        return self.balance\n\n    def withdraw(self, amount):\n        if amount > self.balance:\n            raise ValueError('insufficient funds')\n        self.balance -= amount\n        return self.balance\n",
    "output": "At its core BankAccount_7 uses an object-oriented balance manager to transform owner and balance into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts owner, balance and returns the updated account balance. One notable detail is that withdrawals larger than the current balance raise ValueError."
  },
  {
    "input": "def bubble_sort_7(values, descending=False):\n    for end in range(len(values) - 1, 0, -1):\n        for index in range(end):\n            if (values[index] > values[index + 1]) != descending:\n                values[index], values[index + 1] = values[index + 1], values[index]\n    return values\n",
    "output": "Under the hood bubble_sort_7 uses bubble sort to transform values and descending into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, descending and returns the list sorted in place and returned. One notable detail is that it works in quadratic time and mutates the original list."
  },
  {
    "input": "def binary_search_7(values, target, offset=0):\n    left = 0\n    right = len(values) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if values[mid] == target:\n            return offset + mid\n        if values[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return -1\n",
    "output": "When called with binary_search_7 uses binary search to transform values and target into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, target and returns the index of target or negative one. One notable detail is that the search assumes values is already sorted."
  },
  {
    "input": "def timing_decorator_7(func):\n    import functools, time\n    @functools.wraps(func)\n    def wrapper(*args, **kwargs):\n        start = time.perf_counter()\n        result = func(*args, **kwargs)\n        print(func.__name__, time.perf_counter() - start)\n        return result\n    return wrapper\n",
    "output": "This utility timing_decorator_7 uses a timing wrapper to transform func and args into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts func, args and returns the wrapped function result after printing elapsed time. One notable detail is that it preserves the original function metadata with functools.wraps."
  },
  {
    "input": "def chunked_7(items, size):\n    for index in range(0, len(items), size):\n        yield items[index:index + size]\n",
    "output": "The method chunked_7 uses generator-based chunking to transform items and size into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, size and returns lists of items in successive chunks. One notable detail is that the final chunk may be shorter if the length is not divisible by size."
  },
  {
    "input": "class file_context_7:\n    def __init__(self, path):\n        self.path = path\n\n    def __enter__(self):\n        self.handle = open(self.path, 'r', encoding='utf-8')\n        return self.handle\n\n    def __exit__(self, exc_type, exc, tb):\n        self.handle.close()\n        return False\n",
    "output": "Built around file_context_7 uses a context manager to transform path and self.handle into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts path, self.handle and returns an open file handle during the with-block. One notable detail is that exceptions are not suppressed because __exit__ returns False."
  },
  {
    "input": "def safe_parse_7(text, fallback=0):\n    try:\n        return int(text)\n    except (TypeError, ValueError):\n        return fallback\n",
    "output": "Designed to safe_parse_7 uses exception handling with a fallback to transform text and fallback into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, fallback and returns an integer when parsing succeeds, otherwise the fallback value. One notable detail is that TypeError and ValueError both map to the fallback path."
  },
  {
    "input": "def map_filter_reduce_7(values, factor=2):\n    from functools import reduce\n    doubled = list(map(lambda value: value * factor, values))\n    positives = list(filter(lambda value: value > 0, doubled))\n    return reduce(lambda acc, value: acc + value, positives, 0)\n",
    "output": "Working through map_filter_reduce_7 uses higher-order functions to transform values and factor into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, factor and returns the reduced total as a number. One notable detail is that empty inputs return zero because reduce is seeded with an initial value."
  },
  {
    "input": "class Animal_7:\n    def speak(self):\n        return 'sound'\n\nclass Dog_7(Animal_7):\n    def speak(self):\n        return 'woof'\n",
    "output": "The generator Animal_7 and Dog_7 uses inheritance and method overriding to transform self and Animal_7 into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts self, Animal_7 and returns a sound string from the overridden speak method. One notable detail is that the derived class returns a dog-specific sound instead of the base sound."
  },
  {
    "input": "async def async_collect_7(items, delay=0):\n    import asyncio\n    results = []\n    for item in items:\n        await asyncio.sleep(delay)\n        results.append(item)\n    return results\n",
    "output": "This decorator async_collect_7 uses async iteration with await to transform items and delay into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, delay and returns the collected list after awaiting each item. One notable detail is that it yields control back to the event loop on every pass."
  },
  {
    "input": "class SingletonFactory_7:\n    _instance = None\n\n    def __new__(cls, *args, **kwargs):\n        if cls._instance is None:\n            cls._instance = super().__new__(cls)\n        return cls._instance\n",
    "output": "The recursive SingletonFactory_7 uses the singleton pattern to transform args and kwargs into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts args, kwargs and returns the same instance every time. One notable detail is that state is shared because __new__ reuses the cached instance."
  },
  {
    "input": "def data_pipeline_7(records, minimum_age=18):\n    filtered = [record for record in records if record['age'] >= minimum_age]\n    normalized = [{**record, 'name': record['name'].strip().title()} for record in filtered]\n    return sorted(normalized, key=lambda record: record['name'])\n",
    "output": "Inside this class data_pipeline_7 uses a filter-transform-sort pipeline to transform records and minimum_age into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts records, minimum_age and returns a sorted list of normalized record dictionaries. One notable detail is that missing age or name keys will still raise KeyError."
  },
  {
    "input": "def module_worker_a_7(numbers):\n    return sum(numbers)\n\ndef module_worker_b_7(numbers):\n    return max(numbers) if numbers else None\n\ndef module_worker_c_7(numbers):\n    return min(numbers) if numbers else None\n",
    "output": "The function module_worker_a_7 uses a small multi-function module to transform numbers and numbers into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts numbers, numbers and returns the computed aggregate returned by each helper. One notable detail is that empty lists cause module_worker_b and module_worker_c to return None."
  },
  {
    "input": "def gcd_calculator_8(left, right):\n    while right != 0:\n        left, right = right, left % right\n    return left\n",
    "output": "This class gcd_calculator_8 uses the Euclidean algorithm to transform left and right into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts left, right and returns the greatest common divisor as an integer. One notable detail is that it returns the final left value when right becomes zero."
  },
  {
    "input": "def reverse_text_8(text, preserve_case=False):\n    if preserve_case:\n        return text[::-1]\n    return text.strip()[::-1]\n",
    "output": "The algorithm reverse_text_8 uses slice-based string reversal to transform text and preserve_case into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, preserve_case and returns the reversed string. One notable detail is that it optionally strips surrounding whitespace before reversing."
  },
  {
    "input": "def dedupe_values_8(items, keep_order=True):\n    seen = set()\n    output = []\n    for item in items:\n        if item not in seen:\n            seen.add(item)\n            output.append(item)\n    return output if keep_order else sorted(set(items))\n",
    "output": "At its core dedupe_values_8 uses order-preserving deduplication with a set to transform items and keep_order into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, keep_order and returns a list of unique values. One notable detail is that the ordered branch preserves the first appearance of each item."
  },
  {
    "input": "def factorial_recursive_8(n, depth=0):\n    if n <= 1:\n        return 1\n    return n * factorial_recursive_8(n - 1, depth + 1)\n",
    "output": "Under the hood factorial_recursive_8 uses recursive multiplication to transform n and depth into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts n, depth and returns the factorial of n as an integer. One notable detail is that it uses the n <= 1 base case, so zero and one both return one."
  },
  {
    "input": "class BankAccount_8:\n    def __init__(self, owner, balance=0):\n        self.owner = owner\n        self.balance = balance\n\n    def deposit(self, amount):\n        self.balance += amount\n        return self.balance\n\n    def withdraw(self, amount):\n        if amount > self.balance:\n            raise ValueError('insufficient funds')\n        self.balance -= amount\n        return self.balance\n",
    "output": "When called with BankAccount_8 uses an object-oriented balance manager to transform owner and balance into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts owner, balance and returns the updated account balance. One notable detail is that withdrawals larger than the current balance raise ValueError."
  },
  {
    "input": "def bubble_sort_8(values, descending=False):\n    for end in range(len(values) - 1, 0, -1):\n        for index in range(end):\n            if (values[index] > values[index + 1]) != descending:\n                values[index], values[index + 1] = values[index + 1], values[index]\n    return values\n",
    "output": "This utility bubble_sort_8 uses bubble sort to transform values and descending into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, descending and returns the list sorted in place and returned. One notable detail is that it works in quadratic time and mutates the original list."
  },
  {
    "input": "def binary_search_8(values, target, offset=0):\n    left = 0\n    right = len(values) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if values[mid] == target:\n            return offset + mid\n        if values[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return -1\n",
    "output": "The method binary_search_8 uses binary search to transform values and target into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, target and returns the index of target or negative one. One notable detail is that the search assumes values is already sorted."
  },
  {
    "input": "def timing_decorator_8(func):\n    import functools, time\n    @functools.wraps(func)\n    def wrapper(*args, **kwargs):\n        start = time.perf_counter()\n        result = func(*args, **kwargs)\n        print(func.__name__, time.perf_counter() - start)\n        return result\n    return wrapper\n",
    "output": "Built around timing_decorator_8 uses a timing wrapper to transform func and args into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts func, args and returns the wrapped function result after printing elapsed time. One notable detail is that it preserves the original function metadata with functools.wraps."
  },
  {
    "input": "def chunked_8(items, size):\n    for index in range(0, len(items), size):\n        yield items[index:index + size]\n",
    "output": "Designed to chunked_8 uses generator-based chunking to transform items and size into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, size and returns lists of items in successive chunks. One notable detail is that the final chunk may be shorter if the length is not divisible by size."
  },
  {
    "input": "class file_context_8:\n    def __init__(self, path):\n        self.path = path\n\n    def __enter__(self):\n        self.handle = open(self.path, 'r', encoding='utf-8')\n        return self.handle\n\n    def __exit__(self, exc_type, exc, tb):\n        self.handle.close()\n        return False\n",
    "output": "Working through file_context_8 uses a context manager to transform path and self.handle into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts path, self.handle and returns an open file handle during the with-block. One notable detail is that exceptions are not suppressed because __exit__ returns False."
  },
  {
    "input": "def safe_parse_8(text, fallback=0):\n    try:\n        return int(text)\n    except (TypeError, ValueError):\n        return fallback\n",
    "output": "The generator safe_parse_8 uses exception handling with a fallback to transform text and fallback into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, fallback and returns an integer when parsing succeeds, otherwise the fallback value. One notable detail is that TypeError and ValueError both map to the fallback path."
  },
  {
    "input": "def map_filter_reduce_8(values, factor=2):\n    from functools import reduce\n    doubled = list(map(lambda value: value * factor, values))\n    positives = list(filter(lambda value: value > 0, doubled))\n    return reduce(lambda acc, value: acc + value, positives, 0)\n",
    "output": "This decorator map_filter_reduce_8 uses higher-order functions to transform values and factor into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, factor and returns the reduced total as a number. One notable detail is that empty inputs return zero because reduce is seeded with an initial value."
  },
  {
    "input": "class Animal_8:\n    def speak(self):\n        return 'sound'\n\nclass Dog_8(Animal_8):\n    def speak(self):\n        return 'woof'\n",
    "output": "The recursive Animal_8 and Dog_8 uses inheritance and method overriding to transform self and Animal_8 into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts self, Animal_8 and returns a sound string from the overridden speak method. One notable detail is that the derived class returns a dog-specific sound instead of the base sound."
  },
  {
    "input": "async def async_collect_8(items, delay=0):\n    import asyncio\n    results = []\n    for item in items:\n        await asyncio.sleep(delay)\n        results.append(item)\n    return results\n",
    "output": "Inside this class async_collect_8 uses async iteration with await to transform items and delay into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, delay and returns the collected list after awaiting each item. One notable detail is that it yields control back to the event loop on every pass."
  },
  {
    "input": "class SingletonFactory_8:\n    _instance = None\n\n    def __new__(cls, *args, **kwargs):\n        if cls._instance is None:\n            cls._instance = super().__new__(cls)\n        return cls._instance\n",
    "output": "The function SingletonFactory_8 uses the singleton pattern to transform args and kwargs into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts args, kwargs and returns the same instance every time. One notable detail is that state is shared because __new__ reuses the cached instance."
  },
  {
    "input": "def data_pipeline_8(records, minimum_age=18):\n    filtered = [record for record in records if record['age'] >= minimum_age]\n    normalized = [{**record, 'name': record['name'].strip().title()} for record in filtered]\n    return sorted(normalized, key=lambda record: record['name'])\n",
    "output": "This class data_pipeline_8 uses a filter-transform-sort pipeline to transform records and minimum_age into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts records, minimum_age and returns a sorted list of normalized record dictionaries. One notable detail is that missing age or name keys will still raise KeyError."
  },
  {
    "input": "def module_worker_a_8(numbers):\n    return sum(numbers)\n\ndef module_worker_b_8(numbers):\n    return max(numbers) if numbers else None\n\ndef module_worker_c_8(numbers):\n    return min(numbers) if numbers else None\n",
    "output": "The algorithm module_worker_a_8 uses a small multi-function module to transform numbers and numbers into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts numbers, numbers and returns the computed aggregate returned by each helper. One notable detail is that empty lists cause module_worker_b and module_worker_c to return None."
  },
  {
    "input": "def gcd_calculator_9(left, right):\n    while right != 0:\n        left, right = right, left % right\n    return left\n",
    "output": "At its core gcd_calculator_9 uses the Euclidean algorithm to transform left and right into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts left, right and returns the greatest common divisor as an integer. One notable detail is that it returns the final left value when right becomes zero."
  },
  {
    "input": "def reverse_text_9(text, preserve_case=False):\n    if preserve_case:\n        return text[::-1]\n    return text.strip()[::-1]\n",
    "output": "Under the hood reverse_text_9 uses slice-based string reversal to transform text and preserve_case into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, preserve_case and returns the reversed string. One notable detail is that it optionally strips surrounding whitespace before reversing."
  },
  {
    "input": "def dedupe_values_9(items, keep_order=True):\n    seen = set()\n    output = []\n    for item in items:\n        if item not in seen:\n            seen.add(item)\n            output.append(item)\n    return output if keep_order else sorted(set(items))\n",
    "output": "When called with dedupe_values_9 uses order-preserving deduplication with a set to transform items and keep_order into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, keep_order and returns a list of unique values. One notable detail is that the ordered branch preserves the first appearance of each item."
  },
  {
    "input": "def factorial_recursive_9(n, depth=0):\n    if n <= 1:\n        return 1\n    return n * factorial_recursive_9(n - 1, depth + 1)\n",
    "output": "This utility factorial_recursive_9 uses recursive multiplication to transform n and depth into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts n, depth and returns the factorial of n as an integer. One notable detail is that it uses the n <= 1 base case, so zero and one both return one."
  },
  {
    "input": "class BankAccount_9:\n    def __init__(self, owner, balance=0):\n        self.owner = owner\n        self.balance = balance\n\n    def deposit(self, amount):\n        self.balance += amount\n        return self.balance\n\n    def withdraw(self, amount):\n        if amount > self.balance:\n            raise ValueError('insufficient funds')\n        self.balance -= amount\n        return self.balance\n",
    "output": "The method BankAccount_9 uses an object-oriented balance manager to transform owner and balance into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts owner, balance and returns the updated account balance. One notable detail is that withdrawals larger than the current balance raise ValueError."
  },
  {
    "input": "def bubble_sort_9(values, descending=False):\n    for end in range(len(values) - 1, 0, -1):\n        for index in range(end):\n            if (values[index] > values[index + 1]) != descending:\n                values[index], values[index + 1] = values[index + 1], values[index]\n    return values\n",
    "output": "Built around bubble_sort_9 uses bubble sort to transform values and descending into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, descending and returns the list sorted in place and returned. One notable detail is that it works in quadratic time and mutates the original list."
  },
  {
    "input": "def binary_search_9(values, target, offset=0):\n    left = 0\n    right = len(values) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if values[mid] == target:\n            return offset + mid\n        if values[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return -1\n",
    "output": "Designed to binary_search_9 uses binary search to transform values and target into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, target and returns the index of target or negative one. One notable detail is that the search assumes values is already sorted."
  },
  {
    "input": "def timing_decorator_9(func):\n    import functools, time\n    @functools.wraps(func)\n    def wrapper(*args, **kwargs):\n        start = time.perf_counter()\n        result = func(*args, **kwargs)\n        print(func.__name__, time.perf_counter() - start)\n        return result\n    return wrapper\n",
    "output": "Working through timing_decorator_9 uses a timing wrapper to transform func and args into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts func, args and returns the wrapped function result after printing elapsed time. One notable detail is that it preserves the original function metadata with functools.wraps."
  },
  {
    "input": "def chunked_9(items, size):\n    for index in range(0, len(items), size):\n        yield items[index:index + size]\n",
    "output": "The generator chunked_9 uses generator-based chunking to transform items and size into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, size and returns lists of items in successive chunks. One notable detail is that the final chunk may be shorter if the length is not divisible by size."
  },
  {
    "input": "class file_context_9:\n    def __init__(self, path):\n        self.path = path\n\n    def __enter__(self):\n        self.handle = open(self.path, 'r', encoding='utf-8')\n        return self.handle\n\n    def __exit__(self, exc_type, exc, tb):\n        self.handle.close()\n        return False\n",
    "output": "This decorator file_context_9 uses a context manager to transform path and self.handle into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts path, self.handle and returns an open file handle during the with-block. One notable detail is that exceptions are not suppressed because __exit__ returns False."
  },
  {
    "input": "def safe_parse_9(text, fallback=0):\n    try:\n        return int(text)\n    except (TypeError, ValueError):\n        return fallback\n",
    "output": "The recursive safe_parse_9 uses exception handling with a fallback to transform text and fallback into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, fallback and returns an integer when parsing succeeds, otherwise the fallback value. One notable detail is that TypeError and ValueError both map to the fallback path."
  },
  {
    "input": "def map_filter_reduce_9(values, factor=2):\n    from functools import reduce\n    doubled = list(map(lambda value: value * factor, values))\n    positives = list(filter(lambda value: value > 0, doubled))\n    return reduce(lambda acc, value: acc + value, positives, 0)\n",
    "output": "Inside this class map_filter_reduce_9 uses higher-order functions to transform values and factor into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, factor and returns the reduced total as a number. One notable detail is that empty inputs return zero because reduce is seeded with an initial value."
  },
  {
    "input": "class Animal_9:\n    def speak(self):\n        return 'sound'\n\nclass Dog_9(Animal_9):\n    def speak(self):\n        return 'woof'\n",
    "output": "The function Animal_9 and Dog_9 uses inheritance and method overriding to transform self and Animal_9 into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts self, Animal_9 and returns a sound string from the overridden speak method. One notable detail is that the derived class returns a dog-specific sound instead of the base sound."
  },
  {
    "input": "async def async_collect_9(items, delay=0):\n    import asyncio\n    results = []\n    for item in items:\n        await asyncio.sleep(delay)\n        results.append(item)\n    return results\n",
    "output": "This class async_collect_9 uses async iteration with await to transform items and delay into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, delay and returns the collected list after awaiting each item. One notable detail is that it yields control back to the event loop on every pass."
  },
  {
    "input": "class SingletonFactory_9:\n    _instance = None\n\n    def __new__(cls, *args, **kwargs):\n        if cls._instance is None:\n            cls._instance = super().__new__(cls)\n        return cls._instance\n",
    "output": "The algorithm SingletonFactory_9 uses the singleton pattern to transform args and kwargs into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts args, kwargs and returns the same instance every time. One notable detail is that state is shared because __new__ reuses the cached instance."
  },
  {
    "input": "def data_pipeline_9(records, minimum_age=18):\n    filtered = [record for record in records if record['age'] >= minimum_age]\n    normalized = [{**record, 'name': record['name'].strip().title()} for record in filtered]\n    return sorted(normalized, key=lambda record: record['name'])\n",
    "output": "At its core data_pipeline_9 uses a filter-transform-sort pipeline to transform records and minimum_age into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts records, minimum_age and returns a sorted list of normalized record dictionaries. One notable detail is that missing age or name keys will still raise KeyError."
  },
  {
    "input": "def module_worker_a_9(numbers):\n    return sum(numbers)\n\ndef module_worker_b_9(numbers):\n    return max(numbers) if numbers else None\n\ndef module_worker_c_9(numbers):\n    return min(numbers) if numbers else None\n",
    "output": "Under the hood module_worker_a_9 uses a small multi-function module to transform numbers and numbers into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts numbers, numbers and returns the computed aggregate returned by each helper. One notable detail is that empty lists cause module_worker_b and module_worker_c to return None."
  },
  {
    "input": "def gcd_calculator_10(left, right):\n    while right != 0:\n        left, right = right, left % right\n    return left\n",
    "output": "When called with gcd_calculator_10 uses the Euclidean algorithm to transform left and right into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts left, right and returns the greatest common divisor as an integer. One notable detail is that it returns the final left value when right becomes zero."
  },
  {
    "input": "def reverse_text_10(text, preserve_case=False):\n    if preserve_case:\n        return text[::-1]\n    return text.strip()[::-1]\n",
    "output": "This utility reverse_text_10 uses slice-based string reversal to transform text and preserve_case into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, preserve_case and returns the reversed string. One notable detail is that it optionally strips surrounding whitespace before reversing."
  },
  {
    "input": "def dedupe_values_10(items, keep_order=True):\n    seen = set()\n    output = []\n    for item in items:\n        if item not in seen:\n            seen.add(item)\n            output.append(item)\n    return output if keep_order else sorted(set(items))\n",
    "output": "The method dedupe_values_10 uses order-preserving deduplication with a set to transform items and keep_order into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, keep_order and returns a list of unique values. One notable detail is that the ordered branch preserves the first appearance of each item."
  },
  {
    "input": "def factorial_recursive_10(n, depth=0):\n    if n <= 1:\n        return 1\n    return n * factorial_recursive_10(n - 1, depth + 1)\n",
    "output": "Built around factorial_recursive_10 uses recursive multiplication to transform n and depth into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts n, depth and returns the factorial of n as an integer. One notable detail is that it uses the n <= 1 base case, so zero and one both return one."
  },
  {
    "input": "class BankAccount_10:\n    def __init__(self, owner, balance=0):\n        self.owner = owner\n        self.balance = balance\n\n    def deposit(self, amount):\n        self.balance += amount\n        return self.balance\n\n    def withdraw(self, amount):\n        if amount > self.balance:\n            raise ValueError('insufficient funds')\n        self.balance -= amount\n        return self.balance\n",
    "output": "Designed to BankAccount_10 uses an object-oriented balance manager to transform owner and balance into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts owner, balance and returns the updated account balance. One notable detail is that withdrawals larger than the current balance raise ValueError."
  },
  {
    "input": "def bubble_sort_10(values, descending=False):\n    for end in range(len(values) - 1, 0, -1):\n        for index in range(end):\n            if (values[index] > values[index + 1]) != descending:\n                values[index], values[index + 1] = values[index + 1], values[index]\n    return values\n",
    "output": "Working through bubble_sort_10 uses bubble sort to transform values and descending into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, descending and returns the list sorted in place and returned. One notable detail is that it works in quadratic time and mutates the original list."
  },
  {
    "input": "def binary_search_10(values, target, offset=0):\n    left = 0\n    right = len(values) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if values[mid] == target:\n            return offset + mid\n        if values[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return -1\n",
    "output": "The generator binary_search_10 uses binary search to transform values and target into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, target and returns the index of target or negative one. One notable detail is that the search assumes values is already sorted."
  },
  {
    "input": "def timing_decorator_10(func):\n    import functools, time\n    @functools.wraps(func)\n    def wrapper(*args, **kwargs):\n        start = time.perf_counter()\n        result = func(*args, **kwargs)\n        print(func.__name__, time.perf_counter() - start)\n        return result\n    return wrapper\n",
    "output": "This decorator timing_decorator_10 uses a timing wrapper to transform func and args into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts func, args and returns the wrapped function result after printing elapsed time. One notable detail is that it preserves the original function metadata with functools.wraps."
  },
  {
    "input": "def chunked_10(items, size):\n    for index in range(0, len(items), size):\n        yield items[index:index + size]\n",
    "output": "The recursive chunked_10 uses generator-based chunking to transform items and size into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, size and returns lists of items in successive chunks. One notable detail is that the final chunk may be shorter if the length is not divisible by size."
  },
  {
    "input": "class file_context_10:\n    def __init__(self, path):\n        self.path = path\n\n    def __enter__(self):\n        self.handle = open(self.path, 'r', encoding='utf-8')\n        return self.handle\n\n    def __exit__(self, exc_type, exc, tb):\n        self.handle.close()\n        return False\n",
    "output": "Inside this class file_context_10 uses a context manager to transform path and self.handle into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts path, self.handle and returns an open file handle during the with-block. One notable detail is that exceptions are not suppressed because __exit__ returns False."
  },
  {
    "input": "def safe_parse_10(text, fallback=0):\n    try:\n        return int(text)\n    except (TypeError, ValueError):\n        return fallback\n",
    "output": "The function safe_parse_10 uses exception handling with a fallback to transform text and fallback into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, fallback and returns an integer when parsing succeeds, otherwise the fallback value. One notable detail is that TypeError and ValueError both map to the fallback path."
  },
  {
    "input": "def map_filter_reduce_10(values, factor=2):\n    from functools import reduce\n    doubled = list(map(lambda value: value * factor, values))\n    positives = list(filter(lambda value: value > 0, doubled))\n    return reduce(lambda acc, value: acc + value, positives, 0)\n",
    "output": "This class map_filter_reduce_10 uses higher-order functions to transform values and factor into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, factor and returns the reduced total as a number. One notable detail is that empty inputs return zero because reduce is seeded with an initial value."
  },
  {
    "input": "class Animal_10:\n    def speak(self):\n        return 'sound'\n\nclass Dog_10(Animal_10):\n    def speak(self):\n        return 'woof'\n",
    "output": "The algorithm Animal_10 and Dog_10 uses inheritance and method overriding to transform self and Animal_10 into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts self, Animal_10 and returns a sound string from the overridden speak method. One notable detail is that the derived class returns a dog-specific sound instead of the base sound."
  },
  {
    "input": "async def async_collect_10(items, delay=0):\n    import asyncio\n    results = []\n    for item in items:\n        await asyncio.sleep(delay)\n        results.append(item)\n    return results\n",
    "output": "At its core async_collect_10 uses async iteration with await to transform items and delay into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, delay and returns the collected list after awaiting each item. One notable detail is that it yields control back to the event loop on every pass."
  },
  {
    "input": "class SingletonFactory_10:\n    _instance = None\n\n    def __new__(cls, *args, **kwargs):\n        if cls._instance is None:\n            cls._instance = super().__new__(cls)\n        return cls._instance\n",
    "output": "Under the hood SingletonFactory_10 uses the singleton pattern to transform args and kwargs into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts args, kwargs and returns the same instance every time. One notable detail is that state is shared because __new__ reuses the cached instance."
  },
  {
    "input": "def data_pipeline_10(records, minimum_age=18):\n    filtered = [record for record in records if record['age'] >= minimum_age]\n    normalized = [{**record, 'name': record['name'].strip().title()} for record in filtered]\n    return sorted(normalized, key=lambda record: record['name'])\n",
    "output": "When called with data_pipeline_10 uses a filter-transform-sort pipeline to transform records and minimum_age into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts records, minimum_age and returns a sorted list of normalized record dictionaries. One notable detail is that missing age or name keys will still raise KeyError."
  },
  {
    "input": "def module_worker_a_10(numbers):\n    return sum(numbers)\n\ndef module_worker_b_10(numbers):\n    return max(numbers) if numbers else None\n\ndef module_worker_c_10(numbers):\n    return min(numbers) if numbers else None\n",
    "output": "This utility module_worker_a_10 uses a small multi-function module to transform numbers and numbers into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts numbers, numbers and returns the computed aggregate returned by each helper. One notable detail is that empty lists cause module_worker_b and module_worker_c to return None."
  },
  {
    "input": "def gcd_calculator_11(left, right):\n    while right != 0:\n        left, right = right, left % right\n    return left\n",
    "output": "The method gcd_calculator_11 uses the Euclidean algorithm to transform left and right into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts left, right and returns the greatest common divisor as an integer. One notable detail is that it returns the final left value when right becomes zero."
  },
  {
    "input": "def reverse_text_11(text, preserve_case=False):\n    if preserve_case:\n        return text[::-1]\n    return text.strip()[::-1]\n",
    "output": "Built around reverse_text_11 uses slice-based string reversal to transform text and preserve_case into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, preserve_case and returns the reversed string. One notable detail is that it optionally strips surrounding whitespace before reversing."
  },
  {
    "input": "def dedupe_values_11(items, keep_order=True):\n    seen = set()\n    output = []\n    for item in items:\n        if item not in seen:\n            seen.add(item)\n            output.append(item)\n    return output if keep_order else sorted(set(items))\n",
    "output": "Designed to dedupe_values_11 uses order-preserving deduplication with a set to transform items and keep_order into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, keep_order and returns a list of unique values. One notable detail is that the ordered branch preserves the first appearance of each item."
  },
  {
    "input": "def factorial_recursive_11(n, depth=0):\n    if n <= 1:\n        return 1\n    return n * factorial_recursive_11(n - 1, depth + 1)\n",
    "output": "Working through factorial_recursive_11 uses recursive multiplication to transform n and depth into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts n, depth and returns the factorial of n as an integer. One notable detail is that it uses the n <= 1 base case, so zero and one both return one."
  },
  {
    "input": "class BankAccount_11:\n    def __init__(self, owner, balance=0):\n        self.owner = owner\n        self.balance = balance\n\n    def deposit(self, amount):\n        self.balance += amount\n        return self.balance\n\n    def withdraw(self, amount):\n        if amount > self.balance:\n            raise ValueError('insufficient funds')\n        self.balance -= amount\n        return self.balance\n",
    "output": "The generator BankAccount_11 uses an object-oriented balance manager to transform owner and balance into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts owner, balance and returns the updated account balance. One notable detail is that withdrawals larger than the current balance raise ValueError."
  },
  {
    "input": "def bubble_sort_11(values, descending=False):\n    for end in range(len(values) - 1, 0, -1):\n        for index in range(end):\n            if (values[index] > values[index + 1]) != descending:\n                values[index], values[index + 1] = values[index + 1], values[index]\n    return values\n",
    "output": "This decorator bubble_sort_11 uses bubble sort to transform values and descending into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, descending and returns the list sorted in place and returned. One notable detail is that it works in quadratic time and mutates the original list."
  },
  {
    "input": "def binary_search_11(values, target, offset=0):\n    left = 0\n    right = len(values) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if values[mid] == target:\n            return offset + mid\n        if values[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return -1\n",
    "output": "The recursive binary_search_11 uses binary search to transform values and target into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, target and returns the index of target or negative one. One notable detail is that the search assumes values is already sorted."
  },
  {
    "input": "def timing_decorator_11(func):\n    import functools, time\n    @functools.wraps(func)\n    def wrapper(*args, **kwargs):\n        start = time.perf_counter()\n        result = func(*args, **kwargs)\n        print(func.__name__, time.perf_counter() - start)\n        return result\n    return wrapper\n",
    "output": "Inside this class timing_decorator_11 uses a timing wrapper to transform func and args into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts func, args and returns the wrapped function result after printing elapsed time. One notable detail is that it preserves the original function metadata with functools.wraps."
  },
  {
    "input": "def chunked_11(items, size):\n    for index in range(0, len(items), size):\n        yield items[index:index + size]\n",
    "output": "The function chunked_11 uses generator-based chunking to transform items and size into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, size and returns lists of items in successive chunks. One notable detail is that the final chunk may be shorter if the length is not divisible by size."
  },
  {
    "input": "class file_context_11:\n    def __init__(self, path):\n        self.path = path\n\n    def __enter__(self):\n        self.handle = open(self.path, 'r', encoding='utf-8')\n        return self.handle\n\n    def __exit__(self, exc_type, exc, tb):\n        self.handle.close()\n        return False\n",
    "output": "This class file_context_11 uses a context manager to transform path and self.handle into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts path, self.handle and returns an open file handle during the with-block. One notable detail is that exceptions are not suppressed because __exit__ returns False."
  },
  {
    "input": "def safe_parse_11(text, fallback=0):\n    try:\n        return int(text)\n    except (TypeError, ValueError):\n        return fallback\n",
    "output": "The algorithm safe_parse_11 uses exception handling with a fallback to transform text and fallback into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, fallback and returns an integer when parsing succeeds, otherwise the fallback value. One notable detail is that TypeError and ValueError both map to the fallback path."
  },
  {
    "input": "def map_filter_reduce_11(values, factor=2):\n    from functools import reduce\n    doubled = list(map(lambda value: value * factor, values))\n    positives = list(filter(lambda value: value > 0, doubled))\n    return reduce(lambda acc, value: acc + value, positives, 0)\n",
    "output": "At its core map_filter_reduce_11 uses higher-order functions to transform values and factor into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, factor and returns the reduced total as a number. One notable detail is that empty inputs return zero because reduce is seeded with an initial value."
  },
  {
    "input": "class Animal_11:\n    def speak(self):\n        return 'sound'\n\nclass Dog_11(Animal_11):\n    def speak(self):\n        return 'woof'\n",
    "output": "Under the hood Animal_11 and Dog_11 uses inheritance and method overriding to transform self and Animal_11 into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts self, Animal_11 and returns a sound string from the overridden speak method. One notable detail is that the derived class returns a dog-specific sound instead of the base sound."
  },
  {
    "input": "async def async_collect_11(items, delay=0):\n    import asyncio\n    results = []\n    for item in items:\n        await asyncio.sleep(delay)\n        results.append(item)\n    return results\n",
    "output": "When called with async_collect_11 uses async iteration with await to transform items and delay into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, delay and returns the collected list after awaiting each item. One notable detail is that it yields control back to the event loop on every pass."
  },
  {
    "input": "class SingletonFactory_11:\n    _instance = None\n\n    def __new__(cls, *args, **kwargs):\n        if cls._instance is None:\n            cls._instance = super().__new__(cls)\n        return cls._instance\n",
    "output": "This utility SingletonFactory_11 uses the singleton pattern to transform args and kwargs into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts args, kwargs and returns the same instance every time. One notable detail is that state is shared because __new__ reuses the cached instance."
  },
  {
    "input": "def data_pipeline_11(records, minimum_age=18):\n    filtered = [record for record in records if record['age'] >= minimum_age]\n    normalized = [{**record, 'name': record['name'].strip().title()} for record in filtered]\n    return sorted(normalized, key=lambda record: record['name'])\n",
    "output": "The method data_pipeline_11 uses a filter-transform-sort pipeline to transform records and minimum_age into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts records, minimum_age and returns a sorted list of normalized record dictionaries. One notable detail is that missing age or name keys will still raise KeyError."
  },
  {
    "input": "def module_worker_a_11(numbers):\n    return sum(numbers)\n\ndef module_worker_b_11(numbers):\n    return max(numbers) if numbers else None\n\ndef module_worker_c_11(numbers):\n    return min(numbers) if numbers else None\n",
    "output": "Built around module_worker_a_11 uses a small multi-function module to transform numbers and numbers into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts numbers, numbers and returns the computed aggregate returned by each helper. One notable detail is that empty lists cause module_worker_b and module_worker_c to return None."
  },
  {
    "input": "def gcd_calculator_12(left, right):\n    while right != 0:\n        left, right = right, left % right\n    return left\n",
    "output": "Designed to gcd_calculator_12 uses the Euclidean algorithm to transform left and right into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts left, right and returns the greatest common divisor as an integer. One notable detail is that it returns the final left value when right becomes zero."
  },
  {
    "input": "def reverse_text_12(text, preserve_case=False):\n    if preserve_case:\n        return text[::-1]\n    return text.strip()[::-1]\n",
    "output": "Working through reverse_text_12 uses slice-based string reversal to transform text and preserve_case into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, preserve_case and returns the reversed string. One notable detail is that it optionally strips surrounding whitespace before reversing."
  },
  {
    "input": "def dedupe_values_12(items, keep_order=True):\n    seen = set()\n    output = []\n    for item in items:\n        if item not in seen:\n            seen.add(item)\n            output.append(item)\n    return output if keep_order else sorted(set(items))\n",
    "output": "The generator dedupe_values_12 uses order-preserving deduplication with a set to transform items and keep_order into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, keep_order and returns a list of unique values. One notable detail is that the ordered branch preserves the first appearance of each item."
  },
  {
    "input": "def factorial_recursive_12(n, depth=0):\n    if n <= 1:\n        return 1\n    return n * factorial_recursive_12(n - 1, depth + 1)\n",
    "output": "This decorator factorial_recursive_12 uses recursive multiplication to transform n and depth into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts n, depth and returns the factorial of n as an integer. One notable detail is that it uses the n <= 1 base case, so zero and one both return one."
  },
  {
    "input": "class BankAccount_12:\n    def __init__(self, owner, balance=0):\n        self.owner = owner\n        self.balance = balance\n\n    def deposit(self, amount):\n        self.balance += amount\n        return self.balance\n\n    def withdraw(self, amount):\n        if amount > self.balance:\n            raise ValueError('insufficient funds')\n        self.balance -= amount\n        return self.balance\n",
    "output": "The recursive BankAccount_12 uses an object-oriented balance manager to transform owner and balance into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts owner, balance and returns the updated account balance. One notable detail is that withdrawals larger than the current balance raise ValueError."
  },
  {
    "input": "def bubble_sort_12(values, descending=False):\n    for end in range(len(values) - 1, 0, -1):\n        for index in range(end):\n            if (values[index] > values[index + 1]) != descending:\n                values[index], values[index + 1] = values[index + 1], values[index]\n    return values\n",
    "output": "Inside this class bubble_sort_12 uses bubble sort to transform values and descending into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, descending and returns the list sorted in place and returned. One notable detail is that it works in quadratic time and mutates the original list."
  },
  {
    "input": "def binary_search_12(values, target, offset=0):\n    left = 0\n    right = len(values) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if values[mid] == target:\n            return offset + mid\n        if values[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return -1\n",
    "output": "The function binary_search_12 uses binary search to transform values and target into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, target and returns the index of target or negative one. One notable detail is that the search assumes values is already sorted."
  },
  {
    "input": "def timing_decorator_12(func):\n    import functools, time\n    @functools.wraps(func)\n    def wrapper(*args, **kwargs):\n        start = time.perf_counter()\n        result = func(*args, **kwargs)\n        print(func.__name__, time.perf_counter() - start)\n        return result\n    return wrapper\n",
    "output": "This class timing_decorator_12 uses a timing wrapper to transform func and args into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts func, args and returns the wrapped function result after printing elapsed time. One notable detail is that it preserves the original function metadata with functools.wraps."
  },
  {
    "input": "def chunked_12(items, size):\n    for index in range(0, len(items), size):\n        yield items[index:index + size]\n",
    "output": "The algorithm chunked_12 uses generator-based chunking to transform items and size into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, size and returns lists of items in successive chunks. One notable detail is that the final chunk may be shorter if the length is not divisible by size."
  },
  {
    "input": "class file_context_12:\n    def __init__(self, path):\n        self.path = path\n\n    def __enter__(self):\n        self.handle = open(self.path, 'r', encoding='utf-8')\n        return self.handle\n\n    def __exit__(self, exc_type, exc, tb):\n        self.handle.close()\n        return False\n",
    "output": "At its core file_context_12 uses a context manager to transform path and self.handle into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts path, self.handle and returns an open file handle during the with-block. One notable detail is that exceptions are not suppressed because __exit__ returns False."
  },
  {
    "input": "def safe_parse_12(text, fallback=0):\n    try:\n        return int(text)\n    except (TypeError, ValueError):\n        return fallback\n",
    "output": "Under the hood safe_parse_12 uses exception handling with a fallback to transform text and fallback into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, fallback and returns an integer when parsing succeeds, otherwise the fallback value. One notable detail is that TypeError and ValueError both map to the fallback path."
  },
  {
    "input": "def map_filter_reduce_12(values, factor=2):\n    from functools import reduce\n    doubled = list(map(lambda value: value * factor, values))\n    positives = list(filter(lambda value: value > 0, doubled))\n    return reduce(lambda acc, value: acc + value, positives, 0)\n",
    "output": "When called with map_filter_reduce_12 uses higher-order functions to transform values and factor into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, factor and returns the reduced total as a number. One notable detail is that empty inputs return zero because reduce is seeded with an initial value."
  },
  {
    "input": "class Animal_12:\n    def speak(self):\n        return 'sound'\n\nclass Dog_12(Animal_12):\n    def speak(self):\n        return 'woof'\n",
    "output": "This utility Animal_12 and Dog_12 uses inheritance and method overriding to transform self and Animal_12 into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts self, Animal_12 and returns a sound string from the overridden speak method. One notable detail is that the derived class returns a dog-specific sound instead of the base sound."
  },
  {
    "input": "async def async_collect_12(items, delay=0):\n    import asyncio\n    results = []\n    for item in items:\n        await asyncio.sleep(delay)\n        results.append(item)\n    return results\n",
    "output": "The method async_collect_12 uses async iteration with await to transform items and delay into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, delay and returns the collected list after awaiting each item. One notable detail is that it yields control back to the event loop on every pass."
  },
  {
    "input": "class SingletonFactory_12:\n    _instance = None\n\n    def __new__(cls, *args, **kwargs):\n        if cls._instance is None:\n            cls._instance = super().__new__(cls)\n        return cls._instance\n",
    "output": "Built around SingletonFactory_12 uses the singleton pattern to transform args and kwargs into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts args, kwargs and returns the same instance every time. One notable detail is that state is shared because __new__ reuses the cached instance."
  },
  {
    "input": "def data_pipeline_12(records, minimum_age=18):\n    filtered = [record for record in records if record['age'] >= minimum_age]\n    normalized = [{**record, 'name': record['name'].strip().title()} for record in filtered]\n    return sorted(normalized, key=lambda record: record['name'])\n",
    "output": "Designed to data_pipeline_12 uses a filter-transform-sort pipeline to transform records and minimum_age into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts records, minimum_age and returns a sorted list of normalized record dictionaries. One notable detail is that missing age or name keys will still raise KeyError."
  },
  {
    "input": "def module_worker_a_12(numbers):\n    return sum(numbers)\n\ndef module_worker_b_12(numbers):\n    return max(numbers) if numbers else None\n\ndef module_worker_c_12(numbers):\n    return min(numbers) if numbers else None\n",
    "output": "Working through module_worker_a_12 uses a small multi-function module to transform numbers and numbers into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts numbers, numbers and returns the computed aggregate returned by each helper. One notable detail is that empty lists cause module_worker_b and module_worker_c to return None."
  },
  {
    "input": "def gcd_calculator_13(left, right):\n    while right != 0:\n        left, right = right, left % right\n    return left\n",
    "output": "The generator gcd_calculator_13 uses the Euclidean algorithm to transform left and right into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts left, right and returns the greatest common divisor as an integer. One notable detail is that it returns the final left value when right becomes zero."
  },
  {
    "input": "def reverse_text_13(text, preserve_case=False):\n    if preserve_case:\n        return text[::-1]\n    return text.strip()[::-1]\n",
    "output": "This decorator reverse_text_13 uses slice-based string reversal to transform text and preserve_case into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, preserve_case and returns the reversed string. One notable detail is that it optionally strips surrounding whitespace before reversing."
  },
  {
    "input": "def dedupe_values_13(items, keep_order=True):\n    seen = set()\n    output = []\n    for item in items:\n        if item not in seen:\n            seen.add(item)\n            output.append(item)\n    return output if keep_order else sorted(set(items))\n",
    "output": "The recursive dedupe_values_13 uses order-preserving deduplication with a set to transform items and keep_order into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, keep_order and returns a list of unique values. One notable detail is that the ordered branch preserves the first appearance of each item."
  },
  {
    "input": "def factorial_recursive_13(n, depth=0):\n    if n <= 1:\n        return 1\n    return n * factorial_recursive_13(n - 1, depth + 1)\n",
    "output": "Inside this class factorial_recursive_13 uses recursive multiplication to transform n and depth into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts n, depth and returns the factorial of n as an integer. One notable detail is that it uses the n <= 1 base case, so zero and one both return one."
  },
  {
    "input": "class BankAccount_13:\n    def __init__(self, owner, balance=0):\n        self.owner = owner\n        self.balance = balance\n\n    def deposit(self, amount):\n        self.balance += amount\n        return self.balance\n\n    def withdraw(self, amount):\n        if amount > self.balance:\n            raise ValueError('insufficient funds')\n        self.balance -= amount\n        return self.balance\n",
    "output": "The function BankAccount_13 uses an object-oriented balance manager to transform owner and balance into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts owner, balance and returns the updated account balance. One notable detail is that withdrawals larger than the current balance raise ValueError."
  },
  {
    "input": "def bubble_sort_13(values, descending=False):\n    for end in range(len(values) - 1, 0, -1):\n        for index in range(end):\n            if (values[index] > values[index + 1]) != descending:\n                values[index], values[index + 1] = values[index + 1], values[index]\n    return values\n",
    "output": "This class bubble_sort_13 uses bubble sort to transform values and descending into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, descending and returns the list sorted in place and returned. One notable detail is that it works in quadratic time and mutates the original list."
  },
  {
    "input": "def binary_search_13(values, target, offset=0):\n    left = 0\n    right = len(values) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if values[mid] == target:\n            return offset + mid\n        if values[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return -1\n",
    "output": "The algorithm binary_search_13 uses binary search to transform values and target into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, target and returns the index of target or negative one. One notable detail is that the search assumes values is already sorted."
  },
  {
    "input": "def timing_decorator_13(func):\n    import functools, time\n    @functools.wraps(func)\n    def wrapper(*args, **kwargs):\n        start = time.perf_counter()\n        result = func(*args, **kwargs)\n        print(func.__name__, time.perf_counter() - start)\n        return result\n    return wrapper\n",
    "output": "At its core timing_decorator_13 uses a timing wrapper to transform func and args into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts func, args and returns the wrapped function result after printing elapsed time. One notable detail is that it preserves the original function metadata with functools.wraps."
  },
  {
    "input": "def chunked_13(items, size):\n    for index in range(0, len(items), size):\n        yield items[index:index + size]\n",
    "output": "Under the hood chunked_13 uses generator-based chunking to transform items and size into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, size and returns lists of items in successive chunks. One notable detail is that the final chunk may be shorter if the length is not divisible by size."
  },
  {
    "input": "class file_context_13:\n    def __init__(self, path):\n        self.path = path\n\n    def __enter__(self):\n        self.handle = open(self.path, 'r', encoding='utf-8')\n        return self.handle\n\n    def __exit__(self, exc_type, exc, tb):\n        self.handle.close()\n        return False\n",
    "output": "When called with file_context_13 uses a context manager to transform path and self.handle into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts path, self.handle and returns an open file handle during the with-block. One notable detail is that exceptions are not suppressed because __exit__ returns False."
  },
  {
    "input": "def safe_parse_13(text, fallback=0):\n    try:\n        return int(text)\n    except (TypeError, ValueError):\n        return fallback\n",
    "output": "This utility safe_parse_13 uses exception handling with a fallback to transform text and fallback into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, fallback and returns an integer when parsing succeeds, otherwise the fallback value. One notable detail is that TypeError and ValueError both map to the fallback path."
  },
  {
    "input": "def map_filter_reduce_13(values, factor=2):\n    from functools import reduce\n    doubled = list(map(lambda value: value * factor, values))\n    positives = list(filter(lambda value: value > 0, doubled))\n    return reduce(lambda acc, value: acc + value, positives, 0)\n",
    "output": "The method map_filter_reduce_13 uses higher-order functions to transform values and factor into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, factor and returns the reduced total as a number. One notable detail is that empty inputs return zero because reduce is seeded with an initial value."
  },
  {
    "input": "class Animal_13:\n    def speak(self):\n        return 'sound'\n\nclass Dog_13(Animal_13):\n    def speak(self):\n        return 'woof'\n",
    "output": "Built around Animal_13 and Dog_13 uses inheritance and method overriding to transform self and Animal_13 into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts self, Animal_13 and returns a sound string from the overridden speak method. One notable detail is that the derived class returns a dog-specific sound instead of the base sound."
  },
  {
    "input": "async def async_collect_13(items, delay=0):\n    import asyncio\n    results = []\n    for item in items:\n        await asyncio.sleep(delay)\n        results.append(item)\n    return results\n",
    "output": "Designed to async_collect_13 uses async iteration with await to transform items and delay into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, delay and returns the collected list after awaiting each item. One notable detail is that it yields control back to the event loop on every pass."
  },
  {
    "input": "class SingletonFactory_13:\n    _instance = None\n\n    def __new__(cls, *args, **kwargs):\n        if cls._instance is None:\n            cls._instance = super().__new__(cls)\n        return cls._instance\n",
    "output": "Working through SingletonFactory_13 uses the singleton pattern to transform args and kwargs into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts args, kwargs and returns the same instance every time. One notable detail is that state is shared because __new__ reuses the cached instance."
  },
  {
    "input": "def data_pipeline_13(records, minimum_age=18):\n    filtered = [record for record in records if record['age'] >= minimum_age]\n    normalized = [{**record, 'name': record['name'].strip().title()} for record in filtered]\n    return sorted(normalized, key=lambda record: record['name'])\n",
    "output": "The generator data_pipeline_13 uses a filter-transform-sort pipeline to transform records and minimum_age into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts records, minimum_age and returns a sorted list of normalized record dictionaries. One notable detail is that missing age or name keys will still raise KeyError."
  },
  {
    "input": "def module_worker_a_13(numbers):\n    return sum(numbers)\n\ndef module_worker_b_13(numbers):\n    return max(numbers) if numbers else None\n\ndef module_worker_c_13(numbers):\n    return min(numbers) if numbers else None\n",
    "output": "This decorator module_worker_a_13 uses a small multi-function module to transform numbers and numbers into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts numbers, numbers and returns the computed aggregate returned by each helper. One notable detail is that empty lists cause module_worker_b and module_worker_c to return None."
  },
  {
    "input": "def gcd_calculator_14(left, right):\n    while right != 0:\n        left, right = right, left % right\n    return left\n",
    "output": "The recursive gcd_calculator_14 uses the Euclidean algorithm to transform left and right into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts left, right and returns the greatest common divisor as an integer. One notable detail is that it returns the final left value when right becomes zero."
  },
  {
    "input": "def reverse_text_14(text, preserve_case=False):\n    if preserve_case:\n        return text[::-1]\n    return text.strip()[::-1]\n",
    "output": "Inside this class reverse_text_14 uses slice-based string reversal to transform text and preserve_case into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, preserve_case and returns the reversed string. One notable detail is that it optionally strips surrounding whitespace before reversing."
  },
  {
    "input": "def dedupe_values_14(items, keep_order=True):\n    seen = set()\n    output = []\n    for item in items:\n        if item not in seen:\n            seen.add(item)\n            output.append(item)\n    return output if keep_order else sorted(set(items))\n",
    "output": "The function dedupe_values_14 uses order-preserving deduplication with a set to transform items and keep_order into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, keep_order and returns a list of unique values. One notable detail is that the ordered branch preserves the first appearance of each item."
  },
  {
    "input": "def factorial_recursive_14(n, depth=0):\n    if n <= 1:\n        return 1\n    return n * factorial_recursive_14(n - 1, depth + 1)\n",
    "output": "This class factorial_recursive_14 uses recursive multiplication to transform n and depth into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts n, depth and returns the factorial of n as an integer. One notable detail is that it uses the n <= 1 base case, so zero and one both return one."
  },
  {
    "input": "class BankAccount_14:\n    def __init__(self, owner, balance=0):\n        self.owner = owner\n        self.balance = balance\n\n    def deposit(self, amount):\n        self.balance += amount\n        return self.balance\n\n    def withdraw(self, amount):\n        if amount > self.balance:\n            raise ValueError('insufficient funds')\n        self.balance -= amount\n        return self.balance\n",
    "output": "The algorithm BankAccount_14 uses an object-oriented balance manager to transform owner and balance into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts owner, balance and returns the updated account balance. One notable detail is that withdrawals larger than the current balance raise ValueError."
  },
  {
    "input": "def bubble_sort_14(values, descending=False):\n    for end in range(len(values) - 1, 0, -1):\n        for index in range(end):\n            if (values[index] > values[index + 1]) != descending:\n                values[index], values[index + 1] = values[index + 1], values[index]\n    return values\n",
    "output": "At its core bubble_sort_14 uses bubble sort to transform values and descending into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, descending and returns the list sorted in place and returned. One notable detail is that it works in quadratic time and mutates the original list."
  },
  {
    "input": "def binary_search_14(values, target, offset=0):\n    left = 0\n    right = len(values) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if values[mid] == target:\n            return offset + mid\n        if values[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return -1\n",
    "output": "Under the hood binary_search_14 uses binary search to transform values and target into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, target and returns the index of target or negative one. One notable detail is that the search assumes values is already sorted."
  },
  {
    "input": "def timing_decorator_14(func):\n    import functools, time\n    @functools.wraps(func)\n    def wrapper(*args, **kwargs):\n        start = time.perf_counter()\n        result = func(*args, **kwargs)\n        print(func.__name__, time.perf_counter() - start)\n        return result\n    return wrapper\n",
    "output": "When called with timing_decorator_14 uses a timing wrapper to transform func and args into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts func, args and returns the wrapped function result after printing elapsed time. One notable detail is that it preserves the original function metadata with functools.wraps."
  },
  {
    "input": "def chunked_14(items, size):\n    for index in range(0, len(items), size):\n        yield items[index:index + size]\n",
    "output": "This utility chunked_14 uses generator-based chunking to transform items and size into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, size and returns lists of items in successive chunks. One notable detail is that the final chunk may be shorter if the length is not divisible by size."
  },
  {
    "input": "class file_context_14:\n    def __init__(self, path):\n        self.path = path\n\n    def __enter__(self):\n        self.handle = open(self.path, 'r', encoding='utf-8')\n        return self.handle\n\n    def __exit__(self, exc_type, exc, tb):\n        self.handle.close()\n        return False\n",
    "output": "The method file_context_14 uses a context manager to transform path and self.handle into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts path, self.handle and returns an open file handle during the with-block. One notable detail is that exceptions are not suppressed because __exit__ returns False."
  },
  {
    "input": "def safe_parse_14(text, fallback=0):\n    try:\n        return int(text)\n    except (TypeError, ValueError):\n        return fallback\n",
    "output": "Built around safe_parse_14 uses exception handling with a fallback to transform text and fallback into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, fallback and returns an integer when parsing succeeds, otherwise the fallback value. One notable detail is that TypeError and ValueError both map to the fallback path."
  },
  {
    "input": "def map_filter_reduce_14(values, factor=2):\n    from functools import reduce\n    doubled = list(map(lambda value: value * factor, values))\n    positives = list(filter(lambda value: value > 0, doubled))\n    return reduce(lambda acc, value: acc + value, positives, 0)\n",
    "output": "Designed to map_filter_reduce_14 uses higher-order functions to transform values and factor into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, factor and returns the reduced total as a number. One notable detail is that empty inputs return zero because reduce is seeded with an initial value."
  },
  {
    "input": "class Animal_14:\n    def speak(self):\n        return 'sound'\n\nclass Dog_14(Animal_14):\n    def speak(self):\n        return 'woof'\n",
    "output": "Working through Animal_14 and Dog_14 uses inheritance and method overriding to transform self and Animal_14 into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts self, Animal_14 and returns a sound string from the overridden speak method. One notable detail is that the derived class returns a dog-specific sound instead of the base sound."
  },
  {
    "input": "async def async_collect_14(items, delay=0):\n    import asyncio\n    results = []\n    for item in items:\n        await asyncio.sleep(delay)\n        results.append(item)\n    return results\n",
    "output": "The generator async_collect_14 uses async iteration with await to transform items and delay into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, delay and returns the collected list after awaiting each item. One notable detail is that it yields control back to the event loop on every pass."
  },
  {
    "input": "class SingletonFactory_14:\n    _instance = None\n\n    def __new__(cls, *args, **kwargs):\n        if cls._instance is None:\n            cls._instance = super().__new__(cls)\n        return cls._instance\n",
    "output": "This decorator SingletonFactory_14 uses the singleton pattern to transform args and kwargs into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts args, kwargs and returns the same instance every time. One notable detail is that state is shared because __new__ reuses the cached instance."
  },
  {
    "input": "def data_pipeline_14(records, minimum_age=18):\n    filtered = [record for record in records if record['age'] >= minimum_age]\n    normalized = [{**record, 'name': record['name'].strip().title()} for record in filtered]\n    return sorted(normalized, key=lambda record: record['name'])\n",
    "output": "The recursive data_pipeline_14 uses a filter-transform-sort pipeline to transform records and minimum_age into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts records, minimum_age and returns a sorted list of normalized record dictionaries. One notable detail is that missing age or name keys will still raise KeyError."
  },
  {
    "input": "def module_worker_a_14(numbers):\n    return sum(numbers)\n\ndef module_worker_b_14(numbers):\n    return max(numbers) if numbers else None\n\ndef module_worker_c_14(numbers):\n    return min(numbers) if numbers else None\n",
    "output": "Inside this class module_worker_a_14 uses a small multi-function module to transform numbers and numbers into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts numbers, numbers and returns the computed aggregate returned by each helper. One notable detail is that empty lists cause module_worker_b and module_worker_c to return None."
  },
  {
    "input": "def gcd_calculator_15(left, right):\n    while right != 0:\n        left, right = right, left % right\n    return left\n",
    "output": "The function gcd_calculator_15 uses the Euclidean algorithm to transform left and right into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts left, right and returns the greatest common divisor as an integer. One notable detail is that it returns the final left value when right becomes zero."
  },
  {
    "input": "def reverse_text_15(text, preserve_case=False):\n    if preserve_case:\n        return text[::-1]\n    return text.strip()[::-1]\n",
    "output": "This class reverse_text_15 uses slice-based string reversal to transform text and preserve_case into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, preserve_case and returns the reversed string. One notable detail is that it optionally strips surrounding whitespace before reversing."
  },
  {
    "input": "def dedupe_values_15(items, keep_order=True):\n    seen = set()\n    output = []\n    for item in items:\n        if item not in seen:\n            seen.add(item)\n            output.append(item)\n    return output if keep_order else sorted(set(items))\n",
    "output": "The algorithm dedupe_values_15 uses order-preserving deduplication with a set to transform items and keep_order into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, keep_order and returns a list of unique values. One notable detail is that the ordered branch preserves the first appearance of each item."
  },
  {
    "input": "def factorial_recursive_15(n, depth=0):\n    if n <= 1:\n        return 1\n    return n * factorial_recursive_15(n - 1, depth + 1)\n",
    "output": "At its core factorial_recursive_15 uses recursive multiplication to transform n and depth into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts n, depth and returns the factorial of n as an integer. One notable detail is that it uses the n <= 1 base case, so zero and one both return one."
  },
  {
    "input": "class BankAccount_15:\n    def __init__(self, owner, balance=0):\n        self.owner = owner\n        self.balance = balance\n\n    def deposit(self, amount):\n        self.balance += amount\n        return self.balance\n\n    def withdraw(self, amount):\n        if amount > self.balance:\n            raise ValueError('insufficient funds')\n        self.balance -= amount\n        return self.balance\n",
    "output": "Under the hood BankAccount_15 uses an object-oriented balance manager to transform owner and balance into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts owner, balance and returns the updated account balance. One notable detail is that withdrawals larger than the current balance raise ValueError."
  },
  {
    "input": "def bubble_sort_15(values, descending=False):\n    for end in range(len(values) - 1, 0, -1):\n        for index in range(end):\n            if (values[index] > values[index + 1]) != descending:\n                values[index], values[index + 1] = values[index + 1], values[index]\n    return values\n",
    "output": "When called with bubble_sort_15 uses bubble sort to transform values and descending into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, descending and returns the list sorted in place and returned. One notable detail is that it works in quadratic time and mutates the original list."
  },
  {
    "input": "def binary_search_15(values, target, offset=0):\n    left = 0\n    right = len(values) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if values[mid] == target:\n            return offset + mid\n        if values[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return -1\n",
    "output": "This utility binary_search_15 uses binary search to transform values and target into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, target and returns the index of target or negative one. One notable detail is that the search assumes values is already sorted."
  },
  {
    "input": "def timing_decorator_15(func):\n    import functools, time\n    @functools.wraps(func)\n    def wrapper(*args, **kwargs):\n        start = time.perf_counter()\n        result = func(*args, **kwargs)\n        print(func.__name__, time.perf_counter() - start)\n        return result\n    return wrapper\n",
    "output": "The method timing_decorator_15 uses a timing wrapper to transform func and args into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts func, args and returns the wrapped function result after printing elapsed time. One notable detail is that it preserves the original function metadata with functools.wraps."
  },
  {
    "input": "def chunked_15(items, size):\n    for index in range(0, len(items), size):\n        yield items[index:index + size]\n",
    "output": "Built around chunked_15 uses generator-based chunking to transform items and size into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, size and returns lists of items in successive chunks. One notable detail is that the final chunk may be shorter if the length is not divisible by size."
  },
  {
    "input": "class file_context_15:\n    def __init__(self, path):\n        self.path = path\n\n    def __enter__(self):\n        self.handle = open(self.path, 'r', encoding='utf-8')\n        return self.handle\n\n    def __exit__(self, exc_type, exc, tb):\n        self.handle.close()\n        return False\n",
    "output": "Designed to file_context_15 uses a context manager to transform path and self.handle into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts path, self.handle and returns an open file handle during the with-block. One notable detail is that exceptions are not suppressed because __exit__ returns False."
  },
  {
    "input": "def safe_parse_15(text, fallback=0):\n    try:\n        return int(text)\n    except (TypeError, ValueError):\n        return fallback\n",
    "output": "Working through safe_parse_15 uses exception handling with a fallback to transform text and fallback into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, fallback and returns an integer when parsing succeeds, otherwise the fallback value. One notable detail is that TypeError and ValueError both map to the fallback path."
  },
  {
    "input": "def map_filter_reduce_15(values, factor=2):\n    from functools import reduce\n    doubled = list(map(lambda value: value * factor, values))\n    positives = list(filter(lambda value: value > 0, doubled))\n    return reduce(lambda acc, value: acc + value, positives, 0)\n",
    "output": "The generator map_filter_reduce_15 uses higher-order functions to transform values and factor into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, factor and returns the reduced total as a number. One notable detail is that empty inputs return zero because reduce is seeded with an initial value."
  },
  {
    "input": "class Animal_15:\n    def speak(self):\n        return 'sound'\n\nclass Dog_15(Animal_15):\n    def speak(self):\n        return 'woof'\n",
    "output": "This decorator Animal_15 and Dog_15 uses inheritance and method overriding to transform self and Animal_15 into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts self, Animal_15 and returns a sound string from the overridden speak method. One notable detail is that the derived class returns a dog-specific sound instead of the base sound."
  },
  {
    "input": "async def async_collect_15(items, delay=0):\n    import asyncio\n    results = []\n    for item in items:\n        await asyncio.sleep(delay)\n        results.append(item)\n    return results\n",
    "output": "The recursive async_collect_15 uses async iteration with await to transform items and delay into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, delay and returns the collected list after awaiting each item. One notable detail is that it yields control back to the event loop on every pass."
  },
  {
    "input": "class SingletonFactory_15:\n    _instance = None\n\n    def __new__(cls, *args, **kwargs):\n        if cls._instance is None:\n            cls._instance = super().__new__(cls)\n        return cls._instance\n",
    "output": "Inside this class SingletonFactory_15 uses the singleton pattern to transform args and kwargs into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts args, kwargs and returns the same instance every time. One notable detail is that state is shared because __new__ reuses the cached instance."
  },
  {
    "input": "def data_pipeline_15(records, minimum_age=18):\n    filtered = [record for record in records if record['age'] >= minimum_age]\n    normalized = [{**record, 'name': record['name'].strip().title()} for record in filtered]\n    return sorted(normalized, key=lambda record: record['name'])\n",
    "output": "The function data_pipeline_15 uses a filter-transform-sort pipeline to transform records and minimum_age into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts records, minimum_age and returns a sorted list of normalized record dictionaries. One notable detail is that missing age or name keys will still raise KeyError."
  },
  {
    "input": "def module_worker_a_15(numbers):\n    return sum(numbers)\n\ndef module_worker_b_15(numbers):\n    return max(numbers) if numbers else None\n\ndef module_worker_c_15(numbers):\n    return min(numbers) if numbers else None\n",
    "output": "This class module_worker_a_15 uses a small multi-function module to transform numbers and numbers into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts numbers, numbers and returns the computed aggregate returned by each helper. One notable detail is that empty lists cause module_worker_b and module_worker_c to return None."
  },
  {
    "input": "def gcd_calculator_16(left, right):\n    while right != 0:\n        left, right = right, left % right\n    return left\n",
    "output": "The algorithm gcd_calculator_16 uses the Euclidean algorithm to transform left and right into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts left, right and returns the greatest common divisor as an integer. One notable detail is that it returns the final left value when right becomes zero."
  },
  {
    "input": "def reverse_text_16(text, preserve_case=False):\n    if preserve_case:\n        return text[::-1]\n    return text.strip()[::-1]\n",
    "output": "At its core reverse_text_16 uses slice-based string reversal to transform text and preserve_case into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, preserve_case and returns the reversed string. One notable detail is that it optionally strips surrounding whitespace before reversing."
  },
  {
    "input": "def dedupe_values_16(items, keep_order=True):\n    seen = set()\n    output = []\n    for item in items:\n        if item not in seen:\n            seen.add(item)\n            output.append(item)\n    return output if keep_order else sorted(set(items))\n",
    "output": "Under the hood dedupe_values_16 uses order-preserving deduplication with a set to transform items and keep_order into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, keep_order and returns a list of unique values. One notable detail is that the ordered branch preserves the first appearance of each item."
  },
  {
    "input": "def factorial_recursive_16(n, depth=0):\n    if n <= 1:\n        return 1\n    return n * factorial_recursive_16(n - 1, depth + 1)\n",
    "output": "When called with factorial_recursive_16 uses recursive multiplication to transform n and depth into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts n, depth and returns the factorial of n as an integer. One notable detail is that it uses the n <= 1 base case, so zero and one both return one."
  },
  {
    "input": "class BankAccount_16:\n    def __init__(self, owner, balance=0):\n        self.owner = owner\n        self.balance = balance\n\n    def deposit(self, amount):\n        self.balance += amount\n        return self.balance\n\n    def withdraw(self, amount):\n        if amount > self.balance:\n            raise ValueError('insufficient funds')\n        self.balance -= amount\n        return self.balance\n",
    "output": "This utility BankAccount_16 uses an object-oriented balance manager to transform owner and balance into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts owner, balance and returns the updated account balance. One notable detail is that withdrawals larger than the current balance raise ValueError."
  },
  {
    "input": "def bubble_sort_16(values, descending=False):\n    for end in range(len(values) - 1, 0, -1):\n        for index in range(end):\n            if (values[index] > values[index + 1]) != descending:\n                values[index], values[index + 1] = values[index + 1], values[index]\n    return values\n",
    "output": "The method bubble_sort_16 uses bubble sort to transform values and descending into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, descending and returns the list sorted in place and returned. One notable detail is that it works in quadratic time and mutates the original list."
  },
  {
    "input": "def binary_search_16(values, target, offset=0):\n    left = 0\n    right = len(values) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if values[mid] == target:\n            return offset + mid\n        if values[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return -1\n",
    "output": "Built around binary_search_16 uses binary search to transform values and target into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, target and returns the index of target or negative one. One notable detail is that the search assumes values is already sorted."
  },
  {
    "input": "def timing_decorator_16(func):\n    import functools, time\n    @functools.wraps(func)\n    def wrapper(*args, **kwargs):\n        start = time.perf_counter()\n        result = func(*args, **kwargs)\n        print(func.__name__, time.perf_counter() - start)\n        return result\n    return wrapper\n",
    "output": "Designed to timing_decorator_16 uses a timing wrapper to transform func and args into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts func, args and returns the wrapped function result after printing elapsed time. One notable detail is that it preserves the original function metadata with functools.wraps."
  },
  {
    "input": "def chunked_16(items, size):\n    for index in range(0, len(items), size):\n        yield items[index:index + size]\n",
    "output": "Working through chunked_16 uses generator-based chunking to transform items and size into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, size and returns lists of items in successive chunks. One notable detail is that the final chunk may be shorter if the length is not divisible by size."
  },
  {
    "input": "class file_context_16:\n    def __init__(self, path):\n        self.path = path\n\n    def __enter__(self):\n        self.handle = open(self.path, 'r', encoding='utf-8')\n        return self.handle\n\n    def __exit__(self, exc_type, exc, tb):\n        self.handle.close()\n        return False\n",
    "output": "The generator file_context_16 uses a context manager to transform path and self.handle into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts path, self.handle and returns an open file handle during the with-block. One notable detail is that exceptions are not suppressed because __exit__ returns False."
  },
  {
    "input": "def safe_parse_16(text, fallback=0):\n    try:\n        return int(text)\n    except (TypeError, ValueError):\n        return fallback\n",
    "output": "This decorator safe_parse_16 uses exception handling with a fallback to transform text and fallback into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, fallback and returns an integer when parsing succeeds, otherwise the fallback value. One notable detail is that TypeError and ValueError both map to the fallback path."
  },
  {
    "input": "def map_filter_reduce_16(values, factor=2):\n    from functools import reduce\n    doubled = list(map(lambda value: value * factor, values))\n    positives = list(filter(lambda value: value > 0, doubled))\n    return reduce(lambda acc, value: acc + value, positives, 0)\n",
    "output": "The recursive map_filter_reduce_16 uses higher-order functions to transform values and factor into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, factor and returns the reduced total as a number. One notable detail is that empty inputs return zero because reduce is seeded with an initial value."
  },
  {
    "input": "class Animal_16:\n    def speak(self):\n        return 'sound'\n\nclass Dog_16(Animal_16):\n    def speak(self):\n        return 'woof'\n",
    "output": "Inside this class Animal_16 and Dog_16 uses inheritance and method overriding to transform self and Animal_16 into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts self, Animal_16 and returns a sound string from the overridden speak method. One notable detail is that the derived class returns a dog-specific sound instead of the base sound."
  },
  {
    "input": "async def async_collect_16(items, delay=0):\n    import asyncio\n    results = []\n    for item in items:\n        await asyncio.sleep(delay)\n        results.append(item)\n    return results\n",
    "output": "The function async_collect_16 uses async iteration with await to transform items and delay into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, delay and returns the collected list after awaiting each item. One notable detail is that it yields control back to the event loop on every pass."
  },
  {
    "input": "class SingletonFactory_16:\n    _instance = None\n\n    def __new__(cls, *args, **kwargs):\n        if cls._instance is None:\n            cls._instance = super().__new__(cls)\n        return cls._instance\n",
    "output": "This class SingletonFactory_16 uses the singleton pattern to transform args and kwargs into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts args, kwargs and returns the same instance every time. One notable detail is that state is shared because __new__ reuses the cached instance."
  },
  {
    "input": "def data_pipeline_16(records, minimum_age=18):\n    filtered = [record for record in records if record['age'] >= minimum_age]\n    normalized = [{**record, 'name': record['name'].strip().title()} for record in filtered]\n    return sorted(normalized, key=lambda record: record['name'])\n",
    "output": "The algorithm data_pipeline_16 uses a filter-transform-sort pipeline to transform records and minimum_age into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts records, minimum_age and returns a sorted list of normalized record dictionaries. One notable detail is that missing age or name keys will still raise KeyError."
  },
  {
    "input": "def module_worker_a_16(numbers):\n    return sum(numbers)\n\ndef module_worker_b_16(numbers):\n    return max(numbers) if numbers else None\n\ndef module_worker_c_16(numbers):\n    return min(numbers) if numbers else None\n",
    "output": "At its core module_worker_a_16 uses a small multi-function module to transform numbers and numbers into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts numbers, numbers and returns the computed aggregate returned by each helper. One notable detail is that empty lists cause module_worker_b and module_worker_c to return None."
  },
  {
    "input": "def gcd_calculator_17(left, right):\n    while right != 0:\n        left, right = right, left % right\n    return left\n",
    "output": "Under the hood gcd_calculator_17 uses the Euclidean algorithm to transform left and right into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts left, right and returns the greatest common divisor as an integer. One notable detail is that it returns the final left value when right becomes zero."
  },
  {
    "input": "def reverse_text_17(text, preserve_case=False):\n    if preserve_case:\n        return text[::-1]\n    return text.strip()[::-1]\n",
    "output": "When called with reverse_text_17 uses slice-based string reversal to transform text and preserve_case into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, preserve_case and returns the reversed string. One notable detail is that it optionally strips surrounding whitespace before reversing."
  },
  {
    "input": "def dedupe_values_17(items, keep_order=True):\n    seen = set()\n    output = []\n    for item in items:\n        if item not in seen:\n            seen.add(item)\n            output.append(item)\n    return output if keep_order else sorted(set(items))\n",
    "output": "This utility dedupe_values_17 uses order-preserving deduplication with a set to transform items and keep_order into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, keep_order and returns a list of unique values. One notable detail is that the ordered branch preserves the first appearance of each item."
  },
  {
    "input": "def factorial_recursive_17(n, depth=0):\n    if n <= 1:\n        return 1\n    return n * factorial_recursive_17(n - 1, depth + 1)\n",
    "output": "The method factorial_recursive_17 uses recursive multiplication to transform n and depth into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts n, depth and returns the factorial of n as an integer. One notable detail is that it uses the n <= 1 base case, so zero and one both return one."
  },
  {
    "input": "class BankAccount_17:\n    def __init__(self, owner, balance=0):\n        self.owner = owner\n        self.balance = balance\n\n    def deposit(self, amount):\n        self.balance += amount\n        return self.balance\n\n    def withdraw(self, amount):\n        if amount > self.balance:\n            raise ValueError('insufficient funds')\n        self.balance -= amount\n        return self.balance\n",
    "output": "Built around BankAccount_17 uses an object-oriented balance manager to transform owner and balance into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts owner, balance and returns the updated account balance. One notable detail is that withdrawals larger than the current balance raise ValueError."
  },
  {
    "input": "def bubble_sort_17(values, descending=False):\n    for end in range(len(values) - 1, 0, -1):\n        for index in range(end):\n            if (values[index] > values[index + 1]) != descending:\n                values[index], values[index + 1] = values[index + 1], values[index]\n    return values\n",
    "output": "Designed to bubble_sort_17 uses bubble sort to transform values and descending into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, descending and returns the list sorted in place and returned. One notable detail is that it works in quadratic time and mutates the original list."
  },
  {
    "input": "def binary_search_17(values, target, offset=0):\n    left = 0\n    right = len(values) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if values[mid] == target:\n            return offset + mid\n        if values[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return -1\n",
    "output": "Working through binary_search_17 uses binary search to transform values and target into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, target and returns the index of target or negative one. One notable detail is that the search assumes values is already sorted."
  },
  {
    "input": "def timing_decorator_17(func):\n    import functools, time\n    @functools.wraps(func)\n    def wrapper(*args, **kwargs):\n        start = time.perf_counter()\n        result = func(*args, **kwargs)\n        print(func.__name__, time.perf_counter() - start)\n        return result\n    return wrapper\n",
    "output": "The generator timing_decorator_17 uses a timing wrapper to transform func and args into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts func, args and returns the wrapped function result after printing elapsed time. One notable detail is that it preserves the original function metadata with functools.wraps."
  },
  {
    "input": "def chunked_17(items, size):\n    for index in range(0, len(items), size):\n        yield items[index:index + size]\n",
    "output": "This decorator chunked_17 uses generator-based chunking to transform items and size into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, size and returns lists of items in successive chunks. One notable detail is that the final chunk may be shorter if the length is not divisible by size."
  },
  {
    "input": "class file_context_17:\n    def __init__(self, path):\n        self.path = path\n\n    def __enter__(self):\n        self.handle = open(self.path, 'r', encoding='utf-8')\n        return self.handle\n\n    def __exit__(self, exc_type, exc, tb):\n        self.handle.close()\n        return False\n",
    "output": "The recursive file_context_17 uses a context manager to transform path and self.handle into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts path, self.handle and returns an open file handle during the with-block. One notable detail is that exceptions are not suppressed because __exit__ returns False."
  },
  {
    "input": "def safe_parse_17(text, fallback=0):\n    try:\n        return int(text)\n    except (TypeError, ValueError):\n        return fallback\n",
    "output": "Inside this class safe_parse_17 uses exception handling with a fallback to transform text and fallback into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, fallback and returns an integer when parsing succeeds, otherwise the fallback value. One notable detail is that TypeError and ValueError both map to the fallback path."
  },
  {
    "input": "def map_filter_reduce_17(values, factor=2):\n    from functools import reduce\n    doubled = list(map(lambda value: value * factor, values))\n    positives = list(filter(lambda value: value > 0, doubled))\n    return reduce(lambda acc, value: acc + value, positives, 0)\n",
    "output": "The function map_filter_reduce_17 uses higher-order functions to transform values and factor into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, factor and returns the reduced total as a number. One notable detail is that empty inputs return zero because reduce is seeded with an initial value."
  },
  {
    "input": "class Animal_17:\n    def speak(self):\n        return 'sound'\n\nclass Dog_17(Animal_17):\n    def speak(self):\n        return 'woof'\n",
    "output": "This class Animal_17 and Dog_17 uses inheritance and method overriding to transform self and Animal_17 into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts self, Animal_17 and returns a sound string from the overridden speak method. One notable detail is that the derived class returns a dog-specific sound instead of the base sound."
  },
  {
    "input": "async def async_collect_17(items, delay=0):\n    import asyncio\n    results = []\n    for item in items:\n        await asyncio.sleep(delay)\n        results.append(item)\n    return results\n",
    "output": "The algorithm async_collect_17 uses async iteration with await to transform items and delay into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, delay and returns the collected list after awaiting each item. One notable detail is that it yields control back to the event loop on every pass."
  },
  {
    "input": "class SingletonFactory_17:\n    _instance = None\n\n    def __new__(cls, *args, **kwargs):\n        if cls._instance is None:\n            cls._instance = super().__new__(cls)\n        return cls._instance\n",
    "output": "At its core SingletonFactory_17 uses the singleton pattern to transform args and kwargs into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts args, kwargs and returns the same instance every time. One notable detail is that state is shared because __new__ reuses the cached instance."
  },
  {
    "input": "def data_pipeline_17(records, minimum_age=18):\n    filtered = [record for record in records if record['age'] >= minimum_age]\n    normalized = [{**record, 'name': record['name'].strip().title()} for record in filtered]\n    return sorted(normalized, key=lambda record: record['name'])\n",
    "output": "Under the hood data_pipeline_17 uses a filter-transform-sort pipeline to transform records and minimum_age into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts records, minimum_age and returns a sorted list of normalized record dictionaries. One notable detail is that missing age or name keys will still raise KeyError."
  },
  {
    "input": "def module_worker_a_17(numbers):\n    return sum(numbers)\n\ndef module_worker_b_17(numbers):\n    return max(numbers) if numbers else None\n\ndef module_worker_c_17(numbers):\n    return min(numbers) if numbers else None\n",
    "output": "When called with module_worker_a_17 uses a small multi-function module to transform numbers and numbers into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts numbers, numbers and returns the computed aggregate returned by each helper. One notable detail is that empty lists cause module_worker_b and module_worker_c to return None."
  },
  {
    "input": "def gcd_calculator_18(left, right):\n    while right != 0:\n        left, right = right, left % right\n    return left\n",
    "output": "This utility gcd_calculator_18 uses the Euclidean algorithm to transform left and right into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts left, right and returns the greatest common divisor as an integer. One notable detail is that it returns the final left value when right becomes zero."
  },
  {
    "input": "def reverse_text_18(text, preserve_case=False):\n    if preserve_case:\n        return text[::-1]\n    return text.strip()[::-1]\n",
    "output": "The method reverse_text_18 uses slice-based string reversal to transform text and preserve_case into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, preserve_case and returns the reversed string. One notable detail is that it optionally strips surrounding whitespace before reversing."
  },
  {
    "input": "def dedupe_values_18(items, keep_order=True):\n    seen = set()\n    output = []\n    for item in items:\n        if item not in seen:\n            seen.add(item)\n            output.append(item)\n    return output if keep_order else sorted(set(items))\n",
    "output": "Built around dedupe_values_18 uses order-preserving deduplication with a set to transform items and keep_order into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, keep_order and returns a list of unique values. One notable detail is that the ordered branch preserves the first appearance of each item."
  },
  {
    "input": "def factorial_recursive_18(n, depth=0):\n    if n <= 1:\n        return 1\n    return n * factorial_recursive_18(n - 1, depth + 1)\n",
    "output": "Designed to factorial_recursive_18 uses recursive multiplication to transform n and depth into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts n, depth and returns the factorial of n as an integer. One notable detail is that it uses the n <= 1 base case, so zero and one both return one."
  },
  {
    "input": "class BankAccount_18:\n    def __init__(self, owner, balance=0):\n        self.owner = owner\n        self.balance = balance\n\n    def deposit(self, amount):\n        self.balance += amount\n        return self.balance\n\n    def withdraw(self, amount):\n        if amount > self.balance:\n            raise ValueError('insufficient funds')\n        self.balance -= amount\n        return self.balance\n",
    "output": "Working through BankAccount_18 uses an object-oriented balance manager to transform owner and balance into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts owner, balance and returns the updated account balance. One notable detail is that withdrawals larger than the current balance raise ValueError."
  },
  {
    "input": "def bubble_sort_18(values, descending=False):\n    for end in range(len(values) - 1, 0, -1):\n        for index in range(end):\n            if (values[index] > values[index + 1]) != descending:\n                values[index], values[index + 1] = values[index + 1], values[index]\n    return values\n",
    "output": "The generator bubble_sort_18 uses bubble sort to transform values and descending into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, descending and returns the list sorted in place and returned. One notable detail is that it works in quadratic time and mutates the original list."
  },
  {
    "input": "def binary_search_18(values, target, offset=0):\n    left = 0\n    right = len(values) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if values[mid] == target:\n            return offset + mid\n        if values[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return -1\n",
    "output": "This decorator binary_search_18 uses binary search to transform values and target into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, target and returns the index of target or negative one. One notable detail is that the search assumes values is already sorted."
  },
  {
    "input": "def timing_decorator_18(func):\n    import functools, time\n    @functools.wraps(func)\n    def wrapper(*args, **kwargs):\n        start = time.perf_counter()\n        result = func(*args, **kwargs)\n        print(func.__name__, time.perf_counter() - start)\n        return result\n    return wrapper\n",
    "output": "The recursive timing_decorator_18 uses a timing wrapper to transform func and args into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts func, args and returns the wrapped function result after printing elapsed time. One notable detail is that it preserves the original function metadata with functools.wraps."
  },
  {
    "input": "def chunked_18(items, size):\n    for index in range(0, len(items), size):\n        yield items[index:index + size]\n",
    "output": "Inside this class chunked_18 uses generator-based chunking to transform items and size into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, size and returns lists of items in successive chunks. One notable detail is that the final chunk may be shorter if the length is not divisible by size."
  },
  {
    "input": "class file_context_18:\n    def __init__(self, path):\n        self.path = path\n\n    def __enter__(self):\n        self.handle = open(self.path, 'r', encoding='utf-8')\n        return self.handle\n\n    def __exit__(self, exc_type, exc, tb):\n        self.handle.close()\n        return False\n",
    "output": "The function file_context_18 uses a context manager to transform path and self.handle into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts path, self.handle and returns an open file handle during the with-block. One notable detail is that exceptions are not suppressed because __exit__ returns False."
  },
  {
    "input": "def safe_parse_18(text, fallback=0):\n    try:\n        return int(text)\n    except (TypeError, ValueError):\n        return fallback\n",
    "output": "This class safe_parse_18 uses exception handling with a fallback to transform text and fallback into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, fallback and returns an integer when parsing succeeds, otherwise the fallback value. One notable detail is that TypeError and ValueError both map to the fallback path."
  },
  {
    "input": "def map_filter_reduce_18(values, factor=2):\n    from functools import reduce\n    doubled = list(map(lambda value: value * factor, values))\n    positives = list(filter(lambda value: value > 0, doubled))\n    return reduce(lambda acc, value: acc + value, positives, 0)\n",
    "output": "The algorithm map_filter_reduce_18 uses higher-order functions to transform values and factor into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, factor and returns the reduced total as a number. One notable detail is that empty inputs return zero because reduce is seeded with an initial value."
  },
  {
    "input": "class Animal_18:\n    def speak(self):\n        return 'sound'\n\nclass Dog_18(Animal_18):\n    def speak(self):\n        return 'woof'\n",
    "output": "At its core Animal_18 and Dog_18 uses inheritance and method overriding to transform self and Animal_18 into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts self, Animal_18 and returns a sound string from the overridden speak method. One notable detail is that the derived class returns a dog-specific sound instead of the base sound."
  },
  {
    "input": "async def async_collect_18(items, delay=0):\n    import asyncio\n    results = []\n    for item in items:\n        await asyncio.sleep(delay)\n        results.append(item)\n    return results\n",
    "output": "Under the hood async_collect_18 uses async iteration with await to transform items and delay into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, delay and returns the collected list after awaiting each item. One notable detail is that it yields control back to the event loop on every pass."
  },
  {
    "input": "class SingletonFactory_18:\n    _instance = None\n\n    def __new__(cls, *args, **kwargs):\n        if cls._instance is None:\n            cls._instance = super().__new__(cls)\n        return cls._instance\n",
    "output": "When called with SingletonFactory_18 uses the singleton pattern to transform args and kwargs into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts args, kwargs and returns the same instance every time. One notable detail is that state is shared because __new__ reuses the cached instance."
  },
  {
    "input": "def data_pipeline_18(records, minimum_age=18):\n    filtered = [record for record in records if record['age'] >= minimum_age]\n    normalized = [{**record, 'name': record['name'].strip().title()} for record in filtered]\n    return sorted(normalized, key=lambda record: record['name'])\n",
    "output": "This utility data_pipeline_18 uses a filter-transform-sort pipeline to transform records and minimum_age into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts records, minimum_age and returns a sorted list of normalized record dictionaries. One notable detail is that missing age or name keys will still raise KeyError."
  },
  {
    "input": "def module_worker_a_18(numbers):\n    return sum(numbers)\n\ndef module_worker_b_18(numbers):\n    return max(numbers) if numbers else None\n\ndef module_worker_c_18(numbers):\n    return min(numbers) if numbers else None\n",
    "output": "The method module_worker_a_18 uses a small multi-function module to transform numbers and numbers into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts numbers, numbers and returns the computed aggregate returned by each helper. One notable detail is that empty lists cause module_worker_b and module_worker_c to return None."
  },
  {
    "input": "def gcd_calculator_19(left, right):\n    while right != 0:\n        left, right = right, left % right\n    return left\n",
    "output": "Built around gcd_calculator_19 uses the Euclidean algorithm to transform left and right into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts left, right and returns the greatest common divisor as an integer. One notable detail is that it returns the final left value when right becomes zero."
  },
  {
    "input": "def reverse_text_19(text, preserve_case=False):\n    if preserve_case:\n        return text[::-1]\n    return text.strip()[::-1]\n",
    "output": "Designed to reverse_text_19 uses slice-based string reversal to transform text and preserve_case into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, preserve_case and returns the reversed string. One notable detail is that it optionally strips surrounding whitespace before reversing."
  },
  {
    "input": "def dedupe_values_19(items, keep_order=True):\n    seen = set()\n    output = []\n    for item in items:\n        if item not in seen:\n            seen.add(item)\n            output.append(item)\n    return output if keep_order else sorted(set(items))\n",
    "output": "Working through dedupe_values_19 uses order-preserving deduplication with a set to transform items and keep_order into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, keep_order and returns a list of unique values. One notable detail is that the ordered branch preserves the first appearance of each item."
  },
  {
    "input": "def factorial_recursive_19(n, depth=0):\n    if n <= 1:\n        return 1\n    return n * factorial_recursive_19(n - 1, depth + 1)\n",
    "output": "The generator factorial_recursive_19 uses recursive multiplication to transform n and depth into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts n, depth and returns the factorial of n as an integer. One notable detail is that it uses the n <= 1 base case, so zero and one both return one."
  },
  {
    "input": "class BankAccount_19:\n    def __init__(self, owner, balance=0):\n        self.owner = owner\n        self.balance = balance\n\n    def deposit(self, amount):\n        self.balance += amount\n        return self.balance\n\n    def withdraw(self, amount):\n        if amount > self.balance:\n            raise ValueError('insufficient funds')\n        self.balance -= amount\n        return self.balance\n",
    "output": "This decorator BankAccount_19 uses an object-oriented balance manager to transform owner and balance into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts owner, balance and returns the updated account balance. One notable detail is that withdrawals larger than the current balance raise ValueError."
  },
  {
    "input": "def bubble_sort_19(values, descending=False):\n    for end in range(len(values) - 1, 0, -1):\n        for index in range(end):\n            if (values[index] > values[index + 1]) != descending:\n                values[index], values[index + 1] = values[index + 1], values[index]\n    return values\n",
    "output": "The recursive bubble_sort_19 uses bubble sort to transform values and descending into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, descending and returns the list sorted in place and returned. One notable detail is that it works in quadratic time and mutates the original list."
  },
  {
    "input": "def binary_search_19(values, target, offset=0):\n    left = 0\n    right = len(values) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if values[mid] == target:\n            return offset + mid\n        if values[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return -1\n",
    "output": "Inside this class binary_search_19 uses binary search to transform values and target into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, target and returns the index of target or negative one. One notable detail is that the search assumes values is already sorted."
  },
  {
    "input": "def timing_decorator_19(func):\n    import functools, time\n    @functools.wraps(func)\n    def wrapper(*args, **kwargs):\n        start = time.perf_counter()\n        result = func(*args, **kwargs)\n        print(func.__name__, time.perf_counter() - start)\n        return result\n    return wrapper\n",
    "output": "The function timing_decorator_19 uses a timing wrapper to transform func and args into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts func, args and returns the wrapped function result after printing elapsed time. One notable detail is that it preserves the original function metadata with functools.wraps."
  },
  {
    "input": "def chunked_19(items, size):\n    for index in range(0, len(items), size):\n        yield items[index:index + size]\n",
    "output": "This class chunked_19 uses generator-based chunking to transform items and size into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, size and returns lists of items in successive chunks. One notable detail is that the final chunk may be shorter if the length is not divisible by size."
  },
  {
    "input": "class file_context_19:\n    def __init__(self, path):\n        self.path = path\n\n    def __enter__(self):\n        self.handle = open(self.path, 'r', encoding='utf-8')\n        return self.handle\n\n    def __exit__(self, exc_type, exc, tb):\n        self.handle.close()\n        return False\n",
    "output": "The algorithm file_context_19 uses a context manager to transform path and self.handle into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts path, self.handle and returns an open file handle during the with-block. One notable detail is that exceptions are not suppressed because __exit__ returns False."
  },
  {
    "input": "def safe_parse_19(text, fallback=0):\n    try:\n        return int(text)\n    except (TypeError, ValueError):\n        return fallback\n",
    "output": "At its core safe_parse_19 uses exception handling with a fallback to transform text and fallback into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, fallback and returns an integer when parsing succeeds, otherwise the fallback value. One notable detail is that TypeError and ValueError both map to the fallback path."
  },
  {
    "input": "def map_filter_reduce_19(values, factor=2):\n    from functools import reduce\n    doubled = list(map(lambda value: value * factor, values))\n    positives = list(filter(lambda value: value > 0, doubled))\n    return reduce(lambda acc, value: acc + value, positives, 0)\n",
    "output": "Under the hood map_filter_reduce_19 uses higher-order functions to transform values and factor into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, factor and returns the reduced total as a number. One notable detail is that empty inputs return zero because reduce is seeded with an initial value."
  },
  {
    "input": "class Animal_19:\n    def speak(self):\n        return 'sound'\n\nclass Dog_19(Animal_19):\n    def speak(self):\n        return 'woof'\n",
    "output": "When called with Animal_19 and Dog_19 uses inheritance and method overriding to transform self and Animal_19 into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts self, Animal_19 and returns a sound string from the overridden speak method. One notable detail is that the derived class returns a dog-specific sound instead of the base sound."
  },
  {
    "input": "async def async_collect_19(items, delay=0):\n    import asyncio\n    results = []\n    for item in items:\n        await asyncio.sleep(delay)\n        results.append(item)\n    return results\n",
    "output": "This utility async_collect_19 uses async iteration with await to transform items and delay into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, delay and returns the collected list after awaiting each item. One notable detail is that it yields control back to the event loop on every pass."
  },
  {
    "input": "class SingletonFactory_19:\n    _instance = None\n\n    def __new__(cls, *args, **kwargs):\n        if cls._instance is None:\n            cls._instance = super().__new__(cls)\n        return cls._instance\n",
    "output": "The method SingletonFactory_19 uses the singleton pattern to transform args and kwargs into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts args, kwargs and returns the same instance every time. One notable detail is that state is shared because __new__ reuses the cached instance."
  },
  {
    "input": "def data_pipeline_19(records, minimum_age=18):\n    filtered = [record for record in records if record['age'] >= minimum_age]\n    normalized = [{**record, 'name': record['name'].strip().title()} for record in filtered]\n    return sorted(normalized, key=lambda record: record['name'])\n",
    "output": "Built around data_pipeline_19 uses a filter-transform-sort pipeline to transform records and minimum_age into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts records, minimum_age and returns a sorted list of normalized record dictionaries. One notable detail is that missing age or name keys will still raise KeyError."
  },
  {
    "input": "def module_worker_a_19(numbers):\n    return sum(numbers)\n\ndef module_worker_b_19(numbers):\n    return max(numbers) if numbers else None\n\ndef module_worker_c_19(numbers):\n    return min(numbers) if numbers else None\n",
    "output": "Designed to module_worker_a_19 uses a small multi-function module to transform numbers and numbers into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts numbers, numbers and returns the computed aggregate returned by each helper. One notable detail is that empty lists cause module_worker_b and module_worker_c to return None."
  },
  {
    "input": "def gcd_calculator_20(left, right):\n    while right != 0:\n        left, right = right, left % right\n    return left\n",
    "output": "Working through gcd_calculator_20 uses the Euclidean algorithm to transform left and right into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts left, right and returns the greatest common divisor as an integer. One notable detail is that it returns the final left value when right becomes zero."
  },
  {
    "input": "def reverse_text_20(text, preserve_case=False):\n    if preserve_case:\n        return text[::-1]\n    return text.strip()[::-1]\n",
    "output": "The generator reverse_text_20 uses slice-based string reversal to transform text and preserve_case into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, preserve_case and returns the reversed string. One notable detail is that it optionally strips surrounding whitespace before reversing."
  },
  {
    "input": "def dedupe_values_20(items, keep_order=True):\n    seen = set()\n    output = []\n    for item in items:\n        if item not in seen:\n            seen.add(item)\n            output.append(item)\n    return output if keep_order else sorted(set(items))\n",
    "output": "This decorator dedupe_values_20 uses order-preserving deduplication with a set to transform items and keep_order into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, keep_order and returns a list of unique values. One notable detail is that the ordered branch preserves the first appearance of each item."
  },
  {
    "input": "def factorial_recursive_20(n, depth=0):\n    if n <= 1:\n        return 1\n    return n * factorial_recursive_20(n - 1, depth + 1)\n",
    "output": "The recursive factorial_recursive_20 uses recursive multiplication to transform n and depth into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts n, depth and returns the factorial of n as an integer. One notable detail is that it uses the n <= 1 base case, so zero and one both return one."
  },
  {
    "input": "class BankAccount_20:\n    def __init__(self, owner, balance=0):\n        self.owner = owner\n        self.balance = balance\n\n    def deposit(self, amount):\n        self.balance += amount\n        return self.balance\n\n    def withdraw(self, amount):\n        if amount > self.balance:\n            raise ValueError('insufficient funds')\n        self.balance -= amount\n        return self.balance\n",
    "output": "Inside this class BankAccount_20 uses an object-oriented balance manager to transform owner and balance into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts owner, balance and returns the updated account balance. One notable detail is that withdrawals larger than the current balance raise ValueError."
  },
  {
    "input": "def bubble_sort_20(values, descending=False):\n    for end in range(len(values) - 1, 0, -1):\n        for index in range(end):\n            if (values[index] > values[index + 1]) != descending:\n                values[index], values[index + 1] = values[index + 1], values[index]\n    return values\n",
    "output": "The function bubble_sort_20 uses bubble sort to transform values and descending into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, descending and returns the list sorted in place and returned. One notable detail is that it works in quadratic time and mutates the original list."
  },
  {
    "input": "def binary_search_20(values, target, offset=0):\n    left = 0\n    right = len(values) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if values[mid] == target:\n            return offset + mid\n        if values[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return -1\n",
    "output": "This class binary_search_20 uses binary search to transform values and target into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, target and returns the index of target or negative one. One notable detail is that the search assumes values is already sorted."
  },
  {
    "input": "def timing_decorator_20(func):\n    import functools, time\n    @functools.wraps(func)\n    def wrapper(*args, **kwargs):\n        start = time.perf_counter()\n        result = func(*args, **kwargs)\n        print(func.__name__, time.perf_counter() - start)\n        return result\n    return wrapper\n",
    "output": "The algorithm timing_decorator_20 uses a timing wrapper to transform func and args into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts func, args and returns the wrapped function result after printing elapsed time. One notable detail is that it preserves the original function metadata with functools.wraps."
  },
  {
    "input": "def chunked_20(items, size):\n    for index in range(0, len(items), size):\n        yield items[index:index + size]\n",
    "output": "At its core chunked_20 uses generator-based chunking to transform items and size into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, size and returns lists of items in successive chunks. One notable detail is that the final chunk may be shorter if the length is not divisible by size."
  },
  {
    "input": "class file_context_20:\n    def __init__(self, path):\n        self.path = path\n\n    def __enter__(self):\n        self.handle = open(self.path, 'r', encoding='utf-8')\n        return self.handle\n\n    def __exit__(self, exc_type, exc, tb):\n        self.handle.close()\n        return False\n",
    "output": "Under the hood file_context_20 uses a context manager to transform path and self.handle into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts path, self.handle and returns an open file handle during the with-block. One notable detail is that exceptions are not suppressed because __exit__ returns False."
  },
  {
    "input": "def safe_parse_20(text, fallback=0):\n    try:\n        return int(text)\n    except (TypeError, ValueError):\n        return fallback\n",
    "output": "When called with safe_parse_20 uses exception handling with a fallback to transform text and fallback into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, fallback and returns an integer when parsing succeeds, otherwise the fallback value. One notable detail is that TypeError and ValueError both map to the fallback path."
  },
  {
    "input": "def map_filter_reduce_20(values, factor=2):\n    from functools import reduce\n    doubled = list(map(lambda value: value * factor, values))\n    positives = list(filter(lambda value: value > 0, doubled))\n    return reduce(lambda acc, value: acc + value, positives, 0)\n",
    "output": "This utility map_filter_reduce_20 uses higher-order functions to transform values and factor into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, factor and returns the reduced total as a number. One notable detail is that empty inputs return zero because reduce is seeded with an initial value."
  },
  {
    "input": "class Animal_20:\n    def speak(self):\n        return 'sound'\n\nclass Dog_20(Animal_20):\n    def speak(self):\n        return 'woof'\n",
    "output": "The method Animal_20 and Dog_20 uses inheritance and method overriding to transform self and Animal_20 into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts self, Animal_20 and returns a sound string from the overridden speak method. One notable detail is that the derived class returns a dog-specific sound instead of the base sound."
  },
  {
    "input": "async def async_collect_20(items, delay=0):\n    import asyncio\n    results = []\n    for item in items:\n        await asyncio.sleep(delay)\n        results.append(item)\n    return results\n",
    "output": "Built around async_collect_20 uses async iteration with await to transform items and delay into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, delay and returns the collected list after awaiting each item. One notable detail is that it yields control back to the event loop on every pass."
  },
  {
    "input": "class SingletonFactory_20:\n    _instance = None\n\n    def __new__(cls, *args, **kwargs):\n        if cls._instance is None:\n            cls._instance = super().__new__(cls)\n        return cls._instance\n",
    "output": "Designed to SingletonFactory_20 uses the singleton pattern to transform args and kwargs into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts args, kwargs and returns the same instance every time. One notable detail is that state is shared because __new__ reuses the cached instance."
  },
  {
    "input": "def data_pipeline_20(records, minimum_age=18):\n    filtered = [record for record in records if record['age'] >= minimum_age]\n    normalized = [{**record, 'name': record['name'].strip().title()} for record in filtered]\n    return sorted(normalized, key=lambda record: record['name'])\n",
    "output": "Working through data_pipeline_20 uses a filter-transform-sort pipeline to transform records and minimum_age into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts records, minimum_age and returns a sorted list of normalized record dictionaries. One notable detail is that missing age or name keys will still raise KeyError."
  },
  {
    "input": "def module_worker_a_20(numbers):\n    return sum(numbers)\n\ndef module_worker_b_20(numbers):\n    return max(numbers) if numbers else None\n\ndef module_worker_c_20(numbers):\n    return min(numbers) if numbers else None\n",
    "output": "The generator module_worker_a_20 uses a small multi-function module to transform numbers and numbers into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts numbers, numbers and returns the computed aggregate returned by each helper. One notable detail is that empty lists cause module_worker_b and module_worker_c to return None."
  },
  {
    "input": "def gcd_calculator_21(left, right):\n    while right != 0:\n        left, right = right, left % right\n    return left\n",
    "output": "This decorator gcd_calculator_21 uses the Euclidean algorithm to transform left and right into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts left, right and returns the greatest common divisor as an integer. One notable detail is that it returns the final left value when right becomes zero."
  },
  {
    "input": "def reverse_text_21(text, preserve_case=False):\n    if preserve_case:\n        return text[::-1]\n    return text.strip()[::-1]\n",
    "output": "The recursive reverse_text_21 uses slice-based string reversal to transform text and preserve_case into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, preserve_case and returns the reversed string. One notable detail is that it optionally strips surrounding whitespace before reversing."
  },
  {
    "input": "def dedupe_values_21(items, keep_order=True):\n    seen = set()\n    output = []\n    for item in items:\n        if item not in seen:\n            seen.add(item)\n            output.append(item)\n    return output if keep_order else sorted(set(items))\n",
    "output": "Inside this class dedupe_values_21 uses order-preserving deduplication with a set to transform items and keep_order into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, keep_order and returns a list of unique values. One notable detail is that the ordered branch preserves the first appearance of each item."
  },
  {
    "input": "def factorial_recursive_21(n, depth=0):\n    if n <= 1:\n        return 1\n    return n * factorial_recursive_21(n - 1, depth + 1)\n",
    "output": "The function factorial_recursive_21 uses recursive multiplication to transform n and depth into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts n, depth and returns the factorial of n as an integer. One notable detail is that it uses the n <= 1 base case, so zero and one both return one."
  },
  {
    "input": "class BankAccount_21:\n    def __init__(self, owner, balance=0):\n        self.owner = owner\n        self.balance = balance\n\n    def deposit(self, amount):\n        self.balance += amount\n        return self.balance\n\n    def withdraw(self, amount):\n        if amount > self.balance:\n            raise ValueError('insufficient funds')\n        self.balance -= amount\n        return self.balance\n",
    "output": "This class BankAccount_21 uses an object-oriented balance manager to transform owner and balance into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts owner, balance and returns the updated account balance. One notable detail is that withdrawals larger than the current balance raise ValueError."
  },
  {
    "input": "def bubble_sort_21(values, descending=False):\n    for end in range(len(values) - 1, 0, -1):\n        for index in range(end):\n            if (values[index] > values[index + 1]) != descending:\n                values[index], values[index + 1] = values[index + 1], values[index]\n    return values\n",
    "output": "The algorithm bubble_sort_21 uses bubble sort to transform values and descending into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, descending and returns the list sorted in place and returned. One notable detail is that it works in quadratic time and mutates the original list."
  },
  {
    "input": "def binary_search_21(values, target, offset=0):\n    left = 0\n    right = len(values) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if values[mid] == target:\n            return offset + mid\n        if values[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return -1\n",
    "output": "At its core binary_search_21 uses binary search to transform values and target into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, target and returns the index of target or negative one. One notable detail is that the search assumes values is already sorted."
  },
  {
    "input": "def timing_decorator_21(func):\n    import functools, time\n    @functools.wraps(func)\n    def wrapper(*args, **kwargs):\n        start = time.perf_counter()\n        result = func(*args, **kwargs)\n        print(func.__name__, time.perf_counter() - start)\n        return result\n    return wrapper\n",
    "output": "Under the hood timing_decorator_21 uses a timing wrapper to transform func and args into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts func, args and returns the wrapped function result after printing elapsed time. One notable detail is that it preserves the original function metadata with functools.wraps."
  },
  {
    "input": "def chunked_21(items, size):\n    for index in range(0, len(items), size):\n        yield items[index:index + size]\n",
    "output": "When called with chunked_21 uses generator-based chunking to transform items and size into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, size and returns lists of items in successive chunks. One notable detail is that the final chunk may be shorter if the length is not divisible by size."
  },
  {
    "input": "class file_context_21:\n    def __init__(self, path):\n        self.path = path\n\n    def __enter__(self):\n        self.handle = open(self.path, 'r', encoding='utf-8')\n        return self.handle\n\n    def __exit__(self, exc_type, exc, tb):\n        self.handle.close()\n        return False\n",
    "output": "This utility file_context_21 uses a context manager to transform path and self.handle into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts path, self.handle and returns an open file handle during the with-block. One notable detail is that exceptions are not suppressed because __exit__ returns False."
  },
  {
    "input": "def safe_parse_21(text, fallback=0):\n    try:\n        return int(text)\n    except (TypeError, ValueError):\n        return fallback\n",
    "output": "The method safe_parse_21 uses exception handling with a fallback to transform text and fallback into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, fallback and returns an integer when parsing succeeds, otherwise the fallback value. One notable detail is that TypeError and ValueError both map to the fallback path."
  },
  {
    "input": "def map_filter_reduce_21(values, factor=2):\n    from functools import reduce\n    doubled = list(map(lambda value: value * factor, values))\n    positives = list(filter(lambda value: value > 0, doubled))\n    return reduce(lambda acc, value: acc + value, positives, 0)\n",
    "output": "Built around map_filter_reduce_21 uses higher-order functions to transform values and factor into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, factor and returns the reduced total as a number. One notable detail is that empty inputs return zero because reduce is seeded with an initial value."
  },
  {
    "input": "class Animal_21:\n    def speak(self):\n        return 'sound'\n\nclass Dog_21(Animal_21):\n    def speak(self):\n        return 'woof'\n",
    "output": "Designed to Animal_21 and Dog_21 uses inheritance and method overriding to transform self and Animal_21 into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts self, Animal_21 and returns a sound string from the overridden speak method. One notable detail is that the derived class returns a dog-specific sound instead of the base sound."
  },
  {
    "input": "async def async_collect_21(items, delay=0):\n    import asyncio\n    results = []\n    for item in items:\n        await asyncio.sleep(delay)\n        results.append(item)\n    return results\n",
    "output": "Working through async_collect_21 uses async iteration with await to transform items and delay into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, delay and returns the collected list after awaiting each item. One notable detail is that it yields control back to the event loop on every pass."
  },
  {
    "input": "class SingletonFactory_21:\n    _instance = None\n\n    def __new__(cls, *args, **kwargs):\n        if cls._instance is None:\n            cls._instance = super().__new__(cls)\n        return cls._instance\n",
    "output": "The generator SingletonFactory_21 uses the singleton pattern to transform args and kwargs into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts args, kwargs and returns the same instance every time. One notable detail is that state is shared because __new__ reuses the cached instance."
  },
  {
    "input": "def data_pipeline_21(records, minimum_age=18):\n    filtered = [record for record in records if record['age'] >= minimum_age]\n    normalized = [{**record, 'name': record['name'].strip().title()} for record in filtered]\n    return sorted(normalized, key=lambda record: record['name'])\n",
    "output": "This decorator data_pipeline_21 uses a filter-transform-sort pipeline to transform records and minimum_age into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts records, minimum_age and returns a sorted list of normalized record dictionaries. One notable detail is that missing age or name keys will still raise KeyError."
  },
  {
    "input": "def module_worker_a_21(numbers):\n    return sum(numbers)\n\ndef module_worker_b_21(numbers):\n    return max(numbers) if numbers else None\n\ndef module_worker_c_21(numbers):\n    return min(numbers) if numbers else None\n",
    "output": "The recursive module_worker_a_21 uses a small multi-function module to transform numbers and numbers into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts numbers, numbers and returns the computed aggregate returned by each helper. One notable detail is that empty lists cause module_worker_b and module_worker_c to return None."
  },
  {
    "input": "def gcd_calculator_22(left, right):\n    while right != 0:\n        left, right = right, left % right\n    return left\n",
    "output": "Inside this class gcd_calculator_22 uses the Euclidean algorithm to transform left and right into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts left, right and returns the greatest common divisor as an integer. One notable detail is that it returns the final left value when right becomes zero."
  },
  {
    "input": "def reverse_text_22(text, preserve_case=False):\n    if preserve_case:\n        return text[::-1]\n    return text.strip()[::-1]\n",
    "output": "The function reverse_text_22 uses slice-based string reversal to transform text and preserve_case into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, preserve_case and returns the reversed string. One notable detail is that it optionally strips surrounding whitespace before reversing."
  },
  {
    "input": "def dedupe_values_22(items, keep_order=True):\n    seen = set()\n    output = []\n    for item in items:\n        if item not in seen:\n            seen.add(item)\n            output.append(item)\n    return output if keep_order else sorted(set(items))\n",
    "output": "This class dedupe_values_22 uses order-preserving deduplication with a set to transform items and keep_order into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, keep_order and returns a list of unique values. One notable detail is that the ordered branch preserves the first appearance of each item."
  },
  {
    "input": "def factorial_recursive_22(n, depth=0):\n    if n <= 1:\n        return 1\n    return n * factorial_recursive_22(n - 1, depth + 1)\n",
    "output": "The algorithm factorial_recursive_22 uses recursive multiplication to transform n and depth into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts n, depth and returns the factorial of n as an integer. One notable detail is that it uses the n <= 1 base case, so zero and one both return one."
  },
  {
    "input": "class BankAccount_22:\n    def __init__(self, owner, balance=0):\n        self.owner = owner\n        self.balance = balance\n\n    def deposit(self, amount):\n        self.balance += amount\n        return self.balance\n\n    def withdraw(self, amount):\n        if amount > self.balance:\n            raise ValueError('insufficient funds')\n        self.balance -= amount\n        return self.balance\n",
    "output": "At its core BankAccount_22 uses an object-oriented balance manager to transform owner and balance into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts owner, balance and returns the updated account balance. One notable detail is that withdrawals larger than the current balance raise ValueError."
  },
  {
    "input": "def bubble_sort_22(values, descending=False):\n    for end in range(len(values) - 1, 0, -1):\n        for index in range(end):\n            if (values[index] > values[index + 1]) != descending:\n                values[index], values[index + 1] = values[index + 1], values[index]\n    return values\n",
    "output": "Under the hood bubble_sort_22 uses bubble sort to transform values and descending into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, descending and returns the list sorted in place and returned. One notable detail is that it works in quadratic time and mutates the original list."
  },
  {
    "input": "def binary_search_22(values, target, offset=0):\n    left = 0\n    right = len(values) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if values[mid] == target:\n            return offset + mid\n        if values[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return -1\n",
    "output": "When called with binary_search_22 uses binary search to transform values and target into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, target and returns the index of target or negative one. One notable detail is that the search assumes values is already sorted."
  },
  {
    "input": "def timing_decorator_22(func):\n    import functools, time\n    @functools.wraps(func)\n    def wrapper(*args, **kwargs):\n        start = time.perf_counter()\n        result = func(*args, **kwargs)\n        print(func.__name__, time.perf_counter() - start)\n        return result\n    return wrapper\n",
    "output": "This utility timing_decorator_22 uses a timing wrapper to transform func and args into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts func, args and returns the wrapped function result after printing elapsed time. One notable detail is that it preserves the original function metadata with functools.wraps."
  },
  {
    "input": "def chunked_22(items, size):\n    for index in range(0, len(items), size):\n        yield items[index:index + size]\n",
    "output": "The method chunked_22 uses generator-based chunking to transform items and size into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, size and returns lists of items in successive chunks. One notable detail is that the final chunk may be shorter if the length is not divisible by size."
  },
  {
    "input": "class file_context_22:\n    def __init__(self, path):\n        self.path = path\n\n    def __enter__(self):\n        self.handle = open(self.path, 'r', encoding='utf-8')\n        return self.handle\n\n    def __exit__(self, exc_type, exc, tb):\n        self.handle.close()\n        return False\n",
    "output": "Built around file_context_22 uses a context manager to transform path and self.handle into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts path, self.handle and returns an open file handle during the with-block. One notable detail is that exceptions are not suppressed because __exit__ returns False."
  },
  {
    "input": "def safe_parse_22(text, fallback=0):\n    try:\n        return int(text)\n    except (TypeError, ValueError):\n        return fallback\n",
    "output": "Designed to safe_parse_22 uses exception handling with a fallback to transform text and fallback into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, fallback and returns an integer when parsing succeeds, otherwise the fallback value. One notable detail is that TypeError and ValueError both map to the fallback path."
  },
  {
    "input": "def map_filter_reduce_22(values, factor=2):\n    from functools import reduce\n    doubled = list(map(lambda value: value * factor, values))\n    positives = list(filter(lambda value: value > 0, doubled))\n    return reduce(lambda acc, value: acc + value, positives, 0)\n",
    "output": "Working through map_filter_reduce_22 uses higher-order functions to transform values and factor into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, factor and returns the reduced total as a number. One notable detail is that empty inputs return zero because reduce is seeded with an initial value."
  },
  {
    "input": "class Animal_22:\n    def speak(self):\n        return 'sound'\n\nclass Dog_22(Animal_22):\n    def speak(self):\n        return 'woof'\n",
    "output": "The generator Animal_22 and Dog_22 uses inheritance and method overriding to transform self and Animal_22 into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts self, Animal_22 and returns a sound string from the overridden speak method. One notable detail is that the derived class returns a dog-specific sound instead of the base sound."
  },
  {
    "input": "async def async_collect_22(items, delay=0):\n    import asyncio\n    results = []\n    for item in items:\n        await asyncio.sleep(delay)\n        results.append(item)\n    return results\n",
    "output": "This decorator async_collect_22 uses async iteration with await to transform items and delay into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, delay and returns the collected list after awaiting each item. One notable detail is that it yields control back to the event loop on every pass."
  },
  {
    "input": "class SingletonFactory_22:\n    _instance = None\n\n    def __new__(cls, *args, **kwargs):\n        if cls._instance is None:\n            cls._instance = super().__new__(cls)\n        return cls._instance\n",
    "output": "The recursive SingletonFactory_22 uses the singleton pattern to transform args and kwargs into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts args, kwargs and returns the same instance every time. One notable detail is that state is shared because __new__ reuses the cached instance."
  },
  {
    "input": "def data_pipeline_22(records, minimum_age=18):\n    filtered = [record for record in records if record['age'] >= minimum_age]\n    normalized = [{**record, 'name': record['name'].strip().title()} for record in filtered]\n    return sorted(normalized, key=lambda record: record['name'])\n",
    "output": "Inside this class data_pipeline_22 uses a filter-transform-sort pipeline to transform records and minimum_age into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts records, minimum_age and returns a sorted list of normalized record dictionaries. One notable detail is that missing age or name keys will still raise KeyError."
  },
  {
    "input": "def module_worker_a_22(numbers):\n    return sum(numbers)\n\ndef module_worker_b_22(numbers):\n    return max(numbers) if numbers else None\n\ndef module_worker_c_22(numbers):\n    return min(numbers) if numbers else None\n",
    "output": "The function module_worker_a_22 uses a small multi-function module to transform numbers and numbers into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts numbers, numbers and returns the computed aggregate returned by each helper. One notable detail is that empty lists cause module_worker_b and module_worker_c to return None."
  },
  {
    "input": "def gcd_calculator_23(left, right):\n    while right != 0:\n        left, right = right, left % right\n    return left\n",
    "output": "This class gcd_calculator_23 uses the Euclidean algorithm to transform left and right into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts left, right and returns the greatest common divisor as an integer. One notable detail is that it returns the final left value when right becomes zero."
  },
  {
    "input": "def reverse_text_23(text, preserve_case=False):\n    if preserve_case:\n        return text[::-1]\n    return text.strip()[::-1]\n",
    "output": "The algorithm reverse_text_23 uses slice-based string reversal to transform text and preserve_case into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, preserve_case and returns the reversed string. One notable detail is that it optionally strips surrounding whitespace before reversing."
  },
  {
    "input": "def dedupe_values_23(items, keep_order=True):\n    seen = set()\n    output = []\n    for item in items:\n        if item not in seen:\n            seen.add(item)\n            output.append(item)\n    return output if keep_order else sorted(set(items))\n",
    "output": "At its core dedupe_values_23 uses order-preserving deduplication with a set to transform items and keep_order into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, keep_order and returns a list of unique values. One notable detail is that the ordered branch preserves the first appearance of each item."
  },
  {
    "input": "def factorial_recursive_23(n, depth=0):\n    if n <= 1:\n        return 1\n    return n * factorial_recursive_23(n - 1, depth + 1)\n",
    "output": "Under the hood factorial_recursive_23 uses recursive multiplication to transform n and depth into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts n, depth and returns the factorial of n as an integer. One notable detail is that it uses the n <= 1 base case, so zero and one both return one."
  },
  {
    "input": "class BankAccount_23:\n    def __init__(self, owner, balance=0):\n        self.owner = owner\n        self.balance = balance\n\n    def deposit(self, amount):\n        self.balance += amount\n        return self.balance\n\n    def withdraw(self, amount):\n        if amount > self.balance:\n            raise ValueError('insufficient funds')\n        self.balance -= amount\n        return self.balance\n",
    "output": "When called with BankAccount_23 uses an object-oriented balance manager to transform owner and balance into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts owner, balance and returns the updated account balance. One notable detail is that withdrawals larger than the current balance raise ValueError."
  },
  {
    "input": "def bubble_sort_23(values, descending=False):\n    for end in range(len(values) - 1, 0, -1):\n        for index in range(end):\n            if (values[index] > values[index + 1]) != descending:\n                values[index], values[index + 1] = values[index + 1], values[index]\n    return values\n",
    "output": "This utility bubble_sort_23 uses bubble sort to transform values and descending into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, descending and returns the list sorted in place and returned. One notable detail is that it works in quadratic time and mutates the original list."
  },
  {
    "input": "def binary_search_23(values, target, offset=0):\n    left = 0\n    right = len(values) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if values[mid] == target:\n            return offset + mid\n        if values[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return -1\n",
    "output": "The method binary_search_23 uses binary search to transform values and target into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, target and returns the index of target or negative one. One notable detail is that the search assumes values is already sorted."
  },
  {
    "input": "def timing_decorator_23(func):\n    import functools, time\n    @functools.wraps(func)\n    def wrapper(*args, **kwargs):\n        start = time.perf_counter()\n        result = func(*args, **kwargs)\n        print(func.__name__, time.perf_counter() - start)\n        return result\n    return wrapper\n",
    "output": "Built around timing_decorator_23 uses a timing wrapper to transform func and args into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts func, args and returns the wrapped function result after printing elapsed time. One notable detail is that it preserves the original function metadata with functools.wraps."
  },
  {
    "input": "def chunked_23(items, size):\n    for index in range(0, len(items), size):\n        yield items[index:index + size]\n",
    "output": "Designed to chunked_23 uses generator-based chunking to transform items and size into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, size and returns lists of items in successive chunks. One notable detail is that the final chunk may be shorter if the length is not divisible by size."
  },
  {
    "input": "class file_context_23:\n    def __init__(self, path):\n        self.path = path\n\n    def __enter__(self):\n        self.handle = open(self.path, 'r', encoding='utf-8')\n        return self.handle\n\n    def __exit__(self, exc_type, exc, tb):\n        self.handle.close()\n        return False\n",
    "output": "Working through file_context_23 uses a context manager to transform path and self.handle into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts path, self.handle and returns an open file handle during the with-block. One notable detail is that exceptions are not suppressed because __exit__ returns False."
  },
  {
    "input": "def safe_parse_23(text, fallback=0):\n    try:\n        return int(text)\n    except (TypeError, ValueError):\n        return fallback\n",
    "output": "The generator safe_parse_23 uses exception handling with a fallback to transform text and fallback into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, fallback and returns an integer when parsing succeeds, otherwise the fallback value. One notable detail is that TypeError and ValueError both map to the fallback path."
  },
  {
    "input": "def map_filter_reduce_23(values, factor=2):\n    from functools import reduce\n    doubled = list(map(lambda value: value * factor, values))\n    positives = list(filter(lambda value: value > 0, doubled))\n    return reduce(lambda acc, value: acc + value, positives, 0)\n",
    "output": "This decorator map_filter_reduce_23 uses higher-order functions to transform values and factor into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, factor and returns the reduced total as a number. One notable detail is that empty inputs return zero because reduce is seeded with an initial value."
  },
  {
    "input": "class Animal_23:\n    def speak(self):\n        return 'sound'\n\nclass Dog_23(Animal_23):\n    def speak(self):\n        return 'woof'\n",
    "output": "The recursive Animal_23 and Dog_23 uses inheritance and method overriding to transform self and Animal_23 into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts self, Animal_23 and returns a sound string from the overridden speak method. One notable detail is that the derived class returns a dog-specific sound instead of the base sound."
  },
  {
    "input": "async def async_collect_23(items, delay=0):\n    import asyncio\n    results = []\n    for item in items:\n        await asyncio.sleep(delay)\n        results.append(item)\n    return results\n",
    "output": "Inside this class async_collect_23 uses async iteration with await to transform items and delay into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, delay and returns the collected list after awaiting each item. One notable detail is that it yields control back to the event loop on every pass."
  },
  {
    "input": "class SingletonFactory_23:\n    _instance = None\n\n    def __new__(cls, *args, **kwargs):\n        if cls._instance is None:\n            cls._instance = super().__new__(cls)\n        return cls._instance\n",
    "output": "The function SingletonFactory_23 uses the singleton pattern to transform args and kwargs into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts args, kwargs and returns the same instance every time. One notable detail is that state is shared because __new__ reuses the cached instance."
  },
  {
    "input": "def data_pipeline_23(records, minimum_age=18):\n    filtered = [record for record in records if record['age'] >= minimum_age]\n    normalized = [{**record, 'name': record['name'].strip().title()} for record in filtered]\n    return sorted(normalized, key=lambda record: record['name'])\n",
    "output": "This class data_pipeline_23 uses a filter-transform-sort pipeline to transform records and minimum_age into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts records, minimum_age and returns a sorted list of normalized record dictionaries. One notable detail is that missing age or name keys will still raise KeyError."
  },
  {
    "input": "def module_worker_a_23(numbers):\n    return sum(numbers)\n\ndef module_worker_b_23(numbers):\n    return max(numbers) if numbers else None\n\ndef module_worker_c_23(numbers):\n    return min(numbers) if numbers else None\n",
    "output": "The algorithm module_worker_a_23 uses a small multi-function module to transform numbers and numbers into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts numbers, numbers and returns the computed aggregate returned by each helper. One notable detail is that empty lists cause module_worker_b and module_worker_c to return None."
  },
  {
    "input": "def gcd_calculator_24(left, right):\n    while right != 0:\n        left, right = right, left % right\n    return left\n",
    "output": "At its core gcd_calculator_24 uses the Euclidean algorithm to transform left and right into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts left, right and returns the greatest common divisor as an integer. One notable detail is that it returns the final left value when right becomes zero."
  },
  {
    "input": "def reverse_text_24(text, preserve_case=False):\n    if preserve_case:\n        return text[::-1]\n    return text.strip()[::-1]\n",
    "output": "Under the hood reverse_text_24 uses slice-based string reversal to transform text and preserve_case into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, preserve_case and returns the reversed string. One notable detail is that it optionally strips surrounding whitespace before reversing."
  },
  {
    "input": "def dedupe_values_24(items, keep_order=True):\n    seen = set()\n    output = []\n    for item in items:\n        if item not in seen:\n            seen.add(item)\n            output.append(item)\n    return output if keep_order else sorted(set(items))\n",
    "output": "When called with dedupe_values_24 uses order-preserving deduplication with a set to transform items and keep_order into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, keep_order and returns a list of unique values. One notable detail is that the ordered branch preserves the first appearance of each item."
  },
  {
    "input": "def factorial_recursive_24(n, depth=0):\n    if n <= 1:\n        return 1\n    return n * factorial_recursive_24(n - 1, depth + 1)\n",
    "output": "This utility factorial_recursive_24 uses recursive multiplication to transform n and depth into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts n, depth and returns the factorial of n as an integer. One notable detail is that it uses the n <= 1 base case, so zero and one both return one."
  },
  {
    "input": "class BankAccount_24:\n    def __init__(self, owner, balance=0):\n        self.owner = owner\n        self.balance = balance\n\n    def deposit(self, amount):\n        self.balance += amount\n        return self.balance\n\n    def withdraw(self, amount):\n        if amount > self.balance:\n            raise ValueError('insufficient funds')\n        self.balance -= amount\n        return self.balance\n",
    "output": "The method BankAccount_24 uses an object-oriented balance manager to transform owner and balance into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts owner, balance and returns the updated account balance. One notable detail is that withdrawals larger than the current balance raise ValueError."
  },
  {
    "input": "def bubble_sort_24(values, descending=False):\n    for end in range(len(values) - 1, 0, -1):\n        for index in range(end):\n            if (values[index] > values[index + 1]) != descending:\n                values[index], values[index + 1] = values[index + 1], values[index]\n    return values\n",
    "output": "Built around bubble_sort_24 uses bubble sort to transform values and descending into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, descending and returns the list sorted in place and returned. One notable detail is that it works in quadratic time and mutates the original list."
  },
  {
    "input": "def binary_search_24(values, target, offset=0):\n    left = 0\n    right = len(values) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if values[mid] == target:\n            return offset + mid\n        if values[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return -1\n",
    "output": "Designed to binary_search_24 uses binary search to transform values and target into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, target and returns the index of target or negative one. One notable detail is that the search assumes values is already sorted."
  },
  {
    "input": "def timing_decorator_24(func):\n    import functools, time\n    @functools.wraps(func)\n    def wrapper(*args, **kwargs):\n        start = time.perf_counter()\n        result = func(*args, **kwargs)\n        print(func.__name__, time.perf_counter() - start)\n        return result\n    return wrapper\n",
    "output": "Working through timing_decorator_24 uses a timing wrapper to transform func and args into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts func, args and returns the wrapped function result after printing elapsed time. One notable detail is that it preserves the original function metadata with functools.wraps."
  },
  {
    "input": "def chunked_24(items, size):\n    for index in range(0, len(items), size):\n        yield items[index:index + size]\n",
    "output": "The generator chunked_24 uses generator-based chunking to transform items and size into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, size and returns lists of items in successive chunks. One notable detail is that the final chunk may be shorter if the length is not divisible by size."
  },
  {
    "input": "class file_context_24:\n    def __init__(self, path):\n        self.path = path\n\n    def __enter__(self):\n        self.handle = open(self.path, 'r', encoding='utf-8')\n        return self.handle\n\n    def __exit__(self, exc_type, exc, tb):\n        self.handle.close()\n        return False\n",
    "output": "This decorator file_context_24 uses a context manager to transform path and self.handle into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts path, self.handle and returns an open file handle during the with-block. One notable detail is that exceptions are not suppressed because __exit__ returns False."
  },
  {
    "input": "def safe_parse_24(text, fallback=0):\n    try:\n        return int(text)\n    except (TypeError, ValueError):\n        return fallback\n",
    "output": "The recursive safe_parse_24 uses exception handling with a fallback to transform text and fallback into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, fallback and returns an integer when parsing succeeds, otherwise the fallback value. One notable detail is that TypeError and ValueError both map to the fallback path."
  },
  {
    "input": "def map_filter_reduce_24(values, factor=2):\n    from functools import reduce\n    doubled = list(map(lambda value: value * factor, values))\n    positives = list(filter(lambda value: value > 0, doubled))\n    return reduce(lambda acc, value: acc + value, positives, 0)\n",
    "output": "Inside this class map_filter_reduce_24 uses higher-order functions to transform values and factor into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, factor and returns the reduced total as a number. One notable detail is that empty inputs return zero because reduce is seeded with an initial value."
  },
  {
    "input": "class Animal_24:\n    def speak(self):\n        return 'sound'\n\nclass Dog_24(Animal_24):\n    def speak(self):\n        return 'woof'\n",
    "output": "The function Animal_24 and Dog_24 uses inheritance and method overriding to transform self and Animal_24 into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts self, Animal_24 and returns a sound string from the overridden speak method. One notable detail is that the derived class returns a dog-specific sound instead of the base sound."
  },
  {
    "input": "async def async_collect_24(items, delay=0):\n    import asyncio\n    results = []\n    for item in items:\n        await asyncio.sleep(delay)\n        results.append(item)\n    return results\n",
    "output": "This class async_collect_24 uses async iteration with await to transform items and delay into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, delay and returns the collected list after awaiting each item. One notable detail is that it yields control back to the event loop on every pass."
  },
  {
    "input": "class SingletonFactory_24:\n    _instance = None\n\n    def __new__(cls, *args, **kwargs):\n        if cls._instance is None:\n            cls._instance = super().__new__(cls)\n        return cls._instance\n",
    "output": "The algorithm SingletonFactory_24 uses the singleton pattern to transform args and kwargs into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts args, kwargs and returns the same instance every time. One notable detail is that state is shared because __new__ reuses the cached instance."
  },
  {
    "input": "def data_pipeline_24(records, minimum_age=18):\n    filtered = [record for record in records if record['age'] >= minimum_age]\n    normalized = [{**record, 'name': record['name'].strip().title()} for record in filtered]\n    return sorted(normalized, key=lambda record: record['name'])\n",
    "output": "At its core data_pipeline_24 uses a filter-transform-sort pipeline to transform records and minimum_age into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts records, minimum_age and returns a sorted list of normalized record dictionaries. One notable detail is that missing age or name keys will still raise KeyError."
  },
  {
    "input": "def module_worker_a_24(numbers):\n    return sum(numbers)\n\ndef module_worker_b_24(numbers):\n    return max(numbers) if numbers else None\n\ndef module_worker_c_24(numbers):\n    return min(numbers) if numbers else None\n",
    "output": "Under the hood module_worker_a_24 uses a small multi-function module to transform numbers and numbers into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts numbers, numbers and returns the computed aggregate returned by each helper. One notable detail is that empty lists cause module_worker_b and module_worker_c to return None."
  },
  {
    "input": "def gcd_calculator_25(left, right):\n    while right != 0:\n        left, right = right, left % right\n    return left\n",
    "output": "When called with gcd_calculator_25 uses the Euclidean algorithm to transform left and right into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts left, right and returns the greatest common divisor as an integer. One notable detail is that it returns the final left value when right becomes zero."
  },
  {
    "input": "def reverse_text_25(text, preserve_case=False):\n    if preserve_case:\n        return text[::-1]\n    return text.strip()[::-1]\n",
    "output": "This utility reverse_text_25 uses slice-based string reversal to transform text and preserve_case into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, preserve_case and returns the reversed string. One notable detail is that it optionally strips surrounding whitespace before reversing."
  },
  {
    "input": "def dedupe_values_25(items, keep_order=True):\n    seen = set()\n    output = []\n    for item in items:\n        if item not in seen:\n            seen.add(item)\n            output.append(item)\n    return output if keep_order else sorted(set(items))\n",
    "output": "The method dedupe_values_25 uses order-preserving deduplication with a set to transform items and keep_order into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, keep_order and returns a list of unique values. One notable detail is that the ordered branch preserves the first appearance of each item."
  },
  {
    "input": "def factorial_recursive_25(n, depth=0):\n    if n <= 1:\n        return 1\n    return n * factorial_recursive_25(n - 1, depth + 1)\n",
    "output": "Built around factorial_recursive_25 uses recursive multiplication to transform n and depth into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts n, depth and returns the factorial of n as an integer. One notable detail is that it uses the n <= 1 base case, so zero and one both return one."
  },
  {
    "input": "class BankAccount_25:\n    def __init__(self, owner, balance=0):\n        self.owner = owner\n        self.balance = balance\n\n    def deposit(self, amount):\n        self.balance += amount\n        return self.balance\n\n    def withdraw(self, amount):\n        if amount > self.balance:\n            raise ValueError('insufficient funds')\n        self.balance -= amount\n        return self.balance\n",
    "output": "Designed to BankAccount_25 uses an object-oriented balance manager to transform owner and balance into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts owner, balance and returns the updated account balance. One notable detail is that withdrawals larger than the current balance raise ValueError."
  },
  {
    "input": "def bubble_sort_25(values, descending=False):\n    for end in range(len(values) - 1, 0, -1):\n        for index in range(end):\n            if (values[index] > values[index + 1]) != descending:\n                values[index], values[index + 1] = values[index + 1], values[index]\n    return values\n",
    "output": "Working through bubble_sort_25 uses bubble sort to transform values and descending into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, descending and returns the list sorted in place and returned. One notable detail is that it works in quadratic time and mutates the original list."
  },
  {
    "input": "def binary_search_25(values, target, offset=0):\n    left = 0\n    right = len(values) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if values[mid] == target:\n            return offset + mid\n        if values[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return -1\n",
    "output": "The generator binary_search_25 uses binary search to transform values and target into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, target and returns the index of target or negative one. One notable detail is that the search assumes values is already sorted."
  },
  {
    "input": "def timing_decorator_25(func):\n    import functools, time\n    @functools.wraps(func)\n    def wrapper(*args, **kwargs):\n        start = time.perf_counter()\n        result = func(*args, **kwargs)\n        print(func.__name__, time.perf_counter() - start)\n        return result\n    return wrapper\n",
    "output": "This decorator timing_decorator_25 uses a timing wrapper to transform func and args into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts func, args and returns the wrapped function result after printing elapsed time. One notable detail is that it preserves the original function metadata with functools.wraps."
  },
  {
    "input": "def chunked_25(items, size):\n    for index in range(0, len(items), size):\n        yield items[index:index + size]\n",
    "output": "The recursive chunked_25 uses generator-based chunking to transform items and size into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, size and returns lists of items in successive chunks. One notable detail is that the final chunk may be shorter if the length is not divisible by size."
  },
  {
    "input": "class file_context_25:\n    def __init__(self, path):\n        self.path = path\n\n    def __enter__(self):\n        self.handle = open(self.path, 'r', encoding='utf-8')\n        return self.handle\n\n    def __exit__(self, exc_type, exc, tb):\n        self.handle.close()\n        return False\n",
    "output": "Inside this class file_context_25 uses a context manager to transform path and self.handle into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts path, self.handle and returns an open file handle during the with-block. One notable detail is that exceptions are not suppressed because __exit__ returns False."
  },
  {
    "input": "def safe_parse_25(text, fallback=0):\n    try:\n        return int(text)\n    except (TypeError, ValueError):\n        return fallback\n",
    "output": "The function safe_parse_25 uses exception handling with a fallback to transform text and fallback into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, fallback and returns an integer when parsing succeeds, otherwise the fallback value. One notable detail is that TypeError and ValueError both map to the fallback path."
  },
  {
    "input": "def map_filter_reduce_25(values, factor=2):\n    from functools import reduce\n    doubled = list(map(lambda value: value * factor, values))\n    positives = list(filter(lambda value: value > 0, doubled))\n    return reduce(lambda acc, value: acc + value, positives, 0)\n",
    "output": "This class map_filter_reduce_25 uses higher-order functions to transform values and factor into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, factor and returns the reduced total as a number. One notable detail is that empty inputs return zero because reduce is seeded with an initial value."
  },
  {
    "input": "class Animal_25:\n    def speak(self):\n        return 'sound'\n\nclass Dog_25(Animal_25):\n    def speak(self):\n        return 'woof'\n",
    "output": "The algorithm Animal_25 and Dog_25 uses inheritance and method overriding to transform self and Animal_25 into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts self, Animal_25 and returns a sound string from the overridden speak method. One notable detail is that the derived class returns a dog-specific sound instead of the base sound."
  },
  {
    "input": "async def async_collect_25(items, delay=0):\n    import asyncio\n    results = []\n    for item in items:\n        await asyncio.sleep(delay)\n        results.append(item)\n    return results\n",
    "output": "At its core async_collect_25 uses async iteration with await to transform items and delay into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, delay and returns the collected list after awaiting each item. One notable detail is that it yields control back to the event loop on every pass."
  },
  {
    "input": "class SingletonFactory_25:\n    _instance = None\n\n    def __new__(cls, *args, **kwargs):\n        if cls._instance is None:\n            cls._instance = super().__new__(cls)\n        return cls._instance\n",
    "output": "Under the hood SingletonFactory_25 uses the singleton pattern to transform args and kwargs into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts args, kwargs and returns the same instance every time. One notable detail is that state is shared because __new__ reuses the cached instance."
  },
  {
    "input": "def data_pipeline_25(records, minimum_age=18):\n    filtered = [record for record in records if record['age'] >= minimum_age]\n    normalized = [{**record, 'name': record['name'].strip().title()} for record in filtered]\n    return sorted(normalized, key=lambda record: record['name'])\n",
    "output": "When called with data_pipeline_25 uses a filter-transform-sort pipeline to transform records and minimum_age into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts records, minimum_age and returns a sorted list of normalized record dictionaries. One notable detail is that missing age or name keys will still raise KeyError."
  },
  {
    "input": "def module_worker_a_25(numbers):\n    return sum(numbers)\n\ndef module_worker_b_25(numbers):\n    return max(numbers) if numbers else None\n\ndef module_worker_c_25(numbers):\n    return min(numbers) if numbers else None\n",
    "output": "This utility module_worker_a_25 uses a small multi-function module to transform numbers and numbers into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts numbers, numbers and returns the computed aggregate returned by each helper. One notable detail is that empty lists cause module_worker_b and module_worker_c to return None."
  },
  {
    "input": "def gcd_calculator_26(left, right):\n    while right != 0:\n        left, right = right, left % right\n    return left\n",
    "output": "The method gcd_calculator_26 uses the Euclidean algorithm to transform left and right into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts left, right and returns the greatest common divisor as an integer. One notable detail is that it returns the final left value when right becomes zero."
  },
  {
    "input": "def reverse_text_26(text, preserve_case=False):\n    if preserve_case:\n        return text[::-1]\n    return text.strip()[::-1]\n",
    "output": "Built around reverse_text_26 uses slice-based string reversal to transform text and preserve_case into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, preserve_case and returns the reversed string. One notable detail is that it optionally strips surrounding whitespace before reversing."
  },
  {
    "input": "def dedupe_values_26(items, keep_order=True):\n    seen = set()\n    output = []\n    for item in items:\n        if item not in seen:\n            seen.add(item)\n            output.append(item)\n    return output if keep_order else sorted(set(items))\n",
    "output": "Designed to dedupe_values_26 uses order-preserving deduplication with a set to transform items and keep_order into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, keep_order and returns a list of unique values. One notable detail is that the ordered branch preserves the first appearance of each item."
  },
  {
    "input": "def factorial_recursive_26(n, depth=0):\n    if n <= 1:\n        return 1\n    return n * factorial_recursive_26(n - 1, depth + 1)\n",
    "output": "Working through factorial_recursive_26 uses recursive multiplication to transform n and depth into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts n, depth and returns the factorial of n as an integer. One notable detail is that it uses the n <= 1 base case, so zero and one both return one."
  },
  {
    "input": "class BankAccount_26:\n    def __init__(self, owner, balance=0):\n        self.owner = owner\n        self.balance = balance\n\n    def deposit(self, amount):\n        self.balance += amount\n        return self.balance\n\n    def withdraw(self, amount):\n        if amount > self.balance:\n            raise ValueError('insufficient funds')\n        self.balance -= amount\n        return self.balance\n",
    "output": "The generator BankAccount_26 uses an object-oriented balance manager to transform owner and balance into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts owner, balance and returns the updated account balance. One notable detail is that withdrawals larger than the current balance raise ValueError."
  },
  {
    "input": "def bubble_sort_26(values, descending=False):\n    for end in range(len(values) - 1, 0, -1):\n        for index in range(end):\n            if (values[index] > values[index + 1]) != descending:\n                values[index], values[index + 1] = values[index + 1], values[index]\n    return values\n",
    "output": "This decorator bubble_sort_26 uses bubble sort to transform values and descending into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, descending and returns the list sorted in place and returned. One notable detail is that it works in quadratic time and mutates the original list."
  },
  {
    "input": "def binary_search_26(values, target, offset=0):\n    left = 0\n    right = len(values) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if values[mid] == target:\n            return offset + mid\n        if values[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return -1\n",
    "output": "The recursive binary_search_26 uses binary search to transform values and target into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, target and returns the index of target or negative one. One notable detail is that the search assumes values is already sorted."
  },
  {
    "input": "def timing_decorator_26(func):\n    import functools, time\n    @functools.wraps(func)\n    def wrapper(*args, **kwargs):\n        start = time.perf_counter()\n        result = func(*args, **kwargs)\n        print(func.__name__, time.perf_counter() - start)\n        return result\n    return wrapper\n",
    "output": "Inside this class timing_decorator_26 uses a timing wrapper to transform func and args into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts func, args and returns the wrapped function result after printing elapsed time. One notable detail is that it preserves the original function metadata with functools.wraps."
  },
  {
    "input": "def chunked_26(items, size):\n    for index in range(0, len(items), size):\n        yield items[index:index + size]\n",
    "output": "The function chunked_26 uses generator-based chunking to transform items and size into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, size and returns lists of items in successive chunks. One notable detail is that the final chunk may be shorter if the length is not divisible by size."
  },
  {
    "input": "class file_context_26:\n    def __init__(self, path):\n        self.path = path\n\n    def __enter__(self):\n        self.handle = open(self.path, 'r', encoding='utf-8')\n        return self.handle\n\n    def __exit__(self, exc_type, exc, tb):\n        self.handle.close()\n        return False\n",
    "output": "This class file_context_26 uses a context manager to transform path and self.handle into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts path, self.handle and returns an open file handle during the with-block. One notable detail is that exceptions are not suppressed because __exit__ returns False."
  },
  {
    "input": "def safe_parse_26(text, fallback=0):\n    try:\n        return int(text)\n    except (TypeError, ValueError):\n        return fallback\n",
    "output": "The algorithm safe_parse_26 uses exception handling with a fallback to transform text and fallback into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, fallback and returns an integer when parsing succeeds, otherwise the fallback value. One notable detail is that TypeError and ValueError both map to the fallback path."
  },
  {
    "input": "def map_filter_reduce_26(values, factor=2):\n    from functools import reduce\n    doubled = list(map(lambda value: value * factor, values))\n    positives = list(filter(lambda value: value > 0, doubled))\n    return reduce(lambda acc, value: acc + value, positives, 0)\n",
    "output": "At its core map_filter_reduce_26 uses higher-order functions to transform values and factor into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, factor and returns the reduced total as a number. One notable detail is that empty inputs return zero because reduce is seeded with an initial value."
  },
  {
    "input": "class Animal_26:\n    def speak(self):\n        return 'sound'\n\nclass Dog_26(Animal_26):\n    def speak(self):\n        return 'woof'\n",
    "output": "Under the hood Animal_26 and Dog_26 uses inheritance and method overriding to transform self and Animal_26 into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts self, Animal_26 and returns a sound string from the overridden speak method. One notable detail is that the derived class returns a dog-specific sound instead of the base sound."
  },
  {
    "input": "async def async_collect_26(items, delay=0):\n    import asyncio\n    results = []\n    for item in items:\n        await asyncio.sleep(delay)\n        results.append(item)\n    return results\n",
    "output": "When called with async_collect_26 uses async iteration with await to transform items and delay into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, delay and returns the collected list after awaiting each item. One notable detail is that it yields control back to the event loop on every pass."
  },
  {
    "input": "class SingletonFactory_26:\n    _instance = None\n\n    def __new__(cls, *args, **kwargs):\n        if cls._instance is None:\n            cls._instance = super().__new__(cls)\n        return cls._instance\n",
    "output": "This utility SingletonFactory_26 uses the singleton pattern to transform args and kwargs into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts args, kwargs and returns the same instance every time. One notable detail is that state is shared because __new__ reuses the cached instance."
  },
  {
    "input": "def data_pipeline_26(records, minimum_age=18):\n    filtered = [record for record in records if record['age'] >= minimum_age]\n    normalized = [{**record, 'name': record['name'].strip().title()} for record in filtered]\n    return sorted(normalized, key=lambda record: record['name'])\n",
    "output": "The method data_pipeline_26 uses a filter-transform-sort pipeline to transform records and minimum_age into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts records, minimum_age and returns a sorted list of normalized record dictionaries. One notable detail is that missing age or name keys will still raise KeyError."
  },
  {
    "input": "def module_worker_a_26(numbers):\n    return sum(numbers)\n\ndef module_worker_b_26(numbers):\n    return max(numbers) if numbers else None\n\ndef module_worker_c_26(numbers):\n    return min(numbers) if numbers else None\n",
    "output": "Built around module_worker_a_26 uses a small multi-function module to transform numbers and numbers into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts numbers, numbers and returns the computed aggregate returned by each helper. One notable detail is that empty lists cause module_worker_b and module_worker_c to return None."
  },
  {
    "input": "def gcd_calculator_27(left, right):\n    while right != 0:\n        left, right = right, left % right\n    return left\n",
    "output": "Designed to gcd_calculator_27 uses the Euclidean algorithm to transform left and right into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts left, right and returns the greatest common divisor as an integer. One notable detail is that it returns the final left value when right becomes zero."
  },
  {
    "input": "def reverse_text_27(text, preserve_case=False):\n    if preserve_case:\n        return text[::-1]\n    return text.strip()[::-1]\n",
    "output": "Working through reverse_text_27 uses slice-based string reversal to transform text and preserve_case into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, preserve_case and returns the reversed string. One notable detail is that it optionally strips surrounding whitespace before reversing."
  },
  {
    "input": "def dedupe_values_27(items, keep_order=True):\n    seen = set()\n    output = []\n    for item in items:\n        if item not in seen:\n            seen.add(item)\n            output.append(item)\n    return output if keep_order else sorted(set(items))\n",
    "output": "The generator dedupe_values_27 uses order-preserving deduplication with a set to transform items and keep_order into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, keep_order and returns a list of unique values. One notable detail is that the ordered branch preserves the first appearance of each item."
  },
  {
    "input": "def factorial_recursive_27(n, depth=0):\n    if n <= 1:\n        return 1\n    return n * factorial_recursive_27(n - 1, depth + 1)\n",
    "output": "This decorator factorial_recursive_27 uses recursive multiplication to transform n and depth into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts n, depth and returns the factorial of n as an integer. One notable detail is that it uses the n <= 1 base case, so zero and one both return one."
  },
  {
    "input": "class BankAccount_27:\n    def __init__(self, owner, balance=0):\n        self.owner = owner\n        self.balance = balance\n\n    def deposit(self, amount):\n        self.balance += amount\n        return self.balance\n\n    def withdraw(self, amount):\n        if amount > self.balance:\n            raise ValueError('insufficient funds')\n        self.balance -= amount\n        return self.balance\n",
    "output": "The recursive BankAccount_27 uses an object-oriented balance manager to transform owner and balance into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts owner, balance and returns the updated account balance. One notable detail is that withdrawals larger than the current balance raise ValueError."
  },
  {
    "input": "def bubble_sort_27(values, descending=False):\n    for end in range(len(values) - 1, 0, -1):\n        for index in range(end):\n            if (values[index] > values[index + 1]) != descending:\n                values[index], values[index + 1] = values[index + 1], values[index]\n    return values\n",
    "output": "Inside this class bubble_sort_27 uses bubble sort to transform values and descending into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, descending and returns the list sorted in place and returned. One notable detail is that it works in quadratic time and mutates the original list."
  },
  {
    "input": "def binary_search_27(values, target, offset=0):\n    left = 0\n    right = len(values) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if values[mid] == target:\n            return offset + mid\n        if values[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return -1\n",
    "output": "The function binary_search_27 uses binary search to transform values and target into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, target and returns the index of target or negative one. One notable detail is that the search assumes values is already sorted."
  },
  {
    "input": "def timing_decorator_27(func):\n    import functools, time\n    @functools.wraps(func)\n    def wrapper(*args, **kwargs):\n        start = time.perf_counter()\n        result = func(*args, **kwargs)\n        print(func.__name__, time.perf_counter() - start)\n        return result\n    return wrapper\n",
    "output": "This class timing_decorator_27 uses a timing wrapper to transform func and args into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts func, args and returns the wrapped function result after printing elapsed time. One notable detail is that it preserves the original function metadata with functools.wraps."
  },
  {
    "input": "def chunked_27(items, size):\n    for index in range(0, len(items), size):\n        yield items[index:index + size]\n",
    "output": "The algorithm chunked_27 uses generator-based chunking to transform items and size into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, size and returns lists of items in successive chunks. One notable detail is that the final chunk may be shorter if the length is not divisible by size."
  },
  {
    "input": "class file_context_27:\n    def __init__(self, path):\n        self.path = path\n\n    def __enter__(self):\n        self.handle = open(self.path, 'r', encoding='utf-8')\n        return self.handle\n\n    def __exit__(self, exc_type, exc, tb):\n        self.handle.close()\n        return False\n",
    "output": "At its core file_context_27 uses a context manager to transform path and self.handle into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts path, self.handle and returns an open file handle during the with-block. One notable detail is that exceptions are not suppressed because __exit__ returns False."
  },
  {
    "input": "def safe_parse_27(text, fallback=0):\n    try:\n        return int(text)\n    except (TypeError, ValueError):\n        return fallback\n",
    "output": "Under the hood safe_parse_27 uses exception handling with a fallback to transform text and fallback into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, fallback and returns an integer when parsing succeeds, otherwise the fallback value. One notable detail is that TypeError and ValueError both map to the fallback path."
  },
  {
    "input": "def map_filter_reduce_27(values, factor=2):\n    from functools import reduce\n    doubled = list(map(lambda value: value * factor, values))\n    positives = list(filter(lambda value: value > 0, doubled))\n    return reduce(lambda acc, value: acc + value, positives, 0)\n",
    "output": "When called with map_filter_reduce_27 uses higher-order functions to transform values and factor into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, factor and returns the reduced total as a number. One notable detail is that empty inputs return zero because reduce is seeded with an initial value."
  },
  {
    "input": "class Animal_27:\n    def speak(self):\n        return 'sound'\n\nclass Dog_27(Animal_27):\n    def speak(self):\n        return 'woof'\n",
    "output": "This utility Animal_27 and Dog_27 uses inheritance and method overriding to transform self and Animal_27 into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts self, Animal_27 and returns a sound string from the overridden speak method. One notable detail is that the derived class returns a dog-specific sound instead of the base sound."
  },
  {
    "input": "async def async_collect_27(items, delay=0):\n    import asyncio\n    results = []\n    for item in items:\n        await asyncio.sleep(delay)\n        results.append(item)\n    return results\n",
    "output": "The method async_collect_27 uses async iteration with await to transform items and delay into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, delay and returns the collected list after awaiting each item. One notable detail is that it yields control back to the event loop on every pass."
  },
  {
    "input": "class SingletonFactory_27:\n    _instance = None\n\n    def __new__(cls, *args, **kwargs):\n        if cls._instance is None:\n            cls._instance = super().__new__(cls)\n        return cls._instance\n",
    "output": "Built around SingletonFactory_27 uses the singleton pattern to transform args and kwargs into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts args, kwargs and returns the same instance every time. One notable detail is that state is shared because __new__ reuses the cached instance."
  },
  {
    "input": "def data_pipeline_27(records, minimum_age=18):\n    filtered = [record for record in records if record['age'] >= minimum_age]\n    normalized = [{**record, 'name': record['name'].strip().title()} for record in filtered]\n    return sorted(normalized, key=lambda record: record['name'])\n",
    "output": "Designed to data_pipeline_27 uses a filter-transform-sort pipeline to transform records and minimum_age into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts records, minimum_age and returns a sorted list of normalized record dictionaries. One notable detail is that missing age or name keys will still raise KeyError."
  },
  {
    "input": "def module_worker_a_27(numbers):\n    return sum(numbers)\n\ndef module_worker_b_27(numbers):\n    return max(numbers) if numbers else None\n\ndef module_worker_c_27(numbers):\n    return min(numbers) if numbers else None\n",
    "output": "Working through module_worker_a_27 uses a small multi-function module to transform numbers and numbers into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts numbers, numbers and returns the computed aggregate returned by each helper. One notable detail is that empty lists cause module_worker_b and module_worker_c to return None."
  },
  {
    "input": "def gcd_calculator_28(left, right):\n    while right != 0:\n        left, right = right, left % right\n    return left\n",
    "output": "The generator gcd_calculator_28 uses the Euclidean algorithm to transform left and right into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts left, right and returns the greatest common divisor as an integer. One notable detail is that it returns the final left value when right becomes zero."
  },
  {
    "input": "def reverse_text_28(text, preserve_case=False):\n    if preserve_case:\n        return text[::-1]\n    return text.strip()[::-1]\n",
    "output": "This decorator reverse_text_28 uses slice-based string reversal to transform text and preserve_case into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, preserve_case and returns the reversed string. One notable detail is that it optionally strips surrounding whitespace before reversing."
  },
  {
    "input": "def dedupe_values_28(items, keep_order=True):\n    seen = set()\n    output = []\n    for item in items:\n        if item not in seen:\n            seen.add(item)\n            output.append(item)\n    return output if keep_order else sorted(set(items))\n",
    "output": "The recursive dedupe_values_28 uses order-preserving deduplication with a set to transform items and keep_order into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, keep_order and returns a list of unique values. One notable detail is that the ordered branch preserves the first appearance of each item."
  },
  {
    "input": "def factorial_recursive_28(n, depth=0):\n    if n <= 1:\n        return 1\n    return n * factorial_recursive_28(n - 1, depth + 1)\n",
    "output": "Inside this class factorial_recursive_28 uses recursive multiplication to transform n and depth into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts n, depth and returns the factorial of n as an integer. One notable detail is that it uses the n <= 1 base case, so zero and one both return one."
  },
  {
    "input": "class BankAccount_28:\n    def __init__(self, owner, balance=0):\n        self.owner = owner\n        self.balance = balance\n\n    def deposit(self, amount):\n        self.balance += amount\n        return self.balance\n\n    def withdraw(self, amount):\n        if amount > self.balance:\n            raise ValueError('insufficient funds')\n        self.balance -= amount\n        return self.balance\n",
    "output": "The function BankAccount_28 uses an object-oriented balance manager to transform owner and balance into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts owner, balance and returns the updated account balance. One notable detail is that withdrawals larger than the current balance raise ValueError."
  },
  {
    "input": "def bubble_sort_28(values, descending=False):\n    for end in range(len(values) - 1, 0, -1):\n        for index in range(end):\n            if (values[index] > values[index + 1]) != descending:\n                values[index], values[index + 1] = values[index + 1], values[index]\n    return values\n",
    "output": "This class bubble_sort_28 uses bubble sort to transform values and descending into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, descending and returns the list sorted in place and returned. One notable detail is that it works in quadratic time and mutates the original list."
  },
  {
    "input": "def binary_search_28(values, target, offset=0):\n    left = 0\n    right = len(values) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if values[mid] == target:\n            return offset + mid\n        if values[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return -1\n",
    "output": "The algorithm binary_search_28 uses binary search to transform values and target into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, target and returns the index of target or negative one. One notable detail is that the search assumes values is already sorted."
  },
  {
    "input": "def timing_decorator_28(func):\n    import functools, time\n    @functools.wraps(func)\n    def wrapper(*args, **kwargs):\n        start = time.perf_counter()\n        result = func(*args, **kwargs)\n        print(func.__name__, time.perf_counter() - start)\n        return result\n    return wrapper\n",
    "output": "At its core timing_decorator_28 uses a timing wrapper to transform func and args into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts func, args and returns the wrapped function result after printing elapsed time. One notable detail is that it preserves the original function metadata with functools.wraps."
  },
  {
    "input": "def chunked_28(items, size):\n    for index in range(0, len(items), size):\n        yield items[index:index + size]\n",
    "output": "Under the hood chunked_28 uses generator-based chunking to transform items and size into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, size and returns lists of items in successive chunks. One notable detail is that the final chunk may be shorter if the length is not divisible by size."
  },
  {
    "input": "class file_context_28:\n    def __init__(self, path):\n        self.path = path\n\n    def __enter__(self):\n        self.handle = open(self.path, 'r', encoding='utf-8')\n        return self.handle\n\n    def __exit__(self, exc_type, exc, tb):\n        self.handle.close()\n        return False\n",
    "output": "When called with file_context_28 uses a context manager to transform path and self.handle into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts path, self.handle and returns an open file handle during the with-block. One notable detail is that exceptions are not suppressed because __exit__ returns False."
  },
  {
    "input": "def safe_parse_28(text, fallback=0):\n    try:\n        return int(text)\n    except (TypeError, ValueError):\n        return fallback\n",
    "output": "This utility safe_parse_28 uses exception handling with a fallback to transform text and fallback into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, fallback and returns an integer when parsing succeeds, otherwise the fallback value. One notable detail is that TypeError and ValueError both map to the fallback path."
  },
  {
    "input": "def map_filter_reduce_28(values, factor=2):\n    from functools import reduce\n    doubled = list(map(lambda value: value * factor, values))\n    positives = list(filter(lambda value: value > 0, doubled))\n    return reduce(lambda acc, value: acc + value, positives, 0)\n",
    "output": "The method map_filter_reduce_28 uses higher-order functions to transform values and factor into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, factor and returns the reduced total as a number. One notable detail is that empty inputs return zero because reduce is seeded with an initial value."
  },
  {
    "input": "class Animal_28:\n    def speak(self):\n        return 'sound'\n\nclass Dog_28(Animal_28):\n    def speak(self):\n        return 'woof'\n",
    "output": "Built around Animal_28 and Dog_28 uses inheritance and method overriding to transform self and Animal_28 into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts self, Animal_28 and returns a sound string from the overridden speak method. One notable detail is that the derived class returns a dog-specific sound instead of the base sound."
  },
  {
    "input": "async def async_collect_28(items, delay=0):\n    import asyncio\n    results = []\n    for item in items:\n        await asyncio.sleep(delay)\n        results.append(item)\n    return results\n",
    "output": "Designed to async_collect_28 uses async iteration with await to transform items and delay into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, delay and returns the collected list after awaiting each item. One notable detail is that it yields control back to the event loop on every pass."
  },
  {
    "input": "class SingletonFactory_28:\n    _instance = None\n\n    def __new__(cls, *args, **kwargs):\n        if cls._instance is None:\n            cls._instance = super().__new__(cls)\n        return cls._instance\n",
    "output": "Working through SingletonFactory_28 uses the singleton pattern to transform args and kwargs into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts args, kwargs and returns the same instance every time. One notable detail is that state is shared because __new__ reuses the cached instance."
  },
  {
    "input": "def data_pipeline_28(records, minimum_age=18):\n    filtered = [record for record in records if record['age'] >= minimum_age]\n    normalized = [{**record, 'name': record['name'].strip().title()} for record in filtered]\n    return sorted(normalized, key=lambda record: record['name'])\n",
    "output": "The generator data_pipeline_28 uses a filter-transform-sort pipeline to transform records and minimum_age into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts records, minimum_age and returns a sorted list of normalized record dictionaries. One notable detail is that missing age or name keys will still raise KeyError."
  },
  {
    "input": "def module_worker_a_28(numbers):\n    return sum(numbers)\n\ndef module_worker_b_28(numbers):\n    return max(numbers) if numbers else None\n\ndef module_worker_c_28(numbers):\n    return min(numbers) if numbers else None\n",
    "output": "This decorator module_worker_a_28 uses a small multi-function module to transform numbers and numbers into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts numbers, numbers and returns the computed aggregate returned by each helper. One notable detail is that empty lists cause module_worker_b and module_worker_c to return None."
  },
  {
    "input": "def gcd_calculator_29(left, right):\n    while right != 0:\n        left, right = right, left % right\n    return left\n",
    "output": "The recursive gcd_calculator_29 uses the Euclidean algorithm to transform left and right into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts left, right and returns the greatest common divisor as an integer. One notable detail is that it returns the final left value when right becomes zero."
  },
  {
    "input": "def reverse_text_29(text, preserve_case=False):\n    if preserve_case:\n        return text[::-1]\n    return text.strip()[::-1]\n",
    "output": "Inside this class reverse_text_29 uses slice-based string reversal to transform text and preserve_case into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, preserve_case and returns the reversed string. One notable detail is that it optionally strips surrounding whitespace before reversing."
  },
  {
    "input": "def dedupe_values_29(items, keep_order=True):\n    seen = set()\n    output = []\n    for item in items:\n        if item not in seen:\n            seen.add(item)\n            output.append(item)\n    return output if keep_order else sorted(set(items))\n",
    "output": "The function dedupe_values_29 uses order-preserving deduplication with a set to transform items and keep_order into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, keep_order and returns a list of unique values. One notable detail is that the ordered branch preserves the first appearance of each item."
  },
  {
    "input": "def factorial_recursive_29(n, depth=0):\n    if n <= 1:\n        return 1\n    return n * factorial_recursive_29(n - 1, depth + 1)\n",
    "output": "This class factorial_recursive_29 uses recursive multiplication to transform n and depth into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts n, depth and returns the factorial of n as an integer. One notable detail is that it uses the n <= 1 base case, so zero and one both return one."
  },
  {
    "input": "class BankAccount_29:\n    def __init__(self, owner, balance=0):\n        self.owner = owner\n        self.balance = balance\n\n    def deposit(self, amount):\n        self.balance += amount\n        return self.balance\n\n    def withdraw(self, amount):\n        if amount > self.balance:\n            raise ValueError('insufficient funds')\n        self.balance -= amount\n        return self.balance\n",
    "output": "The algorithm BankAccount_29 uses an object-oriented balance manager to transform owner and balance into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts owner, balance and returns the updated account balance. One notable detail is that withdrawals larger than the current balance raise ValueError."
  },
  {
    "input": "def bubble_sort_29(values, descending=False):\n    for end in range(len(values) - 1, 0, -1):\n        for index in range(end):\n            if (values[index] > values[index + 1]) != descending:\n                values[index], values[index + 1] = values[index + 1], values[index]\n    return values\n",
    "output": "At its core bubble_sort_29 uses bubble sort to transform values and descending into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, descending and returns the list sorted in place and returned. One notable detail is that it works in quadratic time and mutates the original list."
  },
  {
    "input": "def binary_search_29(values, target, offset=0):\n    left = 0\n    right = len(values) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if values[mid] == target:\n            return offset + mid\n        if values[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return -1\n",
    "output": "Under the hood binary_search_29 uses binary search to transform values and target into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, target and returns the index of target or negative one. One notable detail is that the search assumes values is already sorted."
  },
  {
    "input": "def timing_decorator_29(func):\n    import functools, time\n    @functools.wraps(func)\n    def wrapper(*args, **kwargs):\n        start = time.perf_counter()\n        result = func(*args, **kwargs)\n        print(func.__name__, time.perf_counter() - start)\n        return result\n    return wrapper\n",
    "output": "When called with timing_decorator_29 uses a timing wrapper to transform func and args into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts func, args and returns the wrapped function result after printing elapsed time. One notable detail is that it preserves the original function metadata with functools.wraps."
  },
  {
    "input": "def chunked_29(items, size):\n    for index in range(0, len(items), size):\n        yield items[index:index + size]\n",
    "output": "This utility chunked_29 uses generator-based chunking to transform items and size into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, size and returns lists of items in successive chunks. One notable detail is that the final chunk may be shorter if the length is not divisible by size."
  },
  {
    "input": "class file_context_29:\n    def __init__(self, path):\n        self.path = path\n\n    def __enter__(self):\n        self.handle = open(self.path, 'r', encoding='utf-8')\n        return self.handle\n\n    def __exit__(self, exc_type, exc, tb):\n        self.handle.close()\n        return False\n",
    "output": "The method file_context_29 uses a context manager to transform path and self.handle into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts path, self.handle and returns an open file handle during the with-block. One notable detail is that exceptions are not suppressed because __exit__ returns False."
  },
  {
    "input": "def safe_parse_29(text, fallback=0):\n    try:\n        return int(text)\n    except (TypeError, ValueError):\n        return fallback\n",
    "output": "Built around safe_parse_29 uses exception handling with a fallback to transform text and fallback into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, fallback and returns an integer when parsing succeeds, otherwise the fallback value. One notable detail is that TypeError and ValueError both map to the fallback path."
  },
  {
    "input": "def map_filter_reduce_29(values, factor=2):\n    from functools import reduce\n    doubled = list(map(lambda value: value * factor, values))\n    positives = list(filter(lambda value: value > 0, doubled))\n    return reduce(lambda acc, value: acc + value, positives, 0)\n",
    "output": "Designed to map_filter_reduce_29 uses higher-order functions to transform values and factor into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, factor and returns the reduced total as a number. One notable detail is that empty inputs return zero because reduce is seeded with an initial value."
  },
  {
    "input": "class Animal_29:\n    def speak(self):\n        return 'sound'\n\nclass Dog_29(Animal_29):\n    def speak(self):\n        return 'woof'\n",
    "output": "Working through Animal_29 and Dog_29 uses inheritance and method overriding to transform self and Animal_29 into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts self, Animal_29 and returns a sound string from the overridden speak method. One notable detail is that the derived class returns a dog-specific sound instead of the base sound."
  },
  {
    "input": "async def async_collect_29(items, delay=0):\n    import asyncio\n    results = []\n    for item in items:\n        await asyncio.sleep(delay)\n        results.append(item)\n    return results\n",
    "output": "The generator async_collect_29 uses async iteration with await to transform items and delay into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, delay and returns the collected list after awaiting each item. One notable detail is that it yields control back to the event loop on every pass."
  },
  {
    "input": "class SingletonFactory_29:\n    _instance = None\n\n    def __new__(cls, *args, **kwargs):\n        if cls._instance is None:\n            cls._instance = super().__new__(cls)\n        return cls._instance\n",
    "output": "This decorator SingletonFactory_29 uses the singleton pattern to transform args and kwargs into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts args, kwargs and returns the same instance every time. One notable detail is that state is shared because __new__ reuses the cached instance."
  },
  {
    "input": "def data_pipeline_29(records, minimum_age=18):\n    filtered = [record for record in records if record['age'] >= minimum_age]\n    normalized = [{**record, 'name': record['name'].strip().title()} for record in filtered]\n    return sorted(normalized, key=lambda record: record['name'])\n",
    "output": "The recursive data_pipeline_29 uses a filter-transform-sort pipeline to transform records and minimum_age into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts records, minimum_age and returns a sorted list of normalized record dictionaries. One notable detail is that missing age or name keys will still raise KeyError."
  },
  {
    "input": "def module_worker_a_29(numbers):\n    return sum(numbers)\n\ndef module_worker_b_29(numbers):\n    return max(numbers) if numbers else None\n\ndef module_worker_c_29(numbers):\n    return min(numbers) if numbers else None\n",
    "output": "Inside this class module_worker_a_29 uses a small multi-function module to transform numbers and numbers into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts numbers, numbers and returns the computed aggregate returned by each helper. One notable detail is that empty lists cause module_worker_b and module_worker_c to return None."
  },
  {
    "input": "def gcd_calculator_30(left, right):\n    while right != 0:\n        left, right = right, left % right\n    return left\n",
    "output": "The function gcd_calculator_30 uses the Euclidean algorithm to transform left and right into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts left, right and returns the greatest common divisor as an integer. One notable detail is that it returns the final left value when right becomes zero."
  },
  {
    "input": "def reverse_text_30(text, preserve_case=False):\n    if preserve_case:\n        return text[::-1]\n    return text.strip()[::-1]\n",
    "output": "This class reverse_text_30 uses slice-based string reversal to transform text and preserve_case into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, preserve_case and returns the reversed string. One notable detail is that it optionally strips surrounding whitespace before reversing."
  },
  {
    "input": "def dedupe_values_30(items, keep_order=True):\n    seen = set()\n    output = []\n    for item in items:\n        if item not in seen:\n            seen.add(item)\n            output.append(item)\n    return output if keep_order else sorted(set(items))\n",
    "output": "The algorithm dedupe_values_30 uses order-preserving deduplication with a set to transform items and keep_order into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, keep_order and returns a list of unique values. One notable detail is that the ordered branch preserves the first appearance of each item."
  },
  {
    "input": "def factorial_recursive_30(n, depth=0):\n    if n <= 1:\n        return 1\n    return n * factorial_recursive_30(n - 1, depth + 1)\n",
    "output": "At its core factorial_recursive_30 uses recursive multiplication to transform n and depth into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts n, depth and returns the factorial of n as an integer. One notable detail is that it uses the n <= 1 base case, so zero and one both return one."
  },
  {
    "input": "class BankAccount_30:\n    def __init__(self, owner, balance=0):\n        self.owner = owner\n        self.balance = balance\n\n    def deposit(self, amount):\n        self.balance += amount\n        return self.balance\n\n    def withdraw(self, amount):\n        if amount > self.balance:\n            raise ValueError('insufficient funds')\n        self.balance -= amount\n        return self.balance\n",
    "output": "Under the hood BankAccount_30 uses an object-oriented balance manager to transform owner and balance into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts owner, balance and returns the updated account balance. One notable detail is that withdrawals larger than the current balance raise ValueError."
  },
  {
    "input": "def bubble_sort_30(values, descending=False):\n    for end in range(len(values) - 1, 0, -1):\n        for index in range(end):\n            if (values[index] > values[index + 1]) != descending:\n                values[index], values[index + 1] = values[index + 1], values[index]\n    return values\n",
    "output": "When called with bubble_sort_30 uses bubble sort to transform values and descending into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, descending and returns the list sorted in place and returned. One notable detail is that it works in quadratic time and mutates the original list."
  },
  {
    "input": "def binary_search_30(values, target, offset=0):\n    left = 0\n    right = len(values) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if values[mid] == target:\n            return offset + mid\n        if values[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return -1\n",
    "output": "This utility binary_search_30 uses binary search to transform values and target into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, target and returns the index of target or negative one. One notable detail is that the search assumes values is already sorted."
  },
  {
    "input": "def timing_decorator_30(func):\n    import functools, time\n    @functools.wraps(func)\n    def wrapper(*args, **kwargs):\n        start = time.perf_counter()\n        result = func(*args, **kwargs)\n        print(func.__name__, time.perf_counter() - start)\n        return result\n    return wrapper\n",
    "output": "The method timing_decorator_30 uses a timing wrapper to transform func and args into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts func, args and returns the wrapped function result after printing elapsed time. One notable detail is that it preserves the original function metadata with functools.wraps."
  },
  {
    "input": "def chunked_30(items, size):\n    for index in range(0, len(items), size):\n        yield items[index:index + size]\n",
    "output": "Built around chunked_30 uses generator-based chunking to transform items and size into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, size and returns lists of items in successive chunks. One notable detail is that the final chunk may be shorter if the length is not divisible by size."
  },
  {
    "input": "class file_context_30:\n    def __init__(self, path):\n        self.path = path\n\n    def __enter__(self):\n        self.handle = open(self.path, 'r', encoding='utf-8')\n        return self.handle\n\n    def __exit__(self, exc_type, exc, tb):\n        self.handle.close()\n        return False\n",
    "output": "Designed to file_context_30 uses a context manager to transform path and self.handle into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts path, self.handle and returns an open file handle during the with-block. One notable detail is that exceptions are not suppressed because __exit__ returns False."
  },
  {
    "input": "def safe_parse_30(text, fallback=0):\n    try:\n        return int(text)\n    except (TypeError, ValueError):\n        return fallback\n",
    "output": "Working through safe_parse_30 uses exception handling with a fallback to transform text and fallback into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, fallback and returns an integer when parsing succeeds, otherwise the fallback value. One notable detail is that TypeError and ValueError both map to the fallback path."
  },
  {
    "input": "def map_filter_reduce_30(values, factor=2):\n    from functools import reduce\n    doubled = list(map(lambda value: value * factor, values))\n    positives = list(filter(lambda value: value > 0, doubled))\n    return reduce(lambda acc, value: acc + value, positives, 0)\n",
    "output": "The generator map_filter_reduce_30 uses higher-order functions to transform values and factor into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, factor and returns the reduced total as a number. One notable detail is that empty inputs return zero because reduce is seeded with an initial value."
  },
  {
    "input": "class Animal_30:\n    def speak(self):\n        return 'sound'\n\nclass Dog_30(Animal_30):\n    def speak(self):\n        return 'woof'\n",
    "output": "This decorator Animal_30 and Dog_30 uses inheritance and method overriding to transform self and Animal_30 into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts self, Animal_30 and returns a sound string from the overridden speak method. One notable detail is that the derived class returns a dog-specific sound instead of the base sound."
  },
  {
    "input": "async def async_collect_30(items, delay=0):\n    import asyncio\n    results = []\n    for item in items:\n        await asyncio.sleep(delay)\n        results.append(item)\n    return results\n",
    "output": "The recursive async_collect_30 uses async iteration with await to transform items and delay into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, delay and returns the collected list after awaiting each item. One notable detail is that it yields control back to the event loop on every pass."
  },
  {
    "input": "class SingletonFactory_30:\n    _instance = None\n\n    def __new__(cls, *args, **kwargs):\n        if cls._instance is None:\n            cls._instance = super().__new__(cls)\n        return cls._instance\n",
    "output": "Inside this class SingletonFactory_30 uses the singleton pattern to transform args and kwargs into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts args, kwargs and returns the same instance every time. One notable detail is that state is shared because __new__ reuses the cached instance."
  },
  {
    "input": "def data_pipeline_30(records, minimum_age=18):\n    filtered = [record for record in records if record['age'] >= minimum_age]\n    normalized = [{**record, 'name': record['name'].strip().title()} for record in filtered]\n    return sorted(normalized, key=lambda record: record['name'])\n",
    "output": "The function data_pipeline_30 uses a filter-transform-sort pipeline to transform records and minimum_age into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts records, minimum_age and returns a sorted list of normalized record dictionaries. One notable detail is that missing age or name keys will still raise KeyError."
  },
  {
    "input": "def module_worker_a_30(numbers):\n    return sum(numbers)\n\ndef module_worker_b_30(numbers):\n    return max(numbers) if numbers else None\n\ndef module_worker_c_30(numbers):\n    return min(numbers) if numbers else None\n",
    "output": "This class module_worker_a_30 uses a small multi-function module to transform numbers and numbers into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts numbers, numbers and returns the computed aggregate returned by each helper. One notable detail is that empty lists cause module_worker_b and module_worker_c to return None."
  },
  {
    "input": "def gcd_calculator_31(left, right):\n    while right != 0:\n        left, right = right, left % right\n    return left\n",
    "output": "The algorithm gcd_calculator_31 uses the Euclidean algorithm to transform left and right into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts left, right and returns the greatest common divisor as an integer. One notable detail is that it returns the final left value when right becomes zero."
  },
  {
    "input": "def reverse_text_31(text, preserve_case=False):\n    if preserve_case:\n        return text[::-1]\n    return text.strip()[::-1]\n",
    "output": "At its core reverse_text_31 uses slice-based string reversal to transform text and preserve_case into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, preserve_case and returns the reversed string. One notable detail is that it optionally strips surrounding whitespace before reversing."
  },
  {
    "input": "def dedupe_values_31(items, keep_order=True):\n    seen = set()\n    output = []\n    for item in items:\n        if item not in seen:\n            seen.add(item)\n            output.append(item)\n    return output if keep_order else sorted(set(items))\n",
    "output": "Under the hood dedupe_values_31 uses order-preserving deduplication with a set to transform items and keep_order into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, keep_order and returns a list of unique values. One notable detail is that the ordered branch preserves the first appearance of each item."
  },
  {
    "input": "def factorial_recursive_31(n, depth=0):\n    if n <= 1:\n        return 1\n    return n * factorial_recursive_31(n - 1, depth + 1)\n",
    "output": "When called with factorial_recursive_31 uses recursive multiplication to transform n and depth into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts n, depth and returns the factorial of n as an integer. One notable detail is that it uses the n <= 1 base case, so zero and one both return one."
  },
  {
    "input": "class BankAccount_31:\n    def __init__(self, owner, balance=0):\n        self.owner = owner\n        self.balance = balance\n\n    def deposit(self, amount):\n        self.balance += amount\n        return self.balance\n\n    def withdraw(self, amount):\n        if amount > self.balance:\n            raise ValueError('insufficient funds')\n        self.balance -= amount\n        return self.balance\n",
    "output": "This utility BankAccount_31 uses an object-oriented balance manager to transform owner and balance into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts owner, balance and returns the updated account balance. One notable detail is that withdrawals larger than the current balance raise ValueError."
  },
  {
    "input": "def bubble_sort_31(values, descending=False):\n    for end in range(len(values) - 1, 0, -1):\n        for index in range(end):\n            if (values[index] > values[index + 1]) != descending:\n                values[index], values[index + 1] = values[index + 1], values[index]\n    return values\n",
    "output": "The method bubble_sort_31 uses bubble sort to transform values and descending into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, descending and returns the list sorted in place and returned. One notable detail is that it works in quadratic time and mutates the original list."
  },
  {
    "input": "def binary_search_31(values, target, offset=0):\n    left = 0\n    right = len(values) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if values[mid] == target:\n            return offset + mid\n        if values[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return -1\n",
    "output": "Built around binary_search_31 uses binary search to transform values and target into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, target and returns the index of target or negative one. One notable detail is that the search assumes values is already sorted."
  },
  {
    "input": "def timing_decorator_31(func):\n    import functools, time\n    @functools.wraps(func)\n    def wrapper(*args, **kwargs):\n        start = time.perf_counter()\n        result = func(*args, **kwargs)\n        print(func.__name__, time.perf_counter() - start)\n        return result\n    return wrapper\n",
    "output": "Designed to timing_decorator_31 uses a timing wrapper to transform func and args into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts func, args and returns the wrapped function result after printing elapsed time. One notable detail is that it preserves the original function metadata with functools.wraps."
  },
  {
    "input": "def chunked_31(items, size):\n    for index in range(0, len(items), size):\n        yield items[index:index + size]\n",
    "output": "Working through chunked_31 uses generator-based chunking to transform items and size into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, size and returns lists of items in successive chunks. One notable detail is that the final chunk may be shorter if the length is not divisible by size."
  },
  {
    "input": "class file_context_31:\n    def __init__(self, path):\n        self.path = path\n\n    def __enter__(self):\n        self.handle = open(self.path, 'r', encoding='utf-8')\n        return self.handle\n\n    def __exit__(self, exc_type, exc, tb):\n        self.handle.close()\n        return False\n",
    "output": "The generator file_context_31 uses a context manager to transform path and self.handle into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts path, self.handle and returns an open file handle during the with-block. One notable detail is that exceptions are not suppressed because __exit__ returns False."
  },
  {
    "input": "def safe_parse_31(text, fallback=0):\n    try:\n        return int(text)\n    except (TypeError, ValueError):\n        return fallback\n",
    "output": "This decorator safe_parse_31 uses exception handling with a fallback to transform text and fallback into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, fallback and returns an integer when parsing succeeds, otherwise the fallback value. One notable detail is that TypeError and ValueError both map to the fallback path."
  },
  {
    "input": "def map_filter_reduce_31(values, factor=2):\n    from functools import reduce\n    doubled = list(map(lambda value: value * factor, values))\n    positives = list(filter(lambda value: value > 0, doubled))\n    return reduce(lambda acc, value: acc + value, positives, 0)\n",
    "output": "The recursive map_filter_reduce_31 uses higher-order functions to transform values and factor into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, factor and returns the reduced total as a number. One notable detail is that empty inputs return zero because reduce is seeded with an initial value."
  },
  {
    "input": "class Animal_31:\n    def speak(self):\n        return 'sound'\n\nclass Dog_31(Animal_31):\n    def speak(self):\n        return 'woof'\n",
    "output": "Inside this class Animal_31 and Dog_31 uses inheritance and method overriding to transform self and Animal_31 into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts self, Animal_31 and returns a sound string from the overridden speak method. One notable detail is that the derived class returns a dog-specific sound instead of the base sound."
  },
  {
    "input": "async def async_collect_31(items, delay=0):\n    import asyncio\n    results = []\n    for item in items:\n        await asyncio.sleep(delay)\n        results.append(item)\n    return results\n",
    "output": "The function async_collect_31 uses async iteration with await to transform items and delay into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, delay and returns the collected list after awaiting each item. One notable detail is that it yields control back to the event loop on every pass."
  },
  {
    "input": "class SingletonFactory_31:\n    _instance = None\n\n    def __new__(cls, *args, **kwargs):\n        if cls._instance is None:\n            cls._instance = super().__new__(cls)\n        return cls._instance\n",
    "output": "This class SingletonFactory_31 uses the singleton pattern to transform args and kwargs into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts args, kwargs and returns the same instance every time. One notable detail is that state is shared because __new__ reuses the cached instance."
  },
  {
    "input": "def data_pipeline_31(records, minimum_age=18):\n    filtered = [record for record in records if record['age'] >= minimum_age]\n    normalized = [{**record, 'name': record['name'].strip().title()} for record in filtered]\n    return sorted(normalized, key=lambda record: record['name'])\n",
    "output": "The algorithm data_pipeline_31 uses a filter-transform-sort pipeline to transform records and minimum_age into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts records, minimum_age and returns a sorted list of normalized record dictionaries. One notable detail is that missing age or name keys will still raise KeyError."
  },
  {
    "input": "def module_worker_a_31(numbers):\n    return sum(numbers)\n\ndef module_worker_b_31(numbers):\n    return max(numbers) if numbers else None\n\ndef module_worker_c_31(numbers):\n    return min(numbers) if numbers else None\n",
    "output": "At its core module_worker_a_31 uses a small multi-function module to transform numbers and numbers into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts numbers, numbers and returns the computed aggregate returned by each helper. One notable detail is that empty lists cause module_worker_b and module_worker_c to return None."
  },
  {
    "input": "def gcd_calculator_32(left, right):\n    while right != 0:\n        left, right = right, left % right\n    return left\n",
    "output": "Under the hood gcd_calculator_32 uses the Euclidean algorithm to transform left and right into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts left, right and returns the greatest common divisor as an integer. One notable detail is that it returns the final left value when right becomes zero."
  },
  {
    "input": "def reverse_text_32(text, preserve_case=False):\n    if preserve_case:\n        return text[::-1]\n    return text.strip()[::-1]\n",
    "output": "When called with reverse_text_32 uses slice-based string reversal to transform text and preserve_case into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, preserve_case and returns the reversed string. One notable detail is that it optionally strips surrounding whitespace before reversing."
  },
  {
    "input": "def dedupe_values_32(items, keep_order=True):\n    seen = set()\n    output = []\n    for item in items:\n        if item not in seen:\n            seen.add(item)\n            output.append(item)\n    return output if keep_order else sorted(set(items))\n",
    "output": "This utility dedupe_values_32 uses order-preserving deduplication with a set to transform items and keep_order into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, keep_order and returns a list of unique values. One notable detail is that the ordered branch preserves the first appearance of each item."
  },
  {
    "input": "def factorial_recursive_32(n, depth=0):\n    if n <= 1:\n        return 1\n    return n * factorial_recursive_32(n - 1, depth + 1)\n",
    "output": "The method factorial_recursive_32 uses recursive multiplication to transform n and depth into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts n, depth and returns the factorial of n as an integer. One notable detail is that it uses the n <= 1 base case, so zero and one both return one."
  },
  {
    "input": "class BankAccount_32:\n    def __init__(self, owner, balance=0):\n        self.owner = owner\n        self.balance = balance\n\n    def deposit(self, amount):\n        self.balance += amount\n        return self.balance\n\n    def withdraw(self, amount):\n        if amount > self.balance:\n            raise ValueError('insufficient funds')\n        self.balance -= amount\n        return self.balance\n",
    "output": "Built around BankAccount_32 uses an object-oriented balance manager to transform owner and balance into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts owner, balance and returns the updated account balance. One notable detail is that withdrawals larger than the current balance raise ValueError."
  },
  {
    "input": "def bubble_sort_32(values, descending=False):\n    for end in range(len(values) - 1, 0, -1):\n        for index in range(end):\n            if (values[index] > values[index + 1]) != descending:\n                values[index], values[index + 1] = values[index + 1], values[index]\n    return values\n",
    "output": "Designed to bubble_sort_32 uses bubble sort to transform values and descending into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, descending and returns the list sorted in place and returned. One notable detail is that it works in quadratic time and mutates the original list."
  },
  {
    "input": "def binary_search_32(values, target, offset=0):\n    left = 0\n    right = len(values) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if values[mid] == target:\n            return offset + mid\n        if values[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return -1\n",
    "output": "Working through binary_search_32 uses binary search to transform values and target into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, target and returns the index of target or negative one. One notable detail is that the search assumes values is already sorted."
  },
  {
    "input": "def timing_decorator_32(func):\n    import functools, time\n    @functools.wraps(func)\n    def wrapper(*args, **kwargs):\n        start = time.perf_counter()\n        result = func(*args, **kwargs)\n        print(func.__name__, time.perf_counter() - start)\n        return result\n    return wrapper\n",
    "output": "The generator timing_decorator_32 uses a timing wrapper to transform func and args into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts func, args and returns the wrapped function result after printing elapsed time. One notable detail is that it preserves the original function metadata with functools.wraps."
  },
  {
    "input": "def chunked_32(items, size):\n    for index in range(0, len(items), size):\n        yield items[index:index + size]\n",
    "output": "This decorator chunked_32 uses generator-based chunking to transform items and size into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, size and returns lists of items in successive chunks. One notable detail is that the final chunk may be shorter if the length is not divisible by size."
  },
  {
    "input": "class file_context_32:\n    def __init__(self, path):\n        self.path = path\n\n    def __enter__(self):\n        self.handle = open(self.path, 'r', encoding='utf-8')\n        return self.handle\n\n    def __exit__(self, exc_type, exc, tb):\n        self.handle.close()\n        return False\n",
    "output": "The recursive file_context_32 uses a context manager to transform path and self.handle into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts path, self.handle and returns an open file handle during the with-block. One notable detail is that exceptions are not suppressed because __exit__ returns False."
  },
  {
    "input": "def safe_parse_32(text, fallback=0):\n    try:\n        return int(text)\n    except (TypeError, ValueError):\n        return fallback\n",
    "output": "Inside this class safe_parse_32 uses exception handling with a fallback to transform text and fallback into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, fallback and returns an integer when parsing succeeds, otherwise the fallback value. One notable detail is that TypeError and ValueError both map to the fallback path."
  },
  {
    "input": "def map_filter_reduce_32(values, factor=2):\n    from functools import reduce\n    doubled = list(map(lambda value: value * factor, values))\n    positives = list(filter(lambda value: value > 0, doubled))\n    return reduce(lambda acc, value: acc + value, positives, 0)\n",
    "output": "The function map_filter_reduce_32 uses higher-order functions to transform values and factor into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, factor and returns the reduced total as a number. One notable detail is that empty inputs return zero because reduce is seeded with an initial value."
  },
  {
    "input": "class Animal_32:\n    def speak(self):\n        return 'sound'\n\nclass Dog_32(Animal_32):\n    def speak(self):\n        return 'woof'\n",
    "output": "This class Animal_32 and Dog_32 uses inheritance and method overriding to transform self and Animal_32 into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts self, Animal_32 and returns a sound string from the overridden speak method. One notable detail is that the derived class returns a dog-specific sound instead of the base sound."
  },
  {
    "input": "async def async_collect_32(items, delay=0):\n    import asyncio\n    results = []\n    for item in items:\n        await asyncio.sleep(delay)\n        results.append(item)\n    return results\n",
    "output": "The algorithm async_collect_32 uses async iteration with await to transform items and delay into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, delay and returns the collected list after awaiting each item. One notable detail is that it yields control back to the event loop on every pass."
  },
  {
    "input": "class SingletonFactory_32:\n    _instance = None\n\n    def __new__(cls, *args, **kwargs):\n        if cls._instance is None:\n            cls._instance = super().__new__(cls)\n        return cls._instance\n",
    "output": "At its core SingletonFactory_32 uses the singleton pattern to transform args and kwargs into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts args, kwargs and returns the same instance every time. One notable detail is that state is shared because __new__ reuses the cached instance."
  },
  {
    "input": "def data_pipeline_32(records, minimum_age=18):\n    filtered = [record for record in records if record['age'] >= minimum_age]\n    normalized = [{**record, 'name': record['name'].strip().title()} for record in filtered]\n    return sorted(normalized, key=lambda record: record['name'])\n",
    "output": "Under the hood data_pipeline_32 uses a filter-transform-sort pipeline to transform records and minimum_age into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts records, minimum_age and returns a sorted list of normalized record dictionaries. One notable detail is that missing age or name keys will still raise KeyError."
  },
  {
    "input": "def module_worker_a_32(numbers):\n    return sum(numbers)\n\ndef module_worker_b_32(numbers):\n    return max(numbers) if numbers else None\n\ndef module_worker_c_32(numbers):\n    return min(numbers) if numbers else None\n",
    "output": "When called with module_worker_a_32 uses a small multi-function module to transform numbers and numbers into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts numbers, numbers and returns the computed aggregate returned by each helper. One notable detail is that empty lists cause module_worker_b and module_worker_c to return None."
  },
  {
    "input": "def gcd_calculator_33(left, right):\n    while right != 0:\n        left, right = right, left % right\n    return left\n",
    "output": "This utility gcd_calculator_33 uses the Euclidean algorithm to transform left and right into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts left, right and returns the greatest common divisor as an integer. One notable detail is that it returns the final left value when right becomes zero."
  },
  {
    "input": "def reverse_text_33(text, preserve_case=False):\n    if preserve_case:\n        return text[::-1]\n    return text.strip()[::-1]\n",
    "output": "The method reverse_text_33 uses slice-based string reversal to transform text and preserve_case into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, preserve_case and returns the reversed string. One notable detail is that it optionally strips surrounding whitespace before reversing."
  },
  {
    "input": "def dedupe_values_33(items, keep_order=True):\n    seen = set()\n    output = []\n    for item in items:\n        if item not in seen:\n            seen.add(item)\n            output.append(item)\n    return output if keep_order else sorted(set(items))\n",
    "output": "Built around dedupe_values_33 uses order-preserving deduplication with a set to transform items and keep_order into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, keep_order and returns a list of unique values. One notable detail is that the ordered branch preserves the first appearance of each item."
  },
  {
    "input": "def factorial_recursive_33(n, depth=0):\n    if n <= 1:\n        return 1\n    return n * factorial_recursive_33(n - 1, depth + 1)\n",
    "output": "Designed to factorial_recursive_33 uses recursive multiplication to transform n and depth into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts n, depth and returns the factorial of n as an integer. One notable detail is that it uses the n <= 1 base case, so zero and one both return one."
  },
  {
    "input": "class BankAccount_33:\n    def __init__(self, owner, balance=0):\n        self.owner = owner\n        self.balance = balance\n\n    def deposit(self, amount):\n        self.balance += amount\n        return self.balance\n\n    def withdraw(self, amount):\n        if amount > self.balance:\n            raise ValueError('insufficient funds')\n        self.balance -= amount\n        return self.balance\n",
    "output": "Working through BankAccount_33 uses an object-oriented balance manager to transform owner and balance into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts owner, balance and returns the updated account balance. One notable detail is that withdrawals larger than the current balance raise ValueError."
  },
  {
    "input": "def bubble_sort_33(values, descending=False):\n    for end in range(len(values) - 1, 0, -1):\n        for index in range(end):\n            if (values[index] > values[index + 1]) != descending:\n                values[index], values[index + 1] = values[index + 1], values[index]\n    return values\n",
    "output": "The generator bubble_sort_33 uses bubble sort to transform values and descending into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, descending and returns the list sorted in place and returned. One notable detail is that it works in quadratic time and mutates the original list."
  },
  {
    "input": "def binary_search_33(values, target, offset=0):\n    left = 0\n    right = len(values) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if values[mid] == target:\n            return offset + mid\n        if values[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return -1\n",
    "output": "This decorator binary_search_33 uses binary search to transform values and target into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, target and returns the index of target or negative one. One notable detail is that the search assumes values is already sorted."
  },
  {
    "input": "def timing_decorator_33(func):\n    import functools, time\n    @functools.wraps(func)\n    def wrapper(*args, **kwargs):\n        start = time.perf_counter()\n        result = func(*args, **kwargs)\n        print(func.__name__, time.perf_counter() - start)\n        return result\n    return wrapper\n",
    "output": "The recursive timing_decorator_33 uses a timing wrapper to transform func and args into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts func, args and returns the wrapped function result after printing elapsed time. One notable detail is that it preserves the original function metadata with functools.wraps."
  },
  {
    "input": "def chunked_33(items, size):\n    for index in range(0, len(items), size):\n        yield items[index:index + size]\n",
    "output": "Inside this class chunked_33 uses generator-based chunking to transform items and size into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, size and returns lists of items in successive chunks. One notable detail is that the final chunk may be shorter if the length is not divisible by size."
  },
  {
    "input": "class file_context_33:\n    def __init__(self, path):\n        self.path = path\n\n    def __enter__(self):\n        self.handle = open(self.path, 'r', encoding='utf-8')\n        return self.handle\n\n    def __exit__(self, exc_type, exc, tb):\n        self.handle.close()\n        return False\n",
    "output": "The function file_context_33 uses a context manager to transform path and self.handle into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts path, self.handle and returns an open file handle during the with-block. One notable detail is that exceptions are not suppressed because __exit__ returns False."
  },
  {
    "input": "def safe_parse_33(text, fallback=0):\n    try:\n        return int(text)\n    except (TypeError, ValueError):\n        return fallback\n",
    "output": "This class safe_parse_33 uses exception handling with a fallback to transform text and fallback into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, fallback and returns an integer when parsing succeeds, otherwise the fallback value. One notable detail is that TypeError and ValueError both map to the fallback path."
  },
  {
    "input": "def map_filter_reduce_33(values, factor=2):\n    from functools import reduce\n    doubled = list(map(lambda value: value * factor, values))\n    positives = list(filter(lambda value: value > 0, doubled))\n    return reduce(lambda acc, value: acc + value, positives, 0)\n",
    "output": "The algorithm map_filter_reduce_33 uses higher-order functions to transform values and factor into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, factor and returns the reduced total as a number. One notable detail is that empty inputs return zero because reduce is seeded with an initial value."
  },
  {
    "input": "class Animal_33:\n    def speak(self):\n        return 'sound'\n\nclass Dog_33(Animal_33):\n    def speak(self):\n        return 'woof'\n",
    "output": "At its core Animal_33 and Dog_33 uses inheritance and method overriding to transform self and Animal_33 into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts self, Animal_33 and returns a sound string from the overridden speak method. One notable detail is that the derived class returns a dog-specific sound instead of the base sound."
  },
  {
    "input": "async def async_collect_33(items, delay=0):\n    import asyncio\n    results = []\n    for item in items:\n        await asyncio.sleep(delay)\n        results.append(item)\n    return results\n",
    "output": "Under the hood async_collect_33 uses async iteration with await to transform items and delay into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, delay and returns the collected list after awaiting each item. One notable detail is that it yields control back to the event loop on every pass."
  },
  {
    "input": "class SingletonFactory_33:\n    _instance = None\n\n    def __new__(cls, *args, **kwargs):\n        if cls._instance is None:\n            cls._instance = super().__new__(cls)\n        return cls._instance\n",
    "output": "When called with SingletonFactory_33 uses the singleton pattern to transform args and kwargs into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts args, kwargs and returns the same instance every time. One notable detail is that state is shared because __new__ reuses the cached instance."
  },
  {
    "input": "def data_pipeline_33(records, minimum_age=18):\n    filtered = [record for record in records if record['age'] >= minimum_age]\n    normalized = [{**record, 'name': record['name'].strip().title()} for record in filtered]\n    return sorted(normalized, key=lambda record: record['name'])\n",
    "output": "This utility data_pipeline_33 uses a filter-transform-sort pipeline to transform records and minimum_age into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts records, minimum_age and returns a sorted list of normalized record dictionaries. One notable detail is that missing age or name keys will still raise KeyError."
  },
  {
    "input": "def module_worker_a_33(numbers):\n    return sum(numbers)\n\ndef module_worker_b_33(numbers):\n    return max(numbers) if numbers else None\n\ndef module_worker_c_33(numbers):\n    return min(numbers) if numbers else None\n",
    "output": "The method module_worker_a_33 uses a small multi-function module to transform numbers and numbers into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts numbers, numbers and returns the computed aggregate returned by each helper. One notable detail is that empty lists cause module_worker_b and module_worker_c to return None."
  },
  {
    "input": "def gcd_calculator_34(left, right):\n    while right != 0:\n        left, right = right, left % right\n    return left\n",
    "output": "Built around gcd_calculator_34 uses the Euclidean algorithm to transform left and right into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts left, right and returns the greatest common divisor as an integer. One notable detail is that it returns the final left value when right becomes zero."
  },
  {
    "input": "def reverse_text_34(text, preserve_case=False):\n    if preserve_case:\n        return text[::-1]\n    return text.strip()[::-1]\n",
    "output": "Designed to reverse_text_34 uses slice-based string reversal to transform text and preserve_case into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, preserve_case and returns the reversed string. One notable detail is that it optionally strips surrounding whitespace before reversing."
  },
  {
    "input": "def dedupe_values_34(items, keep_order=True):\n    seen = set()\n    output = []\n    for item in items:\n        if item not in seen:\n            seen.add(item)\n            output.append(item)\n    return output if keep_order else sorted(set(items))\n",
    "output": "Working through dedupe_values_34 uses order-preserving deduplication with a set to transform items and keep_order into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, keep_order and returns a list of unique values. One notable detail is that the ordered branch preserves the first appearance of each item."
  },
  {
    "input": "def factorial_recursive_34(n, depth=0):\n    if n <= 1:\n        return 1\n    return n * factorial_recursive_34(n - 1, depth + 1)\n",
    "output": "The generator factorial_recursive_34 uses recursive multiplication to transform n and depth into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts n, depth and returns the factorial of n as an integer. One notable detail is that it uses the n <= 1 base case, so zero and one both return one."
  },
  {
    "input": "class BankAccount_34:\n    def __init__(self, owner, balance=0):\n        self.owner = owner\n        self.balance = balance\n\n    def deposit(self, amount):\n        self.balance += amount\n        return self.balance\n\n    def withdraw(self, amount):\n        if amount > self.balance:\n            raise ValueError('insufficient funds')\n        self.balance -= amount\n        return self.balance\n",
    "output": "This decorator BankAccount_34 uses an object-oriented balance manager to transform owner and balance into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts owner, balance and returns the updated account balance. One notable detail is that withdrawals larger than the current balance raise ValueError."
  },
  {
    "input": "def bubble_sort_34(values, descending=False):\n    for end in range(len(values) - 1, 0, -1):\n        for index in range(end):\n            if (values[index] > values[index + 1]) != descending:\n                values[index], values[index + 1] = values[index + 1], values[index]\n    return values\n",
    "output": "The recursive bubble_sort_34 uses bubble sort to transform values and descending into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, descending and returns the list sorted in place and returned. One notable detail is that it works in quadratic time and mutates the original list."
  },
  {
    "input": "def binary_search_34(values, target, offset=0):\n    left = 0\n    right = len(values) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if values[mid] == target:\n            return offset + mid\n        if values[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return -1\n",
    "output": "Inside this class binary_search_34 uses binary search to transform values and target into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, target and returns the index of target or negative one. One notable detail is that the search assumes values is already sorted."
  },
  {
    "input": "def timing_decorator_34(func):\n    import functools, time\n    @functools.wraps(func)\n    def wrapper(*args, **kwargs):\n        start = time.perf_counter()\n        result = func(*args, **kwargs)\n        print(func.__name__, time.perf_counter() - start)\n        return result\n    return wrapper\n",
    "output": "The function timing_decorator_34 uses a timing wrapper to transform func and args into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts func, args and returns the wrapped function result after printing elapsed time. One notable detail is that it preserves the original function metadata with functools.wraps."
  },
  {
    "input": "def chunked_34(items, size):\n    for index in range(0, len(items), size):\n        yield items[index:index + size]\n",
    "output": "This class chunked_34 uses generator-based chunking to transform items and size into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, size and returns lists of items in successive chunks. One notable detail is that the final chunk may be shorter if the length is not divisible by size."
  },
  {
    "input": "class file_context_34:\n    def __init__(self, path):\n        self.path = path\n\n    def __enter__(self):\n        self.handle = open(self.path, 'r', encoding='utf-8')\n        return self.handle\n\n    def __exit__(self, exc_type, exc, tb):\n        self.handle.close()\n        return False\n",
    "output": "The algorithm file_context_34 uses a context manager to transform path and self.handle into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts path, self.handle and returns an open file handle during the with-block. One notable detail is that exceptions are not suppressed because __exit__ returns False."
  },
  {
    "input": "def safe_parse_34(text, fallback=0):\n    try:\n        return int(text)\n    except (TypeError, ValueError):\n        return fallback\n",
    "output": "At its core safe_parse_34 uses exception handling with a fallback to transform text and fallback into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, fallback and returns an integer when parsing succeeds, otherwise the fallback value. One notable detail is that TypeError and ValueError both map to the fallback path."
  },
  {
    "input": "def map_filter_reduce_34(values, factor=2):\n    from functools import reduce\n    doubled = list(map(lambda value: value * factor, values))\n    positives = list(filter(lambda value: value > 0, doubled))\n    return reduce(lambda acc, value: acc + value, positives, 0)\n",
    "output": "Under the hood map_filter_reduce_34 uses higher-order functions to transform values and factor into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, factor and returns the reduced total as a number. One notable detail is that empty inputs return zero because reduce is seeded with an initial value."
  },
  {
    "input": "class Animal_34:\n    def speak(self):\n        return 'sound'\n\nclass Dog_34(Animal_34):\n    def speak(self):\n        return 'woof'\n",
    "output": "When called with Animal_34 and Dog_34 uses inheritance and method overriding to transform self and Animal_34 into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts self, Animal_34 and returns a sound string from the overridden speak method. One notable detail is that the derived class returns a dog-specific sound instead of the base sound."
  },
  {
    "input": "async def async_collect_34(items, delay=0):\n    import asyncio\n    results = []\n    for item in items:\n        await asyncio.sleep(delay)\n        results.append(item)\n    return results\n",
    "output": "This utility async_collect_34 uses async iteration with await to transform items and delay into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, delay and returns the collected list after awaiting each item. One notable detail is that it yields control back to the event loop on every pass."
  },
  {
    "input": "class SingletonFactory_34:\n    _instance = None\n\n    def __new__(cls, *args, **kwargs):\n        if cls._instance is None:\n            cls._instance = super().__new__(cls)\n        return cls._instance\n",
    "output": "The method SingletonFactory_34 uses the singleton pattern to transform args and kwargs into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts args, kwargs and returns the same instance every time. One notable detail is that state is shared because __new__ reuses the cached instance."
  },
  {
    "input": "def data_pipeline_34(records, minimum_age=18):\n    filtered = [record for record in records if record['age'] >= minimum_age]\n    normalized = [{**record, 'name': record['name'].strip().title()} for record in filtered]\n    return sorted(normalized, key=lambda record: record['name'])\n",
    "output": "Built around data_pipeline_34 uses a filter-transform-sort pipeline to transform records and minimum_age into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts records, minimum_age and returns a sorted list of normalized record dictionaries. One notable detail is that missing age or name keys will still raise KeyError."
  },
  {
    "input": "def module_worker_a_34(numbers):\n    return sum(numbers)\n\ndef module_worker_b_34(numbers):\n    return max(numbers) if numbers else None\n\ndef module_worker_c_34(numbers):\n    return min(numbers) if numbers else None\n",
    "output": "Designed to module_worker_a_34 uses a small multi-function module to transform numbers and numbers into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts numbers, numbers and returns the computed aggregate returned by each helper. One notable detail is that empty lists cause module_worker_b and module_worker_c to return None."
  },
  {
    "input": "def prime_check_1000(candidate, divisor=2):\n    if candidate < 2:\n        return False\n    while divisor * divisor <= candidate:\n        if candidate % divisor == 0:\n            return False\n        divisor += 1\n    return True\n",
    "output": "Working through prime_check_1000 uses trial division prime checking to transform candidate and divisor into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts candidate, divisor and returns a boolean indicating primality. One notable detail is that values below two are treated as not prime."
  },
  {
    "input": "def lcm_value_1001(left, right):\n    if left == 0 or right == 0:\n        return 0\n    a, b = abs(left), abs(right)\n    while b:\n        a, b = b, a % b\n    gcd = a\n    return abs(left * right) // gcd\n",
    "output": "The generator lcm_value_1001 uses the least common multiple formula to transform left and right into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts left, right and returns the least common multiple as an integer. One notable detail is that zero inputs short-circuit to zero."
  },
  {
    "input": "def palindrome_normalizer_1002(text, ignore_case=True):\n    cleaned = ''.join(ch.lower() for ch in text if ch.isalnum()) if ignore_case else ''.join(ch for ch in text if ch.isalnum())\n    return cleaned == cleaned[::-1]\n",
    "output": "This decorator palindrome_normalizer_1002 uses string normalization and palindrome testing to transform text and ignore_case into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, ignore_case and returns a boolean result. One notable detail is that it ignores punctuation and whitespace during the comparison."
  },
  {
    "input": "def grouped_totals_1003(pairs, default=0):\n    totals = {}\n    for category, value in pairs:\n        totals[category] = totals.get(category, default) + value\n    return totals\n",
    "output": "The recursive grouped_totals_1003 uses dictionary accumulation with get to transform pairs and default into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts pairs, default and returns a dictionary of totals. One notable detail is that new categories start from the default value."
  },
  {
    "input": "def quick_select_1004(values, k):\n    if len(values) == 1:\n        return values[0]\n    pivot = values[len(values) // 2]\n    lows = [value for value in values if value < pivot]\n    highs = [value for value in values if value > pivot]\n    pivots = [value for value in values if value == pivot]\n    if k < len(lows):\n        return quick_select_1004(lows, k)\n    if k < len(lows) + len(pivots):\n        return pivots[0]\n    return quick_select_1004(highs, k - len(lows) - len(pivots))\n",
    "output": "Inside this class quick_select_1004 uses the quickselect algorithm to transform values and k into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, k and returns the kth smallest element. One notable detail is that k must be within the valid index range."
  },
  {
    "input": "def bfs_levels_1005(graph, start):\n    levels = {start: 0}\n    queue = [start]\n    while queue:\n        node = queue.pop(0)\n        for neighbor in graph.get(node, []):\n            if neighbor not in levels:\n                levels[neighbor] = levels[node] + 1\n                queue.append(neighbor)\n    return levels\n",
    "output": "The function bfs_levels_1005 uses breadth-first search to transform graph and start into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts graph, start and returns a mapping of nodes to their distance from start. One notable detail is that unreachable nodes never appear in the returned dictionary."
  },
  {
    "input": "def retry_request_1006(request_fn, max_attempts=3):\n    attempt = 0\n    while True:\n        try:\n            return request_fn()\n        except Exception:\n            attempt += 1\n            if attempt >= max_attempts:\n                raise\n",
    "output": "This class retry_request_1006 uses retry with a bounded loop to transform request_fn and max_attempts into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts request_fn, max_attempts and returns the successful return value of request_fn. One notable detail is that after max_attempts failures the last exception is raised again."
  },
  {
    "input": "def rate_limit_1007(calls, limit):\n    def decorator(func):\n        state = {'count': 0}\n        def wrapper(*args, **kwargs):\n            if state['count'] >= limit:\n                raise RuntimeError('rate limit exceeded')\n            state['count'] += 1\n            return func(*args, **kwargs)\n        return wrapper\n    return decorator\n",
    "output": "The algorithm rate_limit_1007 uses a rate-limiting decorator to transform calls and limit into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts calls, limit and returns the wrapped function result while under the limit. One notable detail is that the internal counter is shared by every call to the wrapper."
  },
  {
    "input": "def file_writer_1008(path, lines):\n    with open(path, 'w', encoding='utf-8') as handle:\n        for line in lines:\n            handle.write(line + '\\n')\n    return path\n",
    "output": "At its core file_writer_1008 uses file output with a with-block to transform path and lines into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts path, lines and returns the file path after writing. One notable detail is that each line is written with a trailing newline."
  },
  {
    "input": "def json_like_parser_1009(payload, key, default=None):\n    if key in payload:\n        return payload[key]\n    return default\n",
    "output": "Under the hood json_like_parser_1009 uses dictionary lookup with a fallback to transform payload and key into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts payload, key and returns the matching value or the default. One notable detail is that missing keys fall back to the supplied default."
  },
  {
    "input": "def observer_notify_1010(observers, message):\n    for observer in observers:\n        observer.update(message)\n    return len(observers)\n",
    "output": "When called with observer_notify_1010 uses the observer pattern to transform observers and message into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts observers, message and returns the number of notified observers. One notable detail is that every observer object must provide an update method."
  },
  {
    "input": "def strategy_apply_1011(values, strategy):\n    return strategy(values)\n",
    "output": "This utility strategy_apply_1011 uses the strategy pattern to transform values and strategy into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, strategy and returns whatever the strategy callable returns. One notable detail is that strategy must be callable or Python will raise a TypeError."
  },
  {
    "input": "def async_batch_1012(items):\n    import asyncio\n    async def worker(item):\n        await asyncio.sleep(0)\n        return item * 2\n    return asyncio.gather(*(worker(item) for item in items))\n",
    "output": "The method async_batch_1012 uses asyncio.gather orchestration to transform items and worker into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, worker and returns an awaitable gathering doubled items. One notable detail is that the caller must await the returned gather object."
  },
  {
    "input": "class Temperature_1013:\n    def __init__(self, celsius):\n        self.celsius = celsius\n\n    @property\n    def fahrenheit(self):\n        return self.celsius * 9 / 5 + 32\n",
    "output": "Built around Temperature_1013 uses a computed property to transform celsius and fahrenheit into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts celsius, fahrenheit and returns the Celsius value or derived Fahrenheit property. One notable detail is that the property recalculates on every access."
  },
  {
    "input": "def nested_comprehension_1014(matrix):\n    return [value for row in matrix for value in row if value is not None]\n",
    "output": "Designed to nested_comprehension_1014 uses a nested list comprehension to transform matrix and row into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts matrix, row and returns a flattened list without None values. One notable detail is that it preserves row order while flattening."
  },
  {
    "input": "def loading_pipeline_1015(rows):\n    cleaned = []\n    for row in rows:\n        if row and row.get('active'):\n            cleaned.append({**row, 'name': row['name'].strip()})\n    cleaned.sort(key=lambda row: row['name'])\n    return cleaned\n",
    "output": "Working through loading_pipeline_1015 uses a multi-step cleaning pipeline to transform rows and cleaned into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts rows, cleaned and returns a sorted list of active records. One notable detail is that rows missing the name key will still fail at runtime."
  },
  {
    "input": "def recursive_tree_sum_1016(node):\n    if node is None:\n        return 0\n    return node.value + recursive_tree_sum_1016(node.left) + recursive_tree_sum_1016(node.right)\n",
    "output": "The generator recursive_tree_sum_1016 uses recursive tree traversal to transform node and value into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts node, value and returns the sum of values stored in the tree. One notable detail is that the base case returns zero for empty nodes."
  },
  {
    "input": "def factory_build_1017(kind, payload):\n    if kind == 'circle':\n        return {'type': 'circle', 'radius': payload}\n    if kind == 'square':\n        return {'type': 'square', 'side': payload}\n    raise ValueError('unknown kind')\n",
    "output": "This decorator factory_build_1017 uses a factory function to transform kind and payload into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts kind, payload and returns a shape description dictionary. One notable detail is that unknown kind values raise ValueError."
  },
  {
    "input": "def config_loader_1018(settings, key, env):\n    if key in settings:\n        return settings[key]\n    return env.get(key, 'default')\n",
    "output": "The recursive config_loader_1018 uses configuration fallback lookup to transform settings and key into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts settings, key and returns the configured value or a default string. One notable detail is that environment variables win only when settings omit the key."
  },
  {
    "input": "def dfs_path_1019(graph, start, goal):\n    stack = [(start, [start])]\n    visited = set()\n    while stack:\n        node, path = stack.pop()\n        if node == goal:\n            return path\n        if node in visited:\n            continue\n        visited.add(node)\n        for neighbor in graph.get(node, []):\n            stack.append((neighbor, path + [neighbor]))\n    return None\n",
    "output": "Inside this class dfs_path_1019 uses depth-first search path finding to transform graph and start into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts graph, start and returns a path list or None. One notable detail is that if the goal is unreachable, the function returns None."
  },
  {
    "input": "def prime_check_1020(candidate, divisor=2):\n    if candidate < 2:\n        return False\n    while divisor * divisor <= candidate:\n        if candidate % divisor == 0:\n            return False\n        divisor += 1\n    return True\n",
    "output": "The function prime_check_1020 uses trial division prime checking to transform candidate and divisor into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts candidate, divisor and returns a boolean indicating primality. One notable detail is that values below two are treated as not prime."
  },
  {
    "input": "def lcm_value_1021(left, right):\n    if left == 0 or right == 0:\n        return 0\n    a, b = abs(left), abs(right)\n    while b:\n        a, b = b, a % b\n    gcd = a\n    return abs(left * right) // gcd\n",
    "output": "This class lcm_value_1021 uses the least common multiple formula to transform left and right into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts left, right and returns the least common multiple as an integer. One notable detail is that zero inputs short-circuit to zero."
  },
  {
    "input": "def palindrome_normalizer_1022(text, ignore_case=True):\n    cleaned = ''.join(ch.lower() for ch in text if ch.isalnum()) if ignore_case else ''.join(ch for ch in text if ch.isalnum())\n    return cleaned == cleaned[::-1]\n",
    "output": "The algorithm palindrome_normalizer_1022 uses string normalization and palindrome testing to transform text and ignore_case into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, ignore_case and returns a boolean result. One notable detail is that it ignores punctuation and whitespace during the comparison."
  },
  {
    "input": "def grouped_totals_1023(pairs, default=0):\n    totals = {}\n    for category, value in pairs:\n        totals[category] = totals.get(category, default) + value\n    return totals\n",
    "output": "At its core grouped_totals_1023 uses dictionary accumulation with get to transform pairs and default into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts pairs, default and returns a dictionary of totals. One notable detail is that new categories start from the default value."
  },
  {
    "input": "def quick_select_1024(values, k):\n    if len(values) == 1:\n        return values[0]\n    pivot = values[len(values) // 2]\n    lows = [value for value in values if value < pivot]\n    highs = [value for value in values if value > pivot]\n    pivots = [value for value in values if value == pivot]\n    if k < len(lows):\n        return quick_select_1024(lows, k)\n    if k < len(lows) + len(pivots):\n        return pivots[0]\n    return quick_select_1024(highs, k - len(lows) - len(pivots))\n",
    "output": "Under the hood quick_select_1024 uses the quickselect algorithm to transform values and k into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, k and returns the kth smallest element. One notable detail is that k must be within the valid index range."
  },
  {
    "input": "def bfs_levels_1025(graph, start):\n    levels = {start: 0}\n    queue = [start]\n    while queue:\n        node = queue.pop(0)\n        for neighbor in graph.get(node, []):\n            if neighbor not in levels:\n                levels[neighbor] = levels[node] + 1\n                queue.append(neighbor)\n    return levels\n",
    "output": "When called with bfs_levels_1025 uses breadth-first search to transform graph and start into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts graph, start and returns a mapping of nodes to their distance from start. One notable detail is that unreachable nodes never appear in the returned dictionary."
  },
  {
    "input": "def retry_request_1026(request_fn, max_attempts=3):\n    attempt = 0\n    while True:\n        try:\n            return request_fn()\n        except Exception:\n            attempt += 1\n            if attempt >= max_attempts:\n                raise\n",
    "output": "This utility retry_request_1026 uses retry with a bounded loop to transform request_fn and max_attempts into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts request_fn, max_attempts and returns the successful return value of request_fn. One notable detail is that after max_attempts failures the last exception is raised again."
  },
  {
    "input": "def rate_limit_1027(calls, limit):\n    def decorator(func):\n        state = {'count': 0}\n        def wrapper(*args, **kwargs):\n            if state['count'] >= limit:\n                raise RuntimeError('rate limit exceeded')\n            state['count'] += 1\n            return func(*args, **kwargs)\n        return wrapper\n    return decorator\n",
    "output": "The method rate_limit_1027 uses a rate-limiting decorator to transform calls and limit into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts calls, limit and returns the wrapped function result while under the limit. One notable detail is that the internal counter is shared by every call to the wrapper."
  },
  {
    "input": "def file_writer_1028(path, lines):\n    with open(path, 'w', encoding='utf-8') as handle:\n        for line in lines:\n            handle.write(line + '\\n')\n    return path\n",
    "output": "Built around file_writer_1028 uses file output with a with-block to transform path and lines into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts path, lines and returns the file path after writing. One notable detail is that each line is written with a trailing newline."
  },
  {
    "input": "def json_like_parser_1029(payload, key, default=None):\n    if key in payload:\n        return payload[key]\n    return default\n",
    "output": "Designed to json_like_parser_1029 uses dictionary lookup with a fallback to transform payload and key into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts payload, key and returns the matching value or the default. One notable detail is that missing keys fall back to the supplied default."
  },
  {
    "input": "def observer_notify_1030(observers, message):\n    for observer in observers:\n        observer.update(message)\n    return len(observers)\n",
    "output": "Working through observer_notify_1030 uses the observer pattern to transform observers and message into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts observers, message and returns the number of notified observers. One notable detail is that every observer object must provide an update method."
  },
  {
    "input": "def strategy_apply_1031(values, strategy):\n    return strategy(values)\n",
    "output": "The generator strategy_apply_1031 uses the strategy pattern to transform values and strategy into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, strategy and returns whatever the strategy callable returns. One notable detail is that strategy must be callable or Python will raise a TypeError."
  },
  {
    "input": "def async_batch_1032(items):\n    import asyncio\n    async def worker(item):\n        await asyncio.sleep(0)\n        return item * 2\n    return asyncio.gather(*(worker(item) for item in items))\n",
    "output": "This decorator async_batch_1032 uses asyncio.gather orchestration to transform items and worker into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, worker and returns an awaitable gathering doubled items. One notable detail is that the caller must await the returned gather object."
  },
  {
    "input": "class Temperature_1033:\n    def __init__(self, celsius):\n        self.celsius = celsius\n\n    @property\n    def fahrenheit(self):\n        return self.celsius * 9 / 5 + 32\n",
    "output": "The recursive Temperature_1033 uses a computed property to transform celsius and fahrenheit into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts celsius, fahrenheit and returns the Celsius value or derived Fahrenheit property. One notable detail is that the property recalculates on every access."
  },
  {
    "input": "def nested_comprehension_1034(matrix):\n    return [value for row in matrix for value in row if value is not None]\n",
    "output": "Inside this class nested_comprehension_1034 uses a nested list comprehension to transform matrix and row into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts matrix, row and returns a flattened list without None values. One notable detail is that it preserves row order while flattening."
  },
  {
    "input": "def loading_pipeline_1035(rows):\n    cleaned = []\n    for row in rows:\n        if row and row.get('active'):\n            cleaned.append({**row, 'name': row['name'].strip()})\n    cleaned.sort(key=lambda row: row['name'])\n    return cleaned\n",
    "output": "The function loading_pipeline_1035 uses a multi-step cleaning pipeline to transform rows and cleaned into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts rows, cleaned and returns a sorted list of active records. One notable detail is that rows missing the name key will still fail at runtime."
  },
  {
    "input": "def recursive_tree_sum_1036(node):\n    if node is None:\n        return 0\n    return node.value + recursive_tree_sum_1036(node.left) + recursive_tree_sum_1036(node.right)\n",
    "output": "This class recursive_tree_sum_1036 uses recursive tree traversal to transform node and value into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts node, value and returns the sum of values stored in the tree. One notable detail is that the base case returns zero for empty nodes."
  },
  {
    "input": "def factory_build_1037(kind, payload):\n    if kind == 'circle':\n        return {'type': 'circle', 'radius': payload}\n    if kind == 'square':\n        return {'type': 'square', 'side': payload}\n    raise ValueError('unknown kind')\n",
    "output": "The algorithm factory_build_1037 uses a factory function to transform kind and payload into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts kind, payload and returns a shape description dictionary. One notable detail is that unknown kind values raise ValueError."
  },
  {
    "input": "def config_loader_1038(settings, key, env):\n    if key in settings:\n        return settings[key]\n    return env.get(key, 'default')\n",
    "output": "At its core config_loader_1038 uses configuration fallback lookup to transform settings and key into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts settings, key and returns the configured value or a default string. One notable detail is that environment variables win only when settings omit the key."
  },
  {
    "input": "def dfs_path_1039(graph, start, goal):\n    stack = [(start, [start])]\n    visited = set()\n    while stack:\n        node, path = stack.pop()\n        if node == goal:\n            return path\n        if node in visited:\n            continue\n        visited.add(node)\n        for neighbor in graph.get(node, []):\n            stack.append((neighbor, path + [neighbor]))\n    return None\n",
    "output": "Under the hood dfs_path_1039 uses depth-first search path finding to transform graph and start into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts graph, start and returns a path list or None. One notable detail is that if the goal is unreachable, the function returns None."
  },
  {
    "input": "def prime_check_1040(candidate, divisor=2):\n    if candidate < 2:\n        return False\n    while divisor * divisor <= candidate:\n        if candidate % divisor == 0:\n            return False\n        divisor += 1\n    return True\n",
    "output": "When called with prime_check_1040 uses trial division prime checking to transform candidate and divisor into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts candidate, divisor and returns a boolean indicating primality. One notable detail is that values below two are treated as not prime."
  },
  {
    "input": "def lcm_value_1041(left, right):\n    if left == 0 or right == 0:\n        return 0\n    a, b = abs(left), abs(right)\n    while b:\n        a, b = b, a % b\n    gcd = a\n    return abs(left * right) // gcd\n",
    "output": "This utility lcm_value_1041 uses the least common multiple formula to transform left and right into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts left, right and returns the least common multiple as an integer. One notable detail is that zero inputs short-circuit to zero."
  },
  {
    "input": "def palindrome_normalizer_1042(text, ignore_case=True):\n    cleaned = ''.join(ch.lower() for ch in text if ch.isalnum()) if ignore_case else ''.join(ch for ch in text if ch.isalnum())\n    return cleaned == cleaned[::-1]\n",
    "output": "The method palindrome_normalizer_1042 uses string normalization and palindrome testing to transform text and ignore_case into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, ignore_case and returns a boolean result. One notable detail is that it ignores punctuation and whitespace during the comparison."
  },
  {
    "input": "def grouped_totals_1043(pairs, default=0):\n    totals = {}\n    for category, value in pairs:\n        totals[category] = totals.get(category, default) + value\n    return totals\n",
    "output": "Built around grouped_totals_1043 uses dictionary accumulation with get to transform pairs and default into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts pairs, default and returns a dictionary of totals. One notable detail is that new categories start from the default value."
  },
  {
    "input": "def quick_select_1044(values, k):\n    if len(values) == 1:\n        return values[0]\n    pivot = values[len(values) // 2]\n    lows = [value for value in values if value < pivot]\n    highs = [value for value in values if value > pivot]\n    pivots = [value for value in values if value == pivot]\n    if k < len(lows):\n        return quick_select_1044(lows, k)\n    if k < len(lows) + len(pivots):\n        return pivots[0]\n    return quick_select_1044(highs, k - len(lows) - len(pivots))\n",
    "output": "Designed to quick_select_1044 uses the quickselect algorithm to transform values and k into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, k and returns the kth smallest element. One notable detail is that k must be within the valid index range."
  },
  {
    "input": "def bfs_levels_1045(graph, start):\n    levels = {start: 0}\n    queue = [start]\n    while queue:\n        node = queue.pop(0)\n        for neighbor in graph.get(node, []):\n            if neighbor not in levels:\n                levels[neighbor] = levels[node] + 1\n                queue.append(neighbor)\n    return levels\n",
    "output": "Working through bfs_levels_1045 uses breadth-first search to transform graph and start into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts graph, start and returns a mapping of nodes to their distance from start. One notable detail is that unreachable nodes never appear in the returned dictionary."
  },
  {
    "input": "def retry_request_1046(request_fn, max_attempts=3):\n    attempt = 0\n    while True:\n        try:\n            return request_fn()\n        except Exception:\n            attempt += 1\n            if attempt >= max_attempts:\n                raise\n",
    "output": "The generator retry_request_1046 uses retry with a bounded loop to transform request_fn and max_attempts into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts request_fn, max_attempts and returns the successful return value of request_fn. One notable detail is that after max_attempts failures the last exception is raised again."
  },
  {
    "input": "def rate_limit_1047(calls, limit):\n    def decorator(func):\n        state = {'count': 0}\n        def wrapper(*args, **kwargs):\n            if state['count'] >= limit:\n                raise RuntimeError('rate limit exceeded')\n            state['count'] += 1\n            return func(*args, **kwargs)\n        return wrapper\n    return decorator\n",
    "output": "This decorator rate_limit_1047 uses a rate-limiting decorator to transform calls and limit into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts calls, limit and returns the wrapped function result while under the limit. One notable detail is that the internal counter is shared by every call to the wrapper."
  },
  {
    "input": "def file_writer_1048(path, lines):\n    with open(path, 'w', encoding='utf-8') as handle:\n        for line in lines:\n            handle.write(line + '\\n')\n    return path\n",
    "output": "The recursive file_writer_1048 uses file output with a with-block to transform path and lines into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts path, lines and returns the file path after writing. One notable detail is that each line is written with a trailing newline."
  },
  {
    "input": "def json_like_parser_1049(payload, key, default=None):\n    if key in payload:\n        return payload[key]\n    return default\n",
    "output": "Inside this class json_like_parser_1049 uses dictionary lookup with a fallback to transform payload and key into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts payload, key and returns the matching value or the default. One notable detail is that missing keys fall back to the supplied default."
  },
  {
    "input": "def observer_notify_1050(observers, message):\n    for observer in observers:\n        observer.update(message)\n    return len(observers)\n",
    "output": "The function observer_notify_1050 uses the observer pattern to transform observers and message into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts observers, message and returns the number of notified observers. One notable detail is that every observer object must provide an update method."
  },
  {
    "input": "def strategy_apply_1051(values, strategy):\n    return strategy(values)\n",
    "output": "This class strategy_apply_1051 uses the strategy pattern to transform values and strategy into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, strategy and returns whatever the strategy callable returns. One notable detail is that strategy must be callable or Python will raise a TypeError."
  },
  {
    "input": "def async_batch_1052(items):\n    import asyncio\n    async def worker(item):\n        await asyncio.sleep(0)\n        return item * 2\n    return asyncio.gather(*(worker(item) for item in items))\n",
    "output": "The algorithm async_batch_1052 uses asyncio.gather orchestration to transform items and worker into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, worker and returns an awaitable gathering doubled items. One notable detail is that the caller must await the returned gather object."
  },
  {
    "input": "class Temperature_1053:\n    def __init__(self, celsius):\n        self.celsius = celsius\n\n    @property\n    def fahrenheit(self):\n        return self.celsius * 9 / 5 + 32\n",
    "output": "At its core Temperature_1053 uses a computed property to transform celsius and fahrenheit into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts celsius, fahrenheit and returns the Celsius value or derived Fahrenheit property. One notable detail is that the property recalculates on every access."
  },
  {
    "input": "def nested_comprehension_1054(matrix):\n    return [value for row in matrix for value in row if value is not None]\n",
    "output": "Under the hood nested_comprehension_1054 uses a nested list comprehension to transform matrix and row into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts matrix, row and returns a flattened list without None values. One notable detail is that it preserves row order while flattening."
  },
  {
    "input": "def loading_pipeline_1055(rows):\n    cleaned = []\n    for row in rows:\n        if row and row.get('active'):\n            cleaned.append({**row, 'name': row['name'].strip()})\n    cleaned.sort(key=lambda row: row['name'])\n    return cleaned\n",
    "output": "When called with loading_pipeline_1055 uses a multi-step cleaning pipeline to transform rows and cleaned into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts rows, cleaned and returns a sorted list of active records. One notable detail is that rows missing the name key will still fail at runtime."
  },
  {
    "input": "def recursive_tree_sum_1056(node):\n    if node is None:\n        return 0\n    return node.value + recursive_tree_sum_1056(node.left) + recursive_tree_sum_1056(node.right)\n",
    "output": "This utility recursive_tree_sum_1056 uses recursive tree traversal to transform node and value into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts node, value and returns the sum of values stored in the tree. One notable detail is that the base case returns zero for empty nodes."
  },
  {
    "input": "def factory_build_1057(kind, payload):\n    if kind == 'circle':\n        return {'type': 'circle', 'radius': payload}\n    if kind == 'square':\n        return {'type': 'square', 'side': payload}\n    raise ValueError('unknown kind')\n",
    "output": "The method factory_build_1057 uses a factory function to transform kind and payload into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts kind, payload and returns a shape description dictionary. One notable detail is that unknown kind values raise ValueError."
  },
  {
    "input": "def config_loader_1058(settings, key, env):\n    if key in settings:\n        return settings[key]\n    return env.get(key, 'default')\n",
    "output": "Built around config_loader_1058 uses configuration fallback lookup to transform settings and key into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts settings, key and returns the configured value or a default string. One notable detail is that environment variables win only when settings omit the key."
  },
  {
    "input": "def dfs_path_1059(graph, start, goal):\n    stack = [(start, [start])]\n    visited = set()\n    while stack:\n        node, path = stack.pop()\n        if node == goal:\n            return path\n        if node in visited:\n            continue\n        visited.add(node)\n        for neighbor in graph.get(node, []):\n            stack.append((neighbor, path + [neighbor]))\n    return None\n",
    "output": "Designed to dfs_path_1059 uses depth-first search path finding to transform graph and start into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts graph, start and returns a path list or None. One notable detail is that if the goal is unreachable, the function returns None."
  },
  {
    "input": "def prime_check_1060(candidate, divisor=2):\n    if candidate < 2:\n        return False\n    while divisor * divisor <= candidate:\n        if candidate % divisor == 0:\n            return False\n        divisor += 1\n    return True\n",
    "output": "Working through prime_check_1060 uses trial division prime checking to transform candidate and divisor into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts candidate, divisor and returns a boolean indicating primality. One notable detail is that values below two are treated as not prime."
  },
  {
    "input": "def lcm_value_1061(left, right):\n    if left == 0 or right == 0:\n        return 0\n    a, b = abs(left), abs(right)\n    while b:\n        a, b = b, a % b\n    gcd = a\n    return abs(left * right) // gcd\n",
    "output": "The generator lcm_value_1061 uses the least common multiple formula to transform left and right into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts left, right and returns the least common multiple as an integer. One notable detail is that zero inputs short-circuit to zero."
  },
  {
    "input": "def palindrome_normalizer_1062(text, ignore_case=True):\n    cleaned = ''.join(ch.lower() for ch in text if ch.isalnum()) if ignore_case else ''.join(ch for ch in text if ch.isalnum())\n    return cleaned == cleaned[::-1]\n",
    "output": "This decorator palindrome_normalizer_1062 uses string normalization and palindrome testing to transform text and ignore_case into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, ignore_case and returns a boolean result. One notable detail is that it ignores punctuation and whitespace during the comparison."
  },
  {
    "input": "def grouped_totals_1063(pairs, default=0):\n    totals = {}\n    for category, value in pairs:\n        totals[category] = totals.get(category, default) + value\n    return totals\n",
    "output": "The recursive grouped_totals_1063 uses dictionary accumulation with get to transform pairs and default into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts pairs, default and returns a dictionary of totals. One notable detail is that new categories start from the default value."
  },
  {
    "input": "def quick_select_1064(values, k):\n    if len(values) == 1:\n        return values[0]\n    pivot = values[len(values) // 2]\n    lows = [value for value in values if value < pivot]\n    highs = [value for value in values if value > pivot]\n    pivots = [value for value in values if value == pivot]\n    if k < len(lows):\n        return quick_select_1064(lows, k)\n    if k < len(lows) + len(pivots):\n        return pivots[0]\n    return quick_select_1064(highs, k - len(lows) - len(pivots))\n",
    "output": "Inside this class quick_select_1064 uses the quickselect algorithm to transform values and k into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, k and returns the kth smallest element. One notable detail is that k must be within the valid index range."
  },
  {
    "input": "def bfs_levels_1065(graph, start):\n    levels = {start: 0}\n    queue = [start]\n    while queue:\n        node = queue.pop(0)\n        for neighbor in graph.get(node, []):\n            if neighbor not in levels:\n                levels[neighbor] = levels[node] + 1\n                queue.append(neighbor)\n    return levels\n",
    "output": "The function bfs_levels_1065 uses breadth-first search to transform graph and start into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts graph, start and returns a mapping of nodes to their distance from start. One notable detail is that unreachable nodes never appear in the returned dictionary."
  },
  {
    "input": "def retry_request_1066(request_fn, max_attempts=3):\n    attempt = 0\n    while True:\n        try:\n            return request_fn()\n        except Exception:\n            attempt += 1\n            if attempt >= max_attempts:\n                raise\n",
    "output": "This class retry_request_1066 uses retry with a bounded loop to transform request_fn and max_attempts into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts request_fn, max_attempts and returns the successful return value of request_fn. One notable detail is that after max_attempts failures the last exception is raised again."
  },
  {
    "input": "def rate_limit_1067(calls, limit):\n    def decorator(func):\n        state = {'count': 0}\n        def wrapper(*args, **kwargs):\n            if state['count'] >= limit:\n                raise RuntimeError('rate limit exceeded')\n            state['count'] += 1\n            return func(*args, **kwargs)\n        return wrapper\n    return decorator\n",
    "output": "The algorithm rate_limit_1067 uses a rate-limiting decorator to transform calls and limit into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts calls, limit and returns the wrapped function result while under the limit. One notable detail is that the internal counter is shared by every call to the wrapper."
  },
  {
    "input": "def file_writer_1068(path, lines):\n    with open(path, 'w', encoding='utf-8') as handle:\n        for line in lines:\n            handle.write(line + '\\n')\n    return path\n",
    "output": "At its core file_writer_1068 uses file output with a with-block to transform path and lines into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts path, lines and returns the file path after writing. One notable detail is that each line is written with a trailing newline."
  },
  {
    "input": "def json_like_parser_1069(payload, key, default=None):\n    if key in payload:\n        return payload[key]\n    return default\n",
    "output": "Under the hood json_like_parser_1069 uses dictionary lookup with a fallback to transform payload and key into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts payload, key and returns the matching value or the default. One notable detail is that missing keys fall back to the supplied default."
  },
  {
    "input": "def observer_notify_1070(observers, message):\n    for observer in observers:\n        observer.update(message)\n    return len(observers)\n",
    "output": "When called with observer_notify_1070 uses the observer pattern to transform observers and message into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts observers, message and returns the number of notified observers. One notable detail is that every observer object must provide an update method."
  },
  {
    "input": "def strategy_apply_1071(values, strategy):\n    return strategy(values)\n",
    "output": "This utility strategy_apply_1071 uses the strategy pattern to transform values and strategy into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, strategy and returns whatever the strategy callable returns. One notable detail is that strategy must be callable or Python will raise a TypeError."
  },
  {
    "input": "def async_batch_1072(items):\n    import asyncio\n    async def worker(item):\n        await asyncio.sleep(0)\n        return item * 2\n    return asyncio.gather(*(worker(item) for item in items))\n",
    "output": "The method async_batch_1072 uses asyncio.gather orchestration to transform items and worker into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, worker and returns an awaitable gathering doubled items. One notable detail is that the caller must await the returned gather object."
  },
  {
    "input": "class Temperature_1073:\n    def __init__(self, celsius):\n        self.celsius = celsius\n\n    @property\n    def fahrenheit(self):\n        return self.celsius * 9 / 5 + 32\n",
    "output": "Built around Temperature_1073 uses a computed property to transform celsius and fahrenheit into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts celsius, fahrenheit and returns the Celsius value or derived Fahrenheit property. One notable detail is that the property recalculates on every access."
  },
  {
    "input": "def nested_comprehension_1074(matrix):\n    return [value for row in matrix for value in row if value is not None]\n",
    "output": "Designed to nested_comprehension_1074 uses a nested list comprehension to transform matrix and row into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts matrix, row and returns a flattened list without None values. One notable detail is that it preserves row order while flattening."
  },
  {
    "input": "def loading_pipeline_1075(rows):\n    cleaned = []\n    for row in rows:\n        if row and row.get('active'):\n            cleaned.append({**row, 'name': row['name'].strip()})\n    cleaned.sort(key=lambda row: row['name'])\n    return cleaned\n",
    "output": "Working through loading_pipeline_1075 uses a multi-step cleaning pipeline to transform rows and cleaned into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts rows, cleaned and returns a sorted list of active records. One notable detail is that rows missing the name key will still fail at runtime."
  },
  {
    "input": "def recursive_tree_sum_1076(node):\n    if node is None:\n        return 0\n    return node.value + recursive_tree_sum_1076(node.left) + recursive_tree_sum_1076(node.right)\n",
    "output": "The generator recursive_tree_sum_1076 uses recursive tree traversal to transform node and value into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts node, value and returns the sum of values stored in the tree. One notable detail is that the base case returns zero for empty nodes."
  },
  {
    "input": "def factory_build_1077(kind, payload):\n    if kind == 'circle':\n        return {'type': 'circle', 'radius': payload}\n    if kind == 'square':\n        return {'type': 'square', 'side': payload}\n    raise ValueError('unknown kind')\n",
    "output": "This decorator factory_build_1077 uses a factory function to transform kind and payload into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts kind, payload and returns a shape description dictionary. One notable detail is that unknown kind values raise ValueError."
  },
  {
    "input": "def config_loader_1078(settings, key, env):\n    if key in settings:\n        return settings[key]\n    return env.get(key, 'default')\n",
    "output": "The recursive config_loader_1078 uses configuration fallback lookup to transform settings and key into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts settings, key and returns the configured value or a default string. One notable detail is that environment variables win only when settings omit the key."
  },
  {
    "input": "def dfs_path_1079(graph, start, goal):\n    stack = [(start, [start])]\n    visited = set()\n    while stack:\n        node, path = stack.pop()\n        if node == goal:\n            return path\n        if node in visited:\n            continue\n        visited.add(node)\n        for neighbor in graph.get(node, []):\n            stack.append((neighbor, path + [neighbor]))\n    return None\n",
    "output": "Inside this class dfs_path_1079 uses depth-first search path finding to transform graph and start into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts graph, start and returns a path list or None. One notable detail is that if the goal is unreachable, the function returns None."
  },
  {
    "input": "def prime_check_1080(candidate, divisor=2):\n    if candidate < 2:\n        return False\n    while divisor * divisor <= candidate:\n        if candidate % divisor == 0:\n            return False\n        divisor += 1\n    return True\n",
    "output": "The function prime_check_1080 uses trial division prime checking to transform candidate and divisor into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts candidate, divisor and returns a boolean indicating primality. One notable detail is that values below two are treated as not prime."
  },
  {
    "input": "def lcm_value_1081(left, right):\n    if left == 0 or right == 0:\n        return 0\n    a, b = abs(left), abs(right)\n    while b:\n        a, b = b, a % b\n    gcd = a\n    return abs(left * right) // gcd\n",
    "output": "This class lcm_value_1081 uses the least common multiple formula to transform left and right into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts left, right and returns the least common multiple as an integer. One notable detail is that zero inputs short-circuit to zero."
  },
  {
    "input": "def palindrome_normalizer_1082(text, ignore_case=True):\n    cleaned = ''.join(ch.lower() for ch in text if ch.isalnum()) if ignore_case else ''.join(ch for ch in text if ch.isalnum())\n    return cleaned == cleaned[::-1]\n",
    "output": "The algorithm palindrome_normalizer_1082 uses string normalization and palindrome testing to transform text and ignore_case into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, ignore_case and returns a boolean result. One notable detail is that it ignores punctuation and whitespace during the comparison."
  },
  {
    "input": "def grouped_totals_1083(pairs, default=0):\n    totals = {}\n    for category, value in pairs:\n        totals[category] = totals.get(category, default) + value\n    return totals\n",
    "output": "At its core grouped_totals_1083 uses dictionary accumulation with get to transform pairs and default into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts pairs, default and returns a dictionary of totals. One notable detail is that new categories start from the default value."
  },
  {
    "input": "def quick_select_1084(values, k):\n    if len(values) == 1:\n        return values[0]\n    pivot = values[len(values) // 2]\n    lows = [value for value in values if value < pivot]\n    highs = [value for value in values if value > pivot]\n    pivots = [value for value in values if value == pivot]\n    if k < len(lows):\n        return quick_select_1084(lows, k)\n    if k < len(lows) + len(pivots):\n        return pivots[0]\n    return quick_select_1084(highs, k - len(lows) - len(pivots))\n",
    "output": "Under the hood quick_select_1084 uses the quickselect algorithm to transform values and k into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, k and returns the kth smallest element. One notable detail is that k must be within the valid index range."
  },
  {
    "input": "def bfs_levels_1085(graph, start):\n    levels = {start: 0}\n    queue = [start]\n    while queue:\n        node = queue.pop(0)\n        for neighbor in graph.get(node, []):\n            if neighbor not in levels:\n                levels[neighbor] = levels[node] + 1\n                queue.append(neighbor)\n    return levels\n",
    "output": "When called with bfs_levels_1085 uses breadth-first search to transform graph and start into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts graph, start and returns a mapping of nodes to their distance from start. One notable detail is that unreachable nodes never appear in the returned dictionary."
  },
  {
    "input": "def retry_request_1086(request_fn, max_attempts=3):\n    attempt = 0\n    while True:\n        try:\n            return request_fn()\n        except Exception:\n            attempt += 1\n            if attempt >= max_attempts:\n                raise\n",
    "output": "This utility retry_request_1086 uses retry with a bounded loop to transform request_fn and max_attempts into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts request_fn, max_attempts and returns the successful return value of request_fn. One notable detail is that after max_attempts failures the last exception is raised again."
  },
  {
    "input": "def rate_limit_1087(calls, limit):\n    def decorator(func):\n        state = {'count': 0}\n        def wrapper(*args, **kwargs):\n            if state['count'] >= limit:\n                raise RuntimeError('rate limit exceeded')\n            state['count'] += 1\n            return func(*args, **kwargs)\n        return wrapper\n    return decorator\n",
    "output": "The method rate_limit_1087 uses a rate-limiting decorator to transform calls and limit into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts calls, limit and returns the wrapped function result while under the limit. One notable detail is that the internal counter is shared by every call to the wrapper."
  },
  {
    "input": "def file_writer_1088(path, lines):\n    with open(path, 'w', encoding='utf-8') as handle:\n        for line in lines:\n            handle.write(line + '\\n')\n    return path\n",
    "output": "Built around file_writer_1088 uses file output with a with-block to transform path and lines into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts path, lines and returns the file path after writing. One notable detail is that each line is written with a trailing newline."
  },
  {
    "input": "def json_like_parser_1089(payload, key, default=None):\n    if key in payload:\n        return payload[key]\n    return default\n",
    "output": "Designed to json_like_parser_1089 uses dictionary lookup with a fallback to transform payload and key into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts payload, key and returns the matching value or the default. One notable detail is that missing keys fall back to the supplied default."
  },
  {
    "input": "def observer_notify_1090(observers, message):\n    for observer in observers:\n        observer.update(message)\n    return len(observers)\n",
    "output": "Working through observer_notify_1090 uses the observer pattern to transform observers and message into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts observers, message and returns the number of notified observers. One notable detail is that every observer object must provide an update method."
  },
  {
    "input": "def strategy_apply_1091(values, strategy):\n    return strategy(values)\n",
    "output": "The generator strategy_apply_1091 uses the strategy pattern to transform values and strategy into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, strategy and returns whatever the strategy callable returns. One notable detail is that strategy must be callable or Python will raise a TypeError."
  },
  {
    "input": "def async_batch_1092(items):\n    import asyncio\n    async def worker(item):\n        await asyncio.sleep(0)\n        return item * 2\n    return asyncio.gather(*(worker(item) for item in items))\n",
    "output": "This decorator async_batch_1092 uses asyncio.gather orchestration to transform items and worker into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, worker and returns an awaitable gathering doubled items. One notable detail is that the caller must await the returned gather object."
  },
  {
    "input": "class Temperature_1093:\n    def __init__(self, celsius):\n        self.celsius = celsius\n\n    @property\n    def fahrenheit(self):\n        return self.celsius * 9 / 5 + 32\n",
    "output": "The recursive Temperature_1093 uses a computed property to transform celsius and fahrenheit into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts celsius, fahrenheit and returns the Celsius value or derived Fahrenheit property. One notable detail is that the property recalculates on every access."
  },
  {
    "input": "def nested_comprehension_1094(matrix):\n    return [value for row in matrix for value in row if value is not None]\n",
    "output": "Inside this class nested_comprehension_1094 uses a nested list comprehension to transform matrix and row into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts matrix, row and returns a flattened list without None values. One notable detail is that it preserves row order while flattening."
  },
  {
    "input": "def loading_pipeline_1095(rows):\n    cleaned = []\n    for row in rows:\n        if row and row.get('active'):\n            cleaned.append({**row, 'name': row['name'].strip()})\n    cleaned.sort(key=lambda row: row['name'])\n    return cleaned\n",
    "output": "The function loading_pipeline_1095 uses a multi-step cleaning pipeline to transform rows and cleaned into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts rows, cleaned and returns a sorted list of active records. One notable detail is that rows missing the name key will still fail at runtime."
  },
  {
    "input": "def recursive_tree_sum_1096(node):\n    if node is None:\n        return 0\n    return node.value + recursive_tree_sum_1096(node.left) + recursive_tree_sum_1096(node.right)\n",
    "output": "This class recursive_tree_sum_1096 uses recursive tree traversal to transform node and value into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts node, value and returns the sum of values stored in the tree. One notable detail is that the base case returns zero for empty nodes."
  },
  {
    "input": "def factory_build_1097(kind, payload):\n    if kind == 'circle':\n        return {'type': 'circle', 'radius': payload}\n    if kind == 'square':\n        return {'type': 'square', 'side': payload}\n    raise ValueError('unknown kind')\n",
    "output": "The algorithm factory_build_1097 uses a factory function to transform kind and payload into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts kind, payload and returns a shape description dictionary. One notable detail is that unknown kind values raise ValueError."
  },
  {
    "input": "def config_loader_1098(settings, key, env):\n    if key in settings:\n        return settings[key]\n    return env.get(key, 'default')\n",
    "output": "At its core config_loader_1098 uses configuration fallback lookup to transform settings and key into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts settings, key and returns the configured value or a default string. One notable detail is that environment variables win only when settings omit the key."
  },
  {
    "input": "def dfs_path_1099(graph, start, goal):\n    stack = [(start, [start])]\n    visited = set()\n    while stack:\n        node, path = stack.pop()\n        if node == goal:\n            return path\n        if node in visited:\n            continue\n        visited.add(node)\n        for neighbor in graph.get(node, []):\n            stack.append((neighbor, path + [neighbor]))\n    return None\n",
    "output": "Under the hood dfs_path_1099 uses depth-first search path finding to transform graph and start into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts graph, start and returns a path list or None. One notable detail is that if the goal is unreachable, the function returns None."
  },
  {
    "input": "def prime_check_1100(candidate, divisor=2):\n    if candidate < 2:\n        return False\n    while divisor * divisor <= candidate:\n        if candidate % divisor == 0:\n            return False\n        divisor += 1\n    return True\n",
    "output": "When called with prime_check_1100 uses trial division prime checking to transform candidate and divisor into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts candidate, divisor and returns a boolean indicating primality. One notable detail is that values below two are treated as not prime."
  },
  {
    "input": "def lcm_value_1101(left, right):\n    if left == 0 or right == 0:\n        return 0\n    a, b = abs(left), abs(right)\n    while b:\n        a, b = b, a % b\n    gcd = a\n    return abs(left * right) // gcd\n",
    "output": "This utility lcm_value_1101 uses the least common multiple formula to transform left and right into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts left, right and returns the least common multiple as an integer. One notable detail is that zero inputs short-circuit to zero."
  },
  {
    "input": "def palindrome_normalizer_1102(text, ignore_case=True):\n    cleaned = ''.join(ch.lower() for ch in text if ch.isalnum()) if ignore_case else ''.join(ch for ch in text if ch.isalnum())\n    return cleaned == cleaned[::-1]\n",
    "output": "The method palindrome_normalizer_1102 uses string normalization and palindrome testing to transform text and ignore_case into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, ignore_case and returns a boolean result. One notable detail is that it ignores punctuation and whitespace during the comparison."
  },
  {
    "input": "def grouped_totals_1103(pairs, default=0):\n    totals = {}\n    for category, value in pairs:\n        totals[category] = totals.get(category, default) + value\n    return totals\n",
    "output": "Built around grouped_totals_1103 uses dictionary accumulation with get to transform pairs and default into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts pairs, default and returns a dictionary of totals. One notable detail is that new categories start from the default value."
  },
  {
    "input": "def quick_select_1104(values, k):\n    if len(values) == 1:\n        return values[0]\n    pivot = values[len(values) // 2]\n    lows = [value for value in values if value < pivot]\n    highs = [value for value in values if value > pivot]\n    pivots = [value for value in values if value == pivot]\n    if k < len(lows):\n        return quick_select_1104(lows, k)\n    if k < len(lows) + len(pivots):\n        return pivots[0]\n    return quick_select_1104(highs, k - len(lows) - len(pivots))\n",
    "output": "Designed to quick_select_1104 uses the quickselect algorithm to transform values and k into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, k and returns the kth smallest element. One notable detail is that k must be within the valid index range."
  },
  {
    "input": "def bfs_levels_1105(graph, start):\n    levels = {start: 0}\n    queue = [start]\n    while queue:\n        node = queue.pop(0)\n        for neighbor in graph.get(node, []):\n            if neighbor not in levels:\n                levels[neighbor] = levels[node] + 1\n                queue.append(neighbor)\n    return levels\n",
    "output": "Working through bfs_levels_1105 uses breadth-first search to transform graph and start into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts graph, start and returns a mapping of nodes to their distance from start. One notable detail is that unreachable nodes never appear in the returned dictionary."
  },
  {
    "input": "def retry_request_1106(request_fn, max_attempts=3):\n    attempt = 0\n    while True:\n        try:\n            return request_fn()\n        except Exception:\n            attempt += 1\n            if attempt >= max_attempts:\n                raise\n",
    "output": "The generator retry_request_1106 uses retry with a bounded loop to transform request_fn and max_attempts into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts request_fn, max_attempts and returns the successful return value of request_fn. One notable detail is that after max_attempts failures the last exception is raised again."
  },
  {
    "input": "def rate_limit_1107(calls, limit):\n    def decorator(func):\n        state = {'count': 0}\n        def wrapper(*args, **kwargs):\n            if state['count'] >= limit:\n                raise RuntimeError('rate limit exceeded')\n            state['count'] += 1\n            return func(*args, **kwargs)\n        return wrapper\n    return decorator\n",
    "output": "This decorator rate_limit_1107 uses a rate-limiting decorator to transform calls and limit into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts calls, limit and returns the wrapped function result while under the limit. One notable detail is that the internal counter is shared by every call to the wrapper."
  },
  {
    "input": "def file_writer_1108(path, lines):\n    with open(path, 'w', encoding='utf-8') as handle:\n        for line in lines:\n            handle.write(line + '\\n')\n    return path\n",
    "output": "The recursive file_writer_1108 uses file output with a with-block to transform path and lines into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts path, lines and returns the file path after writing. One notable detail is that each line is written with a trailing newline."
  },
  {
    "input": "def json_like_parser_1109(payload, key, default=None):\n    if key in payload:\n        return payload[key]\n    return default\n",
    "output": "Inside this class json_like_parser_1109 uses dictionary lookup with a fallback to transform payload and key into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts payload, key and returns the matching value or the default. One notable detail is that missing keys fall back to the supplied default."
  },
  {
    "input": "def observer_notify_1110(observers, message):\n    for observer in observers:\n        observer.update(message)\n    return len(observers)\n",
    "output": "The function observer_notify_1110 uses the observer pattern to transform observers and message into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts observers, message and returns the number of notified observers. One notable detail is that every observer object must provide an update method."
  },
  {
    "input": "def strategy_apply_1111(values, strategy):\n    return strategy(values)\n",
    "output": "This class strategy_apply_1111 uses the strategy pattern to transform values and strategy into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, strategy and returns whatever the strategy callable returns. One notable detail is that strategy must be callable or Python will raise a TypeError."
  },
  {
    "input": "def async_batch_1112(items):\n    import asyncio\n    async def worker(item):\n        await asyncio.sleep(0)\n        return item * 2\n    return asyncio.gather(*(worker(item) for item in items))\n",
    "output": "The algorithm async_batch_1112 uses asyncio.gather orchestration to transform items and worker into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, worker and returns an awaitable gathering doubled items. One notable detail is that the caller must await the returned gather object."
  },
  {
    "input": "class Temperature_1113:\n    def __init__(self, celsius):\n        self.celsius = celsius\n\n    @property\n    def fahrenheit(self):\n        return self.celsius * 9 / 5 + 32\n",
    "output": "At its core Temperature_1113 uses a computed property to transform celsius and fahrenheit into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts celsius, fahrenheit and returns the Celsius value or derived Fahrenheit property. One notable detail is that the property recalculates on every access."
  },
  {
    "input": "def nested_comprehension_1114(matrix):\n    return [value for row in matrix for value in row if value is not None]\n",
    "output": "Under the hood nested_comprehension_1114 uses a nested list comprehension to transform matrix and row into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts matrix, row and returns a flattened list without None values. One notable detail is that it preserves row order while flattening."
  },
  {
    "input": "def loading_pipeline_1115(rows):\n    cleaned = []\n    for row in rows:\n        if row and row.get('active'):\n            cleaned.append({**row, 'name': row['name'].strip()})\n    cleaned.sort(key=lambda row: row['name'])\n    return cleaned\n",
    "output": "When called with loading_pipeline_1115 uses a multi-step cleaning pipeline to transform rows and cleaned into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts rows, cleaned and returns a sorted list of active records. One notable detail is that rows missing the name key will still fail at runtime."
  },
  {
    "input": "def recursive_tree_sum_1116(node):\n    if node is None:\n        return 0\n    return node.value + recursive_tree_sum_1116(node.left) + recursive_tree_sum_1116(node.right)\n",
    "output": "This utility recursive_tree_sum_1116 uses recursive tree traversal to transform node and value into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts node, value and returns the sum of values stored in the tree. One notable detail is that the base case returns zero for empty nodes."
  },
  {
    "input": "def factory_build_1117(kind, payload):\n    if kind == 'circle':\n        return {'type': 'circle', 'radius': payload}\n    if kind == 'square':\n        return {'type': 'square', 'side': payload}\n    raise ValueError('unknown kind')\n",
    "output": "The method factory_build_1117 uses a factory function to transform kind and payload into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts kind, payload and returns a shape description dictionary. One notable detail is that unknown kind values raise ValueError."
  },
  {
    "input": "def config_loader_1118(settings, key, env):\n    if key in settings:\n        return settings[key]\n    return env.get(key, 'default')\n",
    "output": "Built around config_loader_1118 uses configuration fallback lookup to transform settings and key into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts settings, key and returns the configured value or a default string. One notable detail is that environment variables win only when settings omit the key."
  },
  {
    "input": "def dfs_path_1119(graph, start, goal):\n    stack = [(start, [start])]\n    visited = set()\n    while stack:\n        node, path = stack.pop()\n        if node == goal:\n            return path\n        if node in visited:\n            continue\n        visited.add(node)\n        for neighbor in graph.get(node, []):\n            stack.append((neighbor, path + [neighbor]))\n    return None\n",
    "output": "Designed to dfs_path_1119 uses depth-first search path finding to transform graph and start into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts graph, start and returns a path list or None. One notable detail is that if the goal is unreachable, the function returns None."
  },
  {
    "input": "def prime_check_1120(candidate, divisor=2):\n    if candidate < 2:\n        return False\n    while divisor * divisor <= candidate:\n        if candidate % divisor == 0:\n            return False\n        divisor += 1\n    return True\n",
    "output": "Working through prime_check_1120 uses trial division prime checking to transform candidate and divisor into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts candidate, divisor and returns a boolean indicating primality. One notable detail is that values below two are treated as not prime."
  },
  {
    "input": "def lcm_value_1121(left, right):\n    if left == 0 or right == 0:\n        return 0\n    a, b = abs(left), abs(right)\n    while b:\n        a, b = b, a % b\n    gcd = a\n    return abs(left * right) // gcd\n",
    "output": "The generator lcm_value_1121 uses the least common multiple formula to transform left and right into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts left, right and returns the least common multiple as an integer. One notable detail is that zero inputs short-circuit to zero."
  },
  {
    "input": "def palindrome_normalizer_1122(text, ignore_case=True):\n    cleaned = ''.join(ch.lower() for ch in text if ch.isalnum()) if ignore_case else ''.join(ch for ch in text if ch.isalnum())\n    return cleaned == cleaned[::-1]\n",
    "output": "This decorator palindrome_normalizer_1122 uses string normalization and palindrome testing to transform text and ignore_case into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, ignore_case and returns a boolean result. One notable detail is that it ignores punctuation and whitespace during the comparison."
  },
  {
    "input": "def grouped_totals_1123(pairs, default=0):\n    totals = {}\n    for category, value in pairs:\n        totals[category] = totals.get(category, default) + value\n    return totals\n",
    "output": "The recursive grouped_totals_1123 uses dictionary accumulation with get to transform pairs and default into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts pairs, default and returns a dictionary of totals. One notable detail is that new categories start from the default value."
  },
  {
    "input": "def quick_select_1124(values, k):\n    if len(values) == 1:\n        return values[0]\n    pivot = values[len(values) // 2]\n    lows = [value for value in values if value < pivot]\n    highs = [value for value in values if value > pivot]\n    pivots = [value for value in values if value == pivot]\n    if k < len(lows):\n        return quick_select_1124(lows, k)\n    if k < len(lows) + len(pivots):\n        return pivots[0]\n    return quick_select_1124(highs, k - len(lows) - len(pivots))\n",
    "output": "Inside this class quick_select_1124 uses the quickselect algorithm to transform values and k into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, k and returns the kth smallest element. One notable detail is that k must be within the valid index range."
  },
  {
    "input": "def bfs_levels_1125(graph, start):\n    levels = {start: 0}\n    queue = [start]\n    while queue:\n        node = queue.pop(0)\n        for neighbor in graph.get(node, []):\n            if neighbor not in levels:\n                levels[neighbor] = levels[node] + 1\n                queue.append(neighbor)\n    return levels\n",
    "output": "The function bfs_levels_1125 uses breadth-first search to transform graph and start into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts graph, start and returns a mapping of nodes to their distance from start. One notable detail is that unreachable nodes never appear in the returned dictionary."
  },
  {
    "input": "def retry_request_1126(request_fn, max_attempts=3):\n    attempt = 0\n    while True:\n        try:\n            return request_fn()\n        except Exception:\n            attempt += 1\n            if attempt >= max_attempts:\n                raise\n",
    "output": "This class retry_request_1126 uses retry with a bounded loop to transform request_fn and max_attempts into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts request_fn, max_attempts and returns the successful return value of request_fn. One notable detail is that after max_attempts failures the last exception is raised again."
  },
  {
    "input": "def rate_limit_1127(calls, limit):\n    def decorator(func):\n        state = {'count': 0}\n        def wrapper(*args, **kwargs):\n            if state['count'] >= limit:\n                raise RuntimeError('rate limit exceeded')\n            state['count'] += 1\n            return func(*args, **kwargs)\n        return wrapper\n    return decorator\n",
    "output": "The algorithm rate_limit_1127 uses a rate-limiting decorator to transform calls and limit into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts calls, limit and returns the wrapped function result while under the limit. One notable detail is that the internal counter is shared by every call to the wrapper."
  },
  {
    "input": "def file_writer_1128(path, lines):\n    with open(path, 'w', encoding='utf-8') as handle:\n        for line in lines:\n            handle.write(line + '\\n')\n    return path\n",
    "output": "At its core file_writer_1128 uses file output with a with-block to transform path and lines into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts path, lines and returns the file path after writing. One notable detail is that each line is written with a trailing newline."
  },
  {
    "input": "def json_like_parser_1129(payload, key, default=None):\n    if key in payload:\n        return payload[key]\n    return default\n",
    "output": "Under the hood json_like_parser_1129 uses dictionary lookup with a fallback to transform payload and key into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts payload, key and returns the matching value or the default. One notable detail is that missing keys fall back to the supplied default."
  },
  {
    "input": "def observer_notify_1130(observers, message):\n    for observer in observers:\n        observer.update(message)\n    return len(observers)\n",
    "output": "When called with observer_notify_1130 uses the observer pattern to transform observers and message into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts observers, message and returns the number of notified observers. One notable detail is that every observer object must provide an update method."
  },
  {
    "input": "def strategy_apply_1131(values, strategy):\n    return strategy(values)\n",
    "output": "This utility strategy_apply_1131 uses the strategy pattern to transform values and strategy into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, strategy and returns whatever the strategy callable returns. One notable detail is that strategy must be callable or Python will raise a TypeError."
  },
  {
    "input": "def async_batch_1132(items):\n    import asyncio\n    async def worker(item):\n        await asyncio.sleep(0)\n        return item * 2\n    return asyncio.gather(*(worker(item) for item in items))\n",
    "output": "The method async_batch_1132 uses asyncio.gather orchestration to transform items and worker into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, worker and returns an awaitable gathering doubled items. One notable detail is that the caller must await the returned gather object."
  },
  {
    "input": "class Temperature_1133:\n    def __init__(self, celsius):\n        self.celsius = celsius\n\n    @property\n    def fahrenheit(self):\n        return self.celsius * 9 / 5 + 32\n",
    "output": "Built around Temperature_1133 uses a computed property to transform celsius and fahrenheit into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts celsius, fahrenheit and returns the Celsius value or derived Fahrenheit property. One notable detail is that the property recalculates on every access."
  },
  {
    "input": "def nested_comprehension_1134(matrix):\n    return [value for row in matrix for value in row if value is not None]\n",
    "output": "Designed to nested_comprehension_1134 uses a nested list comprehension to transform matrix and row into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts matrix, row and returns a flattened list without None values. One notable detail is that it preserves row order while flattening."
  },
  {
    "input": "def loading_pipeline_1135(rows):\n    cleaned = []\n    for row in rows:\n        if row and row.get('active'):\n            cleaned.append({**row, 'name': row['name'].strip()})\n    cleaned.sort(key=lambda row: row['name'])\n    return cleaned\n",
    "output": "Working through loading_pipeline_1135 uses a multi-step cleaning pipeline to transform rows and cleaned into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts rows, cleaned and returns a sorted list of active records. One notable detail is that rows missing the name key will still fail at runtime."
  },
  {
    "input": "def recursive_tree_sum_1136(node):\n    if node is None:\n        return 0\n    return node.value + recursive_tree_sum_1136(node.left) + recursive_tree_sum_1136(node.right)\n",
    "output": "The generator recursive_tree_sum_1136 uses recursive tree traversal to transform node and value into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts node, value and returns the sum of values stored in the tree. One notable detail is that the base case returns zero for empty nodes."
  },
  {
    "input": "def factory_build_1137(kind, payload):\n    if kind == 'circle':\n        return {'type': 'circle', 'radius': payload}\n    if kind == 'square':\n        return {'type': 'square', 'side': payload}\n    raise ValueError('unknown kind')\n",
    "output": "This decorator factory_build_1137 uses a factory function to transform kind and payload into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts kind, payload and returns a shape description dictionary. One notable detail is that unknown kind values raise ValueError."
  },
  {
    "input": "def config_loader_1138(settings, key, env):\n    if key in settings:\n        return settings[key]\n    return env.get(key, 'default')\n",
    "output": "The recursive config_loader_1138 uses configuration fallback lookup to transform settings and key into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts settings, key and returns the configured value or a default string. One notable detail is that environment variables win only when settings omit the key."
  },
  {
    "input": "def dfs_path_1139(graph, start, goal):\n    stack = [(start, [start])]\n    visited = set()\n    while stack:\n        node, path = stack.pop()\n        if node == goal:\n            return path\n        if node in visited:\n            continue\n        visited.add(node)\n        for neighbor in graph.get(node, []):\n            stack.append((neighbor, path + [neighbor]))\n    return None\n",
    "output": "Inside this class dfs_path_1139 uses depth-first search path finding to transform graph and start into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts graph, start and returns a path list or None. One notable detail is that if the goal is unreachable, the function returns None."
  },
  {
    "input": "def prime_check_1140(candidate, divisor=2):\n    if candidate < 2:\n        return False\n    while divisor * divisor <= candidate:\n        if candidate % divisor == 0:\n            return False\n        divisor += 1\n    return True\n",
    "output": "The function prime_check_1140 uses trial division prime checking to transform candidate and divisor into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts candidate, divisor and returns a boolean indicating primality. One notable detail is that values below two are treated as not prime."
  },
  {
    "input": "def lcm_value_1141(left, right):\n    if left == 0 or right == 0:\n        return 0\n    a, b = abs(left), abs(right)\n    while b:\n        a, b = b, a % b\n    gcd = a\n    return abs(left * right) // gcd\n",
    "output": "This class lcm_value_1141 uses the least common multiple formula to transform left and right into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts left, right and returns the least common multiple as an integer. One notable detail is that zero inputs short-circuit to zero."
  },
  {
    "input": "def palindrome_normalizer_1142(text, ignore_case=True):\n    cleaned = ''.join(ch.lower() for ch in text if ch.isalnum()) if ignore_case else ''.join(ch for ch in text if ch.isalnum())\n    return cleaned == cleaned[::-1]\n",
    "output": "The algorithm palindrome_normalizer_1142 uses string normalization and palindrome testing to transform text and ignore_case into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, ignore_case and returns a boolean result. One notable detail is that it ignores punctuation and whitespace during the comparison."
  },
  {
    "input": "def grouped_totals_1143(pairs, default=0):\n    totals = {}\n    for category, value in pairs:\n        totals[category] = totals.get(category, default) + value\n    return totals\n",
    "output": "At its core grouped_totals_1143 uses dictionary accumulation with get to transform pairs and default into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts pairs, default and returns a dictionary of totals. One notable detail is that new categories start from the default value."
  },
  {
    "input": "def quick_select_1144(values, k):\n    if len(values) == 1:\n        return values[0]\n    pivot = values[len(values) // 2]\n    lows = [value for value in values if value < pivot]\n    highs = [value for value in values if value > pivot]\n    pivots = [value for value in values if value == pivot]\n    if k < len(lows):\n        return quick_select_1144(lows, k)\n    if k < len(lows) + len(pivots):\n        return pivots[0]\n    return quick_select_1144(highs, k - len(lows) - len(pivots))\n",
    "output": "Under the hood quick_select_1144 uses the quickselect algorithm to transform values and k into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, k and returns the kth smallest element. One notable detail is that k must be within the valid index range."
  },
  {
    "input": "def bfs_levels_1145(graph, start):\n    levels = {start: 0}\n    queue = [start]\n    while queue:\n        node = queue.pop(0)\n        for neighbor in graph.get(node, []):\n            if neighbor not in levels:\n                levels[neighbor] = levels[node] + 1\n                queue.append(neighbor)\n    return levels\n",
    "output": "When called with bfs_levels_1145 uses breadth-first search to transform graph and start into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts graph, start and returns a mapping of nodes to their distance from start. One notable detail is that unreachable nodes never appear in the returned dictionary."
  },
  {
    "input": "def retry_request_1146(request_fn, max_attempts=3):\n    attempt = 0\n    while True:\n        try:\n            return request_fn()\n        except Exception:\n            attempt += 1\n            if attempt >= max_attempts:\n                raise\n",
    "output": "This utility retry_request_1146 uses retry with a bounded loop to transform request_fn and max_attempts into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts request_fn, max_attempts and returns the successful return value of request_fn. One notable detail is that after max_attempts failures the last exception is raised again."
  },
  {
    "input": "def rate_limit_1147(calls, limit):\n    def decorator(func):\n        state = {'count': 0}\n        def wrapper(*args, **kwargs):\n            if state['count'] >= limit:\n                raise RuntimeError('rate limit exceeded')\n            state['count'] += 1\n            return func(*args, **kwargs)\n        return wrapper\n    return decorator\n",
    "output": "The method rate_limit_1147 uses a rate-limiting decorator to transform calls and limit into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts calls, limit and returns the wrapped function result while under the limit. One notable detail is that the internal counter is shared by every call to the wrapper."
  },
  {
    "input": "def file_writer_1148(path, lines):\n    with open(path, 'w', encoding='utf-8') as handle:\n        for line in lines:\n            handle.write(line + '\\n')\n    return path\n",
    "output": "Built around file_writer_1148 uses file output with a with-block to transform path and lines into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts path, lines and returns the file path after writing. One notable detail is that each line is written with a trailing newline."
  },
  {
    "input": "def json_like_parser_1149(payload, key, default=None):\n    if key in payload:\n        return payload[key]\n    return default\n",
    "output": "Designed to json_like_parser_1149 uses dictionary lookup with a fallback to transform payload and key into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts payload, key and returns the matching value or the default. One notable detail is that missing keys fall back to the supplied default."
  },
  {
    "input": "def observer_notify_1150(observers, message):\n    for observer in observers:\n        observer.update(message)\n    return len(observers)\n",
    "output": "Working through observer_notify_1150 uses the observer pattern to transform observers and message into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts observers, message and returns the number of notified observers. One notable detail is that every observer object must provide an update method."
  },
  {
    "input": "def strategy_apply_1151(values, strategy):\n    return strategy(values)\n",
    "output": "The generator strategy_apply_1151 uses the strategy pattern to transform values and strategy into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, strategy and returns whatever the strategy callable returns. One notable detail is that strategy must be callable or Python will raise a TypeError."
  },
  {
    "input": "def async_batch_1152(items):\n    import asyncio\n    async def worker(item):\n        await asyncio.sleep(0)\n        return item * 2\n    return asyncio.gather(*(worker(item) for item in items))\n",
    "output": "This decorator async_batch_1152 uses asyncio.gather orchestration to transform items and worker into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, worker and returns an awaitable gathering doubled items. One notable detail is that the caller must await the returned gather object."
  },
  {
    "input": "class Temperature_1153:\n    def __init__(self, celsius):\n        self.celsius = celsius\n\n    @property\n    def fahrenheit(self):\n        return self.celsius * 9 / 5 + 32\n",
    "output": "The recursive Temperature_1153 uses a computed property to transform celsius and fahrenheit into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts celsius, fahrenheit and returns the Celsius value or derived Fahrenheit property. One notable detail is that the property recalculates on every access."
  },
  {
    "input": "def nested_comprehension_1154(matrix):\n    return [value for row in matrix for value in row if value is not None]\n",
    "output": "Inside this class nested_comprehension_1154 uses a nested list comprehension to transform matrix and row into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts matrix, row and returns a flattened list without None values. One notable detail is that it preserves row order while flattening."
  },
  {
    "input": "def loading_pipeline_1155(rows):\n    cleaned = []\n    for row in rows:\n        if row and row.get('active'):\n            cleaned.append({**row, 'name': row['name'].strip()})\n    cleaned.sort(key=lambda row: row['name'])\n    return cleaned\n",
    "output": "The function loading_pipeline_1155 uses a multi-step cleaning pipeline to transform rows and cleaned into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts rows, cleaned and returns a sorted list of active records. One notable detail is that rows missing the name key will still fail at runtime."
  },
  {
    "input": "def recursive_tree_sum_1156(node):\n    if node is None:\n        return 0\n    return node.value + recursive_tree_sum_1156(node.left) + recursive_tree_sum_1156(node.right)\n",
    "output": "This class recursive_tree_sum_1156 uses recursive tree traversal to transform node and value into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts node, value and returns the sum of values stored in the tree. One notable detail is that the base case returns zero for empty nodes."
  },
  {
    "input": "def factory_build_1157(kind, payload):\n    if kind == 'circle':\n        return {'type': 'circle', 'radius': payload}\n    if kind == 'square':\n        return {'type': 'square', 'side': payload}\n    raise ValueError('unknown kind')\n",
    "output": "The algorithm factory_build_1157 uses a factory function to transform kind and payload into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts kind, payload and returns a shape description dictionary. One notable detail is that unknown kind values raise ValueError."
  },
  {
    "input": "def config_loader_1158(settings, key, env):\n    if key in settings:\n        return settings[key]\n    return env.get(key, 'default')\n",
    "output": "At its core config_loader_1158 uses configuration fallback lookup to transform settings and key into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts settings, key and returns the configured value or a default string. One notable detail is that environment variables win only when settings omit the key."
  },
  {
    "input": "def dfs_path_1159(graph, start, goal):\n    stack = [(start, [start])]\n    visited = set()\n    while stack:\n        node, path = stack.pop()\n        if node == goal:\n            return path\n        if node in visited:\n            continue\n        visited.add(node)\n        for neighbor in graph.get(node, []):\n            stack.append((neighbor, path + [neighbor]))\n    return None\n",
    "output": "Under the hood dfs_path_1159 uses depth-first search path finding to transform graph and start into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts graph, start and returns a path list or None. One notable detail is that if the goal is unreachable, the function returns None."
  },
  {
    "input": "def prime_check_1160(candidate, divisor=2):\n    if candidate < 2:\n        return False\n    while divisor * divisor <= candidate:\n        if candidate % divisor == 0:\n            return False\n        divisor += 1\n    return True\n",
    "output": "When called with prime_check_1160 uses trial division prime checking to transform candidate and divisor into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts candidate, divisor and returns a boolean indicating primality. One notable detail is that values below two are treated as not prime."
  },
  {
    "input": "def lcm_value_1161(left, right):\n    if left == 0 or right == 0:\n        return 0\n    a, b = abs(left), abs(right)\n    while b:\n        a, b = b, a % b\n    gcd = a\n    return abs(left * right) // gcd\n",
    "output": "This utility lcm_value_1161 uses the least common multiple formula to transform left and right into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts left, right and returns the least common multiple as an integer. One notable detail is that zero inputs short-circuit to zero."
  },
  {
    "input": "def palindrome_normalizer_1162(text, ignore_case=True):\n    cleaned = ''.join(ch.lower() for ch in text if ch.isalnum()) if ignore_case else ''.join(ch for ch in text if ch.isalnum())\n    return cleaned == cleaned[::-1]\n",
    "output": "The method palindrome_normalizer_1162 uses string normalization and palindrome testing to transform text and ignore_case into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, ignore_case and returns a boolean result. One notable detail is that it ignores punctuation and whitespace during the comparison."
  },
  {
    "input": "def grouped_totals_1163(pairs, default=0):\n    totals = {}\n    for category, value in pairs:\n        totals[category] = totals.get(category, default) + value\n    return totals\n",
    "output": "Built around grouped_totals_1163 uses dictionary accumulation with get to transform pairs and default into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts pairs, default and returns a dictionary of totals. One notable detail is that new categories start from the default value."
  },
  {
    "input": "def quick_select_1164(values, k):\n    if len(values) == 1:\n        return values[0]\n    pivot = values[len(values) // 2]\n    lows = [value for value in values if value < pivot]\n    highs = [value for value in values if value > pivot]\n    pivots = [value for value in values if value == pivot]\n    if k < len(lows):\n        return quick_select_1164(lows, k)\n    if k < len(lows) + len(pivots):\n        return pivots[0]\n    return quick_select_1164(highs, k - len(lows) - len(pivots))\n",
    "output": "Designed to quick_select_1164 uses the quickselect algorithm to transform values and k into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, k and returns the kth smallest element. One notable detail is that k must be within the valid index range."
  },
  {
    "input": "def bfs_levels_1165(graph, start):\n    levels = {start: 0}\n    queue = [start]\n    while queue:\n        node = queue.pop(0)\n        for neighbor in graph.get(node, []):\n            if neighbor not in levels:\n                levels[neighbor] = levels[node] + 1\n                queue.append(neighbor)\n    return levels\n",
    "output": "Working through bfs_levels_1165 uses breadth-first search to transform graph and start into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts graph, start and returns a mapping of nodes to their distance from start. One notable detail is that unreachable nodes never appear in the returned dictionary."
  },
  {
    "input": "def retry_request_1166(request_fn, max_attempts=3):\n    attempt = 0\n    while True:\n        try:\n            return request_fn()\n        except Exception:\n            attempt += 1\n            if attempt >= max_attempts:\n                raise\n",
    "output": "The generator retry_request_1166 uses retry with a bounded loop to transform request_fn and max_attempts into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts request_fn, max_attempts and returns the successful return value of request_fn. One notable detail is that after max_attempts failures the last exception is raised again."
  },
  {
    "input": "def rate_limit_1167(calls, limit):\n    def decorator(func):\n        state = {'count': 0}\n        def wrapper(*args, **kwargs):\n            if state['count'] >= limit:\n                raise RuntimeError('rate limit exceeded')\n            state['count'] += 1\n            return func(*args, **kwargs)\n        return wrapper\n    return decorator\n",
    "output": "This decorator rate_limit_1167 uses a rate-limiting decorator to transform calls and limit into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts calls, limit and returns the wrapped function result while under the limit. One notable detail is that the internal counter is shared by every call to the wrapper."
  },
  {
    "input": "def file_writer_1168(path, lines):\n    with open(path, 'w', encoding='utf-8') as handle:\n        for line in lines:\n            handle.write(line + '\\n')\n    return path\n",
    "output": "The recursive file_writer_1168 uses file output with a with-block to transform path and lines into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts path, lines and returns the file path after writing. One notable detail is that each line is written with a trailing newline."
  },
  {
    "input": "def json_like_parser_1169(payload, key, default=None):\n    if key in payload:\n        return payload[key]\n    return default\n",
    "output": "Inside this class json_like_parser_1169 uses dictionary lookup with a fallback to transform payload and key into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts payload, key and returns the matching value or the default. One notable detail is that missing keys fall back to the supplied default."
  },
  {
    "input": "def observer_notify_1170(observers, message):\n    for observer in observers:\n        observer.update(message)\n    return len(observers)\n",
    "output": "The function observer_notify_1170 uses the observer pattern to transform observers and message into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts observers, message and returns the number of notified observers. One notable detail is that every observer object must provide an update method."
  },
  {
    "input": "def strategy_apply_1171(values, strategy):\n    return strategy(values)\n",
    "output": "This class strategy_apply_1171 uses the strategy pattern to transform values and strategy into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, strategy and returns whatever the strategy callable returns. One notable detail is that strategy must be callable or Python will raise a TypeError."
  },
  {
    "input": "def async_batch_1172(items):\n    import asyncio\n    async def worker(item):\n        await asyncio.sleep(0)\n        return item * 2\n    return asyncio.gather(*(worker(item) for item in items))\n",
    "output": "The algorithm async_batch_1172 uses asyncio.gather orchestration to transform items and worker into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, worker and returns an awaitable gathering doubled items. One notable detail is that the caller must await the returned gather object."
  },
  {
    "input": "class Temperature_1173:\n    def __init__(self, celsius):\n        self.celsius = celsius\n\n    @property\n    def fahrenheit(self):\n        return self.celsius * 9 / 5 + 32\n",
    "output": "At its core Temperature_1173 uses a computed property to transform celsius and fahrenheit into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts celsius, fahrenheit and returns the Celsius value or derived Fahrenheit property. One notable detail is that the property recalculates on every access."
  },
  {
    "input": "def nested_comprehension_1174(matrix):\n    return [value for row in matrix for value in row if value is not None]\n",
    "output": "Under the hood nested_comprehension_1174 uses a nested list comprehension to transform matrix and row into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts matrix, row and returns a flattened list without None values. One notable detail is that it preserves row order while flattening."
  },
  {
    "input": "def loading_pipeline_1175(rows):\n    cleaned = []\n    for row in rows:\n        if row and row.get('active'):\n            cleaned.append({**row, 'name': row['name'].strip()})\n    cleaned.sort(key=lambda row: row['name'])\n    return cleaned\n",
    "output": "When called with loading_pipeline_1175 uses a multi-step cleaning pipeline to transform rows and cleaned into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts rows, cleaned and returns a sorted list of active records. One notable detail is that rows missing the name key will still fail at runtime."
  },
  {
    "input": "def recursive_tree_sum_1176(node):\n    if node is None:\n        return 0\n    return node.value + recursive_tree_sum_1176(node.left) + recursive_tree_sum_1176(node.right)\n",
    "output": "This utility recursive_tree_sum_1176 uses recursive tree traversal to transform node and value into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts node, value and returns the sum of values stored in the tree. One notable detail is that the base case returns zero for empty nodes."
  },
  {
    "input": "def factory_build_1177(kind, payload):\n    if kind == 'circle':\n        return {'type': 'circle', 'radius': payload}\n    if kind == 'square':\n        return {'type': 'square', 'side': payload}\n    raise ValueError('unknown kind')\n",
    "output": "The method factory_build_1177 uses a factory function to transform kind and payload into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts kind, payload and returns a shape description dictionary. One notable detail is that unknown kind values raise ValueError."
  },
  {
    "input": "def config_loader_1178(settings, key, env):\n    if key in settings:\n        return settings[key]\n    return env.get(key, 'default')\n",
    "output": "Built around config_loader_1178 uses configuration fallback lookup to transform settings and key into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts settings, key and returns the configured value or a default string. One notable detail is that environment variables win only when settings omit the key."
  },
  {
    "input": "def dfs_path_1179(graph, start, goal):\n    stack = [(start, [start])]\n    visited = set()\n    while stack:\n        node, path = stack.pop()\n        if node == goal:\n            return path\n        if node in visited:\n            continue\n        visited.add(node)\n        for neighbor in graph.get(node, []):\n            stack.append((neighbor, path + [neighbor]))\n    return None\n",
    "output": "Designed to dfs_path_1179 uses depth-first search path finding to transform graph and start into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts graph, start and returns a path list or None. One notable detail is that if the goal is unreachable, the function returns None."
  },
  {
    "input": "def prime_check_1180(candidate, divisor=2):\n    if candidate < 2:\n        return False\n    while divisor * divisor <= candidate:\n        if candidate % divisor == 0:\n            return False\n        divisor += 1\n    return True\n",
    "output": "Working through prime_check_1180 uses trial division prime checking to transform candidate and divisor into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts candidate, divisor and returns a boolean indicating primality. One notable detail is that values below two are treated as not prime."
  },
  {
    "input": "def lcm_value_1181(left, right):\n    if left == 0 or right == 0:\n        return 0\n    a, b = abs(left), abs(right)\n    while b:\n        a, b = b, a % b\n    gcd = a\n    return abs(left * right) // gcd\n",
    "output": "The generator lcm_value_1181 uses the least common multiple formula to transform left and right into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts left, right and returns the least common multiple as an integer. One notable detail is that zero inputs short-circuit to zero."
  },
  {
    "input": "def palindrome_normalizer_1182(text, ignore_case=True):\n    cleaned = ''.join(ch.lower() for ch in text if ch.isalnum()) if ignore_case else ''.join(ch for ch in text if ch.isalnum())\n    return cleaned == cleaned[::-1]\n",
    "output": "This decorator palindrome_normalizer_1182 uses string normalization and palindrome testing to transform text and ignore_case into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, ignore_case and returns a boolean result. One notable detail is that it ignores punctuation and whitespace during the comparison."
  },
  {
    "input": "def grouped_totals_1183(pairs, default=0):\n    totals = {}\n    for category, value in pairs:\n        totals[category] = totals.get(category, default) + value\n    return totals\n",
    "output": "The recursive grouped_totals_1183 uses dictionary accumulation with get to transform pairs and default into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts pairs, default and returns a dictionary of totals. One notable detail is that new categories start from the default value."
  },
  {
    "input": "def quick_select_1184(values, k):\n    if len(values) == 1:\n        return values[0]\n    pivot = values[len(values) // 2]\n    lows = [value for value in values if value < pivot]\n    highs = [value for value in values if value > pivot]\n    pivots = [value for value in values if value == pivot]\n    if k < len(lows):\n        return quick_select_1184(lows, k)\n    if k < len(lows) + len(pivots):\n        return pivots[0]\n    return quick_select_1184(highs, k - len(lows) - len(pivots))\n",
    "output": "Inside this class quick_select_1184 uses the quickselect algorithm to transform values and k into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, k and returns the kth smallest element. One notable detail is that k must be within the valid index range."
  },
  {
    "input": "def bfs_levels_1185(graph, start):\n    levels = {start: 0}\n    queue = [start]\n    while queue:\n        node = queue.pop(0)\n        for neighbor in graph.get(node, []):\n            if neighbor not in levels:\n                levels[neighbor] = levels[node] + 1\n                queue.append(neighbor)\n    return levels\n",
    "output": "The function bfs_levels_1185 uses breadth-first search to transform graph and start into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts graph, start and returns a mapping of nodes to their distance from start. One notable detail is that unreachable nodes never appear in the returned dictionary."
  },
  {
    "input": "def retry_request_1186(request_fn, max_attempts=3):\n    attempt = 0\n    while True:\n        try:\n            return request_fn()\n        except Exception:\n            attempt += 1\n            if attempt >= max_attempts:\n                raise\n",
    "output": "This class retry_request_1186 uses retry with a bounded loop to transform request_fn and max_attempts into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts request_fn, max_attempts and returns the successful return value of request_fn. One notable detail is that after max_attempts failures the last exception is raised again."
  },
  {
    "input": "def rate_limit_1187(calls, limit):\n    def decorator(func):\n        state = {'count': 0}\n        def wrapper(*args, **kwargs):\n            if state['count'] >= limit:\n                raise RuntimeError('rate limit exceeded')\n            state['count'] += 1\n            return func(*args, **kwargs)\n        return wrapper\n    return decorator\n",
    "output": "The algorithm rate_limit_1187 uses a rate-limiting decorator to transform calls and limit into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts calls, limit and returns the wrapped function result while under the limit. One notable detail is that the internal counter is shared by every call to the wrapper."
  },
  {
    "input": "def file_writer_1188(path, lines):\n    with open(path, 'w', encoding='utf-8') as handle:\n        for line in lines:\n            handle.write(line + '\\n')\n    return path\n",
    "output": "At its core file_writer_1188 uses file output with a with-block to transform path and lines into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts path, lines and returns the file path after writing. One notable detail is that each line is written with a trailing newline."
  },
  {
    "input": "def json_like_parser_1189(payload, key, default=None):\n    if key in payload:\n        return payload[key]\n    return default\n",
    "output": "Under the hood json_like_parser_1189 uses dictionary lookup with a fallback to transform payload and key into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts payload, key and returns the matching value or the default. One notable detail is that missing keys fall back to the supplied default."
  },
  {
    "input": "def observer_notify_1190(observers, message):\n    for observer in observers:\n        observer.update(message)\n    return len(observers)\n",
    "output": "When called with observer_notify_1190 uses the observer pattern to transform observers and message into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts observers, message and returns the number of notified observers. One notable detail is that every observer object must provide an update method."
  },
  {
    "input": "def strategy_apply_1191(values, strategy):\n    return strategy(values)\n",
    "output": "This utility strategy_apply_1191 uses the strategy pattern to transform values and strategy into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, strategy and returns whatever the strategy callable returns. One notable detail is that strategy must be callable or Python will raise a TypeError."
  },
  {
    "input": "def async_batch_1192(items):\n    import asyncio\n    async def worker(item):\n        await asyncio.sleep(0)\n        return item * 2\n    return asyncio.gather(*(worker(item) for item in items))\n",
    "output": "The method async_batch_1192 uses asyncio.gather orchestration to transform items and worker into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts items, worker and returns an awaitable gathering doubled items. One notable detail is that the caller must await the returned gather object."
  },
  {
    "input": "class Temperature_1193:\n    def __init__(self, celsius):\n        self.celsius = celsius\n\n    @property\n    def fahrenheit(self):\n        return self.celsius * 9 / 5 + 32\n",
    "output": "Built around Temperature_1193 uses a computed property to transform celsius and fahrenheit into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts celsius, fahrenheit and returns the Celsius value or derived Fahrenheit property. One notable detail is that the property recalculates on every access."
  },
  {
    "input": "def nested_comprehension_1194(matrix):\n    return [value for row in matrix for value in row if value is not None]\n",
    "output": "Designed to nested_comprehension_1194 uses a nested list comprehension to transform matrix and row into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts matrix, row and returns a flattened list without None values. One notable detail is that it preserves row order while flattening."
  },
  {
    "input": "def loading_pipeline_1195(rows):\n    cleaned = []\n    for row in rows:\n        if row and row.get('active'):\n            cleaned.append({**row, 'name': row['name'].strip()})\n    cleaned.sort(key=lambda row: row['name'])\n    return cleaned\n",
    "output": "Working through loading_pipeline_1195 uses a multi-step cleaning pipeline to transform rows and cleaned into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts rows, cleaned and returns a sorted list of active records. One notable detail is that rows missing the name key will still fail at runtime."
  },
  {
    "input": "def recursive_tree_sum_1196(node):\n    if node is None:\n        return 0\n    return node.value + recursive_tree_sum_1196(node.left) + recursive_tree_sum_1196(node.right)\n",
    "output": "The generator recursive_tree_sum_1196 uses recursive tree traversal to transform node and value into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts node, value and returns the sum of values stored in the tree. One notable detail is that the base case returns zero for empty nodes."
  },
  {
    "input": "def factory_build_1197(kind, payload):\n    if kind == 'circle':\n        return {'type': 'circle', 'radius': payload}\n    if kind == 'square':\n        return {'type': 'square', 'side': payload}\n    raise ValueError('unknown kind')\n",
    "output": "This decorator factory_build_1197 uses a factory function to transform kind and payload into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts kind, payload and returns a shape description dictionary. One notable detail is that unknown kind values raise ValueError."
  },
  {
    "input": "def config_loader_1198(settings, key, env):\n    if key in settings:\n        return settings[key]\n    return env.get(key, 'default')\n",
    "output": "The recursive config_loader_1198 uses configuration fallback lookup to transform settings and key into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts settings, key and returns the configured value or a default string. One notable detail is that environment variables win only when settings omit the key."
  },
  {
    "input": "def dfs_path_1199(graph, start, goal):\n    stack = [(start, [start])]\n    visited = set()\n    while stack:\n        node, path = stack.pop()\n        if node == goal:\n            return path\n        if node in visited:\n            continue\n        visited.add(node)\n        for neighbor in graph.get(node, []):\n            stack.append((neighbor, path + [neighbor]))\n    return None\n",
    "output": "Inside this class dfs_path_1199 uses depth-first search path finding to transform graph and start into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts graph, start and returns a path list or None. One notable detail is that if the goal is unreachable, the function returns None."
  },
  {
    "input": "def prime_check_1200(candidate, divisor=2):\n    if candidate < 2:\n        return False\n    while divisor * divisor <= candidate:\n        if candidate % divisor == 0:\n            return False\n        divisor += 1\n    return True\n",
    "output": "The function prime_check_1200 uses trial division prime checking to transform candidate and divisor into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts candidate, divisor and returns a boolean indicating primality. One notable detail is that values below two are treated as not prime."
  },
  {
    "input": "def lcm_value_1201(left, right):\n    if left == 0 or right == 0:\n        return 0\n    a, b = abs(left), abs(right)\n    while b:\n        a, b = b, a % b\n    gcd = a\n    return abs(left * right) // gcd\n",
    "output": "This class lcm_value_1201 uses the least common multiple formula to transform left and right into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts left, right and returns the least common multiple as an integer. One notable detail is that zero inputs short-circuit to zero."
  },
  {
    "input": "def palindrome_normalizer_1202(text, ignore_case=True):\n    cleaned = ''.join(ch.lower() for ch in text if ch.isalnum()) if ignore_case else ''.join(ch for ch in text if ch.isalnum())\n    return cleaned == cleaned[::-1]\n",
    "output": "The algorithm palindrome_normalizer_1202 uses string normalization and palindrome testing to transform text and ignore_case into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts text, ignore_case and returns a boolean result. One notable detail is that it ignores punctuation and whitespace during the comparison."
  },
  {
    "input": "def grouped_totals_1203(pairs, default=0):\n    totals = {}\n    for category, value in pairs:\n        totals[category] = totals.get(category, default) + value\n    return totals\n",
    "output": "At its core grouped_totals_1203 uses dictionary accumulation with get to transform pairs and default into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts pairs, default and returns a dictionary of totals. One notable detail is that new categories start from the default value."
  },
  {
    "input": "def quick_select_1204(values, k):\n    if len(values) == 1:\n        return values[0]\n    pivot = values[len(values) // 2]\n    lows = [value for value in values if value < pivot]\n    highs = [value for value in values if value > pivot]\n    pivots = [value for value in values if value == pivot]\n    if k < len(lows):\n        return quick_select_1204(lows, k)\n    if k < len(lows) + len(pivots):\n        return pivots[0]\n    return quick_select_1204(highs, k - len(lows) - len(pivots))\n",
    "output": "Under the hood quick_select_1204 uses the quickselect algorithm to transform values and k into a result. It begins with the function's main control flow, then follows each step until the final value is produced. It accepts values, k and returns the kth smallest element. One notable detail is that k must be within the valid index range."
  }
]